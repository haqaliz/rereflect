# Implementation Plan — crm-provider-generalization (2026-07-01)

Spec: `./spec.md` · PRD: `../prd.md`
**Build order: aspect 1 of 4 (first).** Owns the first Alembic migration in the chain (chains off head `d4e5f6a7b8c9`).

## 0. Project setup
- **Service:** `services/backend-api` only (+ the worker-service model mirror file).
- **DB change:** YES — one migration adding `crm_enrichment.provider VARCHAR(50) NOT NULL DEFAULT 'hubspot'`.
- **No new deps.**

## Phase 1 — RED: score + serializer stability characterization test
- **Goal:** lock "generalization does not move existing HubSpot output" before any code.
- **Files:** `services/backend-api/tests/test_crm_provider_generalization.py` (new).
- **Steps:**
  1. Fixture: org + customer + a `CrmEnrichment` row (semantic fields incl. `renewal_date`; leave `provider` unset so it defaults). Reuse existing CRM/health fixtures — see `tests/test_health_crm_component.py` for setup.
  2. Assert `compute_health_score(org_id, email, db)` returns the captured snapshot (score, components incl. `crm_component`, risk, confidence) — hardcode the snapshot.
  3. Assert `serialize_customer_profile(...)` returns the 7 `crm_*` fields with the expected values (from `customer_profile_serializer.py:91-130`).
  4. These must PASS today and keep passing through Phases 2-4.
- **Validation:** `pytest tests/test_crm_provider_generalization.py -v`.
- **Commit:** `test(crm): characterization baseline before provider generalization`

## Phase 2 — provider column + migration
- **Files:**
  - `services/backend-api/src/models/crm_enrichment.py` — add `provider = Column(String(50), nullable=False, server_default="hubspot")` (near `:34`).
  - `services/worker-service/src/models/__init__.py` — add the identical column to the mirror `CrmEnrichment` (`:856-887`) — REQUIRED for the parity CI test.
  - `services/backend-api/alembic/versions/<rev>_add_provider_to_crm_enrichment.py` (new; `down_revision='d4e5f6a7b8c9'`).
- **Steps:** add the column both places; `alembic revision -m "add provider to crm_enrichment"`; fill `upgrade()` = `op.add_column('crm_enrichment', sa.Column('provider', sa.String(50), nullable=False, server_default='hubspot'))`, `downgrade()` = drop.
- **Validation:** `alembic upgrade head → downgrade -1 → upgrade head` clean; Phase 1 test still green; the backend/worker parity test green (grep tests for `crm_enrichment_columns_match`).
- **Commit:** `feat(crm): provider column on crm_enrichment (default hubspot), worker mirror + migration`

## Phase 3 — provider-driven timeline events
- **Files:** `services/backend-api/src/services/customer_timeline_service.py` (`_fetch_crm_events`, `:435-491`).
- **Steps:** select `provider` in the query; replace `source="hubspot"` (`:471,484`) with `source=row.provider`; replace "CRM contact synced from HubSpot" text (`:469-470`) with `f"CRM contact synced from {row.provider.title()}"` (and same for renewal event).
- **Validation:** add a test — a `provider='salesforce'` row produces events with `source='salesforce'` + "Salesforce" in the description; a `provider='hubspot'` row still says HubSpot. Phase 1 green.
- **Commit:** `feat(crm): timeline CRM events use row.provider (not hardcoded hubspot)`

## Phase 4 — crm_provider on serializer + API responses
- **Files:**
  - `services/backend-api/src/services/customer_profile_serializer.py` (`_read_crm_fields`, `:91-130`) — add `crm_provider` to the returned dict.
  - `services/backend-api/src/api/routes/customers.py` (`CustomerProfileResponse`, `:108-115`) — add `crm_provider: Optional[str] = None`.
  - `services/backend-api/src/api/routes/public_api.py` (`PublicCustomerProfile360`, `:181-188`) — add the same (keep parity).
  - Update the two "(HubSpot)" comments → "(HubSpot / Salesforce)".
- **Validation:** v1 + public profile responses include `crm_provider`; parity test green; whole suite `pytest tests/ -v` green.
- **Commit:** `feat(crm): expose crm_provider on v1 + public Customer 360 profile`

## File-by-file build order
1. `tests/test_crm_provider_generalization.py`
2. `models/crm_enrichment.py` + `worker-service/src/models/__init__.py` mirror
3. `alembic/versions/<rev>_add_provider_to_crm_enrichment.py`
4. `services/customer_timeline_service.py`
5. `services/customer_profile_serializer.py` + `routes/customers.py` + `routes/public_api.py`

## Edge cases & errors
- Existing rows read back `provider='hubspot'` via server_default — no backfill needed.
- Do NOT touch `_compute_crm_component` / `WEIGHTS` / `_get_org_weights` / health-weights API.
- Multi-tenancy unchanged (reads already `(org, email)`-scoped).

## Agent execution notes
- **Checkpoint after Phase 1** (human glance at the snapshot).
- This migration is the chain head for the feature; integrator hands its revision id to `salesforce-connection`.
- Remember the worker mirror + parity test — the most common miss.
