# Implementation Plan — salesforce-frontend (2026-07-01)

Spec: `./spec.md` · PRD: `../prd.md`
**Build order: aspect 4 of 4 (last).** No DB. Consumes aspect-2 endpoints; buildable vs mocked responses.

## 0. Project setup
- **Service:** `services/frontend-web` only.
- **No new deps** (axios, React Query, shadcn, Recharts present).
- Tests: Vitest (`npm run test`), lint (`npm run lint`), build (`npm run build`).

## Phase 1 — API client + types (RED)
- **Files:** `services/frontend-web/lib/api/salesforce.ts` (new); `lib/api/__tests__/salesforce.test.ts` (new).
- **Steps (status/test/disconnect shape from `lib/api/hubspot.ts`; connect from `lib/api/linear.ts:97-99,122-125`):** types `SalesforceConnectionStatus` (`connected, instance_url, sf_org_id, last_synced_at, last_sync_status, last_error, contacts_synced, contacts_matched, connected_at`); `salesforceAPI`: `getConnectUrl()`→`{auth_url}`, `getStatus()`, `disconnect()`, `test()`. NO `connect(token)`.
- **Validation:** `npm run test` (mock axios) — methods hit correct paths.
- **Commit:** `feat(salesforce-ui): salesforce API client + types (OAuth getConnectUrl)`

## Phase 2 — integrations index tile
- **Files:** `app/(dashboard)/settings/integrations/page.tsx`.
- **Steps:** add `salesforceStatus` state (mirror `hubspotStatus` `:56`); fetch in the `Promise.allSettled` block (mirror `:104-117`); Active tile (mirror HubSpot `:376-431`) + Available tile (mirror `:644-669`), Salesforce-blue (`#00A1E0`), route to `/settings/integrations/salesforce`. Include in empty-state gating (mirror `:218`).
- **Validation:** `npm run test` + `npm run lint`; tile renders available/active by mocked status.
- **Commit:** `feat(salesforce-ui): Salesforce tile on integrations index`

## Phase 3 — connect/detail page (OAuth CTA)
- **Files:** `app/(dashboard)/settings/integrations/salesforce/page.tsx` (new); optional `components/icons/SalesforceIcon.tsx`.
- **Steps:** RBAC member-redirect (mirror `hubspot/page.tsx:57,60-64`); **disconnected state = OAuth CTA** — "Connect with Salesforce" button → `salesforceAPI.getConnectUrl()` → `window.location.href = auth_url` (mirror `linear/page.tsx:339-350`); connected-state grid (instance_url, sf_org_id, connected_at, last_synced_at, contacts_synced/matched) + Test / Disconnect + confirm dialog (mirror hubspot page); handle `?connected=1` / `?oauth_error=` return params (reuse the generic handler).
- **Validation:** `npm run test` + `npm run lint`.
- **Commit:** `feat(salesforce-ui): Salesforce connect/detail page (OAuth redirect CTA)`

## Phase 4 — provider-neutral copy + badge + docs
- **Files:** `components/customers/CrmCompanyCard.tsx` (`:50` empty-state copy → "Connect a CRM (HubSpot or Salesforce) in Settings…"; add `crm_provider` badge when present); `lib/api/customers.ts:100` comment; `app/(dashboard)/customers/[email]/page.tsx:749` comment; `docs/SELF_HOSTING.md` (Salesforce section: Connected App setup, scopes `refresh_token offline_access api`, redirect URI, verify note).
- **Validation:** `npm run test` + `npm run lint` + `npm run build` clean.
- **Commit:** `feat(salesforce-ui): provider-neutral CRM card copy + crm_provider badge + self-host docs`

## File-by-file build order
1. `lib/api/salesforce.ts` (+ test)
2. `app/(dashboard)/settings/integrations/page.tsx` (tile)
3. `app/(dashboard)/settings/integrations/salesforce/page.tsx` (+ optional `SalesforceIcon.tsx`)
4. `components/customers/CrmCompanyCard.tsx` + comments + `docs/SELF_HOSTING.md`

## Edge cases & errors
- No Salesforce connected → tile shows "Available"; card empty state neutral.
- `crm_provider` absent on older payloads → render without badge, no crash.
- OAuth return params handled via existing generic `oauth_error` mechanism.
- Never hardcode colors (CSS vars); strict TS; wrap `useSearchParams` in Suspense if used.

## Agent execution notes
- Buildable against mocked API responses before aspects 2-3 merge; integrate last.
- Update existing integrations-page test/snapshots intentionally (new tile).
- Checkpoint after Phase 3 (visual review of the OAuth connect flow).
