# Implementation Plan — harvester-core

**Aspect spec:** `./spec.md` · **PRD:** `../prd.md` (M2, M4) · **Date:** 2026-07-15 · **Branch:** `feat/crm-churn-labels`
**Service touched:** services/worker-service

Strict TDD (RED → GREEN → REFACTOR). Keep the branch green after every phase.


## 0. Impact analysis (from the dig — verified against source)

Wave 1 is merged; re-verified in this worktree, all file:line-checked.

**Confirmed present (do not re-plan):** `ChurnLabelSuggestion` worker mirror + its UNIQUE
`models/__init__.py:726-748`, migration `f7a8b9c0d1e2` · `churn_labels_enabled`/`churn_label_config`
`:1086-1087` (HS), `:1132-1133` (SF) · `get_lost_opportunities` `clients/salesforce.py:304-318`
(selects `IsClosed, IsWon, Type, CloseDate, …`; `IsClosed=true AND IsWon=false`) ·
`get_opportunity_type_values` `:431-446` · `get_closed_lost_deals_for_company`
`clients/hubspot.py:293-302` (filters `dealstage=="closedlost"`) · `list_deal_pipelines` ·
`pipeline` in deal properties `clients/hubspot.py:251` · characterization locks
`tests/test_hubspot_sync.py:898-960`, `tests/test_salesforce_sync.py:876+` — must stay green.

**Three deltas that change the plan:**

- **D1 — the spec's `harvest_org_suggestions` signature is not satisfiable as written.** Both new
  accessors are **per-company/per-account**, but the spec's kwargs carry no ids. `crm_enrichment`
  has `hubspot_company_id` (`models/__init__.py:1027`) but **no `salesforce_account_id`** — only
  `salesforce_contact_id` (`:1032`) — so ids can't be recovered from the DB for SF. They *are* in
  hand inside `_sync_org` (`hubspot_sync.py:203`, `salesforce_sync.py:255`). **Resolution:** add
  required kwarg `company_ids: dict[str,str]` (email_lower → company/account id), built in the loop
  the spec already nominates. It also supplies the adapters' `contact_email`, known only to the loop.
- **D2 — `last_harvest_status` does not exist.** Spec §3 records failures to it; no such column, and
  columns are another aspect's scope. **Resolution:** log-only + `{"status": "error"}` in the stats
  dict. No schema change here.
- **D3 — config keys.** `org-config-api-and-ui/spec.md:22-23` fixes HubSpot
  `{"renewal_pipeline_ids":[...]}` / SF `{"renewal_opportunity_types":[...]}`.
  `data-model/spec.md:96`'s `renewal_pipelines` is a stale literal. **Follow org-config.**

**Reuse verbatim:** `services/status_sync_core.py:1-8` (purity docstring/shape) ·
`tasks/hubspot_sync.py:239-244` (ISO close-date parse, `Z`→`+00:00`, except `(ValueError,
AttributeError)`→None) · `tasks/salesforce_sync.py:294-300` (same, `str()`-wrapped) · `:251-252`
(per-account memo dicts) · `hubspot_sync.py:179-187` / `salesforce_sync.py:218-226` (`known_emails`)
· `tests/test_hubspot_sync.py:29-66` (file-local SQLite `_ENGINE` + `db` fixture, **not**
conftest's), `:94-100` `_make_mock_client`, `:102-121` `_run_sync_org`.

**Measured baseline (not the brief's 159):** the four CRM files → **150 passed** (34/33/37/46).
Hold to 150 + new, zero deletions.


## 1. Project setup

No new deps, migration, config, or beat. 3 new `src/services/` files, 3 new test files, additive
edits to two task files. Runner:
```bash
cd services/worker-service && SENTRY_DSN="" \
  /Users/aliz/dev/at/rereflect/services/backend-api/venv/bin/python -m pytest <files> -q 2>&1 | grep -E "passed|failed"
```


## 2. Implementation phases

### Phase 1 — Pure decision core
**Goal.** `decide_suggestion(...)` — default-deny, never raises, deterministic deny order.
**Prerequisites.** None (pure). **Files.** `src/services/churn_harvest_core.py`,
`tests/test_churn_harvest_core.py` (both new).
**Steps.**
1. RED: assert `SUGGESTION_DENY_REASONS == ("not_closed","won","discriminator_not_configured",
   "no_discriminator","unknown_customer")` — the tuple *is* the asserted order; a reorder fails.
   One test per deny path with its exact reason, incl. AC-2's R5 trio (`discriminator=None` →
   `no_discriminator`; `renewal_set` None/empty → `discriminator_not_configured`; unknown email →
   `unknown_customer`), plus an **ordering** test: a record tripping all four → `not_closed`.
2. RED: `(True, None)` only for closed + not-won + `discriminator in renewal_set` + email in
   `known_emails`. Exact match: `"renewal"`, `"Renewal "`, `"Renew"` all deny against `{"Renewal"}`.
3. GREEN: keyword-only, stdlib-only, docstring mirroring `status_sync_core.py:1-8` (no
   Celery/SQLAlchemy/FastAPI/httpx/client imports). Blank discriminator → `no_discriminator`.
**Validation.** `pytest tests/test_churn_harvest_core.py -q` green; no I/O, fixtures, or `patch`.
**Commit.** `feat(harvester-core): pure default-deny decide_suggestion with fixed deny order`

### Phase 2 — Two thin adapters
**Goal.** Normalize a HubSpot deal / SF Opportunity to one candidate shape, or `None`.
**Prerequisites.** Phase 1. **Files.** `src/services/churn_harvest_adapters.py`,
`tests/test_churn_harvest_adapters.py` (new).
**Steps.**
1. RED: both map to identical **keys** `{customer_email, external_opportunity_id,
   suggested_churned_at, evidence, is_closed, is_won, discriminator}`; assert `set(hs) == set(sf)`.
2. RED: `suggested_churned_at` == CRM close date (`closedate`/`CloseDate`), **never** "now", and
   **stable across two calls** — what makes re-harvest idempotent (understanding.md F5). Missing id
   → `None`; missing or unparseable close date → `None`.
3. RED: `is_won` — HS `dealstage=="closedwon"`, SF the `IsWon` **boolean** (not string truthiness).
   `is_closed` — HS `dealstage in {"closedwon","closedlost"}`, SF `IsClosed`. `discriminator` —
   HS `properties.pipeline`, SF `Type`. `evidence == {name, stage, type, amount, close_date,
   provider}` (HS `type`=pipeline, SF `type`=`Type`); amount parse failure → `None`, never a raise.
4. GREEN: pure, stdlib-only; reuse the `hubspot_sync.py:239-244` parse verbatim. `customer_email`
   = passed `contact_email`, lowercased.
**Validation.** `pytest tests/test_churn_harvest_adapters.py -q` green.
**Commit.** `feat(harvester-core): hubspot/salesforce candidate adapters on CRM close date`

### Phase 3 — Harvester service (idempotency, suppression, cap)
**Goal.** Write pending rows once, never twice, never an event.
**Prerequisites.** Phases 1–2. **Files.** `src/services/churn_suggestion_harvester.py`,
`tests/test_churn_suggestion_harvester.py`. **Signature** (D1 applied):
```python
PER_RUN_SUGGESTION_CAP = 200
def harvest_org_suggestions(org_id, db, client, *, provider, renewal_set, known_emails,
                            company_ids, cap=PER_RUN_SUGGESTION_CAP) -> dict:
    # {scanned, suggested, skipped_existing, denied, dropped_by_cap, status}
```
**Steps.**
1. RED: hand-written **Fake client class** (not MagicMock — AC 9 forbids `patch`) + file-local
   SQLite `db` fixture. N candidates → N `pending` rows.
2. RED (**AC 4**): second run inserts zero — `scanned > 0`, `suggested == 0`,
   `skipped_existing == N`, row `id`s unchanged (no update-on-reharvest).
3. RED (AC 5): existing `rejected` not re-suggested; `confirmed` not duplicated; customer with an
   active `CustomerChurnEvent` skipped; **recovered** (`recovered_at` set) **not** skipped.
4. RED (AC 6): 250 candidates, `cap=200` → 200 rows, `dropped_by_cap == 50`, **WARNING** naming
   org_id/provider/cap/dropped (`caplog`). RED (**AC 8**): `CustomerChurnEvent` count before == after
   on the happy path. RED: raising Fake client → `{"status":"error"}`, logged, not propagated.
5. GREEN: iterate `sorted(company_ids.items())` for determinism; memoize the per-company fetch
   (`salesforce_sync.py:251-252`). Per candidate: adapt → `decide_suggestion` → deny increments
   `denied` (counters, not rows). On suggest: pre-check `(org, provider, external_opportunity_id)`
   → skip if a row exists in **any** status; else `db.add` + `db.flush()` in `try/except
   IntegrityError` inside `db.begin_nested()` (SAVEPOINT) → `skipped_existing += 1`. No `ON
   CONFLICT` (SQLite-safe, per `_upsert_enrichment`); the DB UNIQUE is the real guarantee. Caller
   owns the transaction — **no `db.commit()`**. Active-event check reimplemented locally (`org +
   email + recovered_at IS NULL`, per `routes/churn_events.py:74-88`) — cannot import backend.
   **Never** write `CustomerChurnEvent` — review-queue's job on human confirm.
**Validation.** `pytest tests/test_churn_suggestion_harvester.py -q` green; zero Celery/httpx/`patch`.
**Commit.** `feat(harvester-core): idempotent, capped churn suggestion harvester`

### Phase 4 — Wire both `_sync_org` seams (HubSpot first, then Salesforce — two commits)
**Goal.** Additive, exception-isolated call; enrichment untouched. **Prerequisites.** Phase 3.
**Files.** `src/tasks/{hubspot,salesforce}_sync.py` (edit); `tests/test_{hubspot,salesforce}_sync.py` (append).
**Steps.**
1. RED (**AC 7**): `churn_labels_enabled=False` (default) → zero rows **and** `_sync_org`'s dict
   equals the wave-1 baseline. A **raising** accessor likewise leaves enrichment + result intact.
2. RED: enabled + non-empty renewal config → pending rows for lost-renewal records only. Enabled
   but empty/absent config → skipped, zero rows (default-deny survives the toggle).
3. GREEN: collect `company_ids[email_lower]` inside the existing loop — HS `company_id`
   (`hubspot_sync.py:203`), SF `account_id` (`salesforce_sync.py:255`, in `matched_by_email`); no
   restructuring. **After** the loop call `_maybe_harvest(...)`: read the integration's
   `churn_labels_enabled`/`churn_label_config` by `org_id`; build `renewal_set = frozenset(
   config.get(<"renewal_pipeline_ids"|"renewal_opportunity_types">) or [])` (D3); return early
   unless enabled **and** `renewal_set` **and** `company_ids`; wrap in `try/except Exception` →
   `logger.exception` (R4). SF reuses the run's `opportunities_cache` pattern for the lost fetch.
4. **Decision — `_sync_org`'s return dict is unchanged, unconditionally.** Harvest stats go to a log
   line, not the dict; AC 7 then holds in *both* toggle states and R4 blast radius stays zero.
**Validation.** HS → 34 + new; SF → 33 + new; both characterization locks green.
**Commits.** `feat(harvester-core): wire suggestion harvest into {hubspot,salesforce} _sync_org (default-deny)`

### Phase 5 — Full green + purity guard
**Goal.** AC 9. **Files.** `tests/test_churn_harvest_core.py` (append).
**Steps.** Source-level purity assertion: read both pure modules, assert no `celery`, `sqlalchemy`,
`fastapi`, `httpx`, or client import (cheap drift guard, mirrors `status_sync_core.py`'s contract).
Then run the 4-file CRM set + the 3 new files.
**Validation.** 150 + new, zero failures; grep confirms no `patch` in the 3 new test files.
**Commit.** `test(harvester-core): purity guard + full worker CRM suite green`


## 3. File-by-file build order

1. `src/services/churn_harvest_core.py` + `tests/test_churn_harvest_core.py` — new, pure (P1, P5)
2. `src/services/churn_harvest_adapters.py` + `tests/test_churn_harvest_adapters.py` — new, pure (P2)
3. `src/services/churn_suggestion_harvester.py` + `tests/test_churn_suggestion_harvester.py` — new, db (P3)
4. `src/tasks/{hubspot,salesforce}_sync.py` (additive edits) + their test files (append) — P4

**No backend mirror** (spec §6, Q6: extract-on-second-use). No migration. No new beat.


## 4. Testing strategy

- **Phases 1–2:** pure asserts — no DB, fixtures, mocks, or `patch`.
- **Phase 3:** file-local SQLite engine + `db` fixture copied from `test_hubspot_sync.py:29-66`
  (**not** conftest's) + a hand-written Fake client class. `caplog` for the cap WARNING.
- **Phase 4:** reuse each file's `_make_mock_client` / `_run_sync_org`; extend the former with the
  new accessor rather than forking a helper. (`_run_sync_org` uses `patch.object` for
  `get_db_session`; AC 9's "zero `patch`" binds the **3 new** test files, which have none.)
- **Every phase:** both `TestEnrichmentCharacterizationLock` classes stay green. **Never** assert
  wall-clock time — `suggested_churned_at` is the CRM close date; compare to a fixed literal.


## 5. Environment & secrets

Nothing new — no env vars, scopes, or tokens; both accessors reuse the already-decrypted client the
tasks hold. `SENTRY_DSN=""` required on every worker test run. R3 stands: the token is never logged;
harvest logs carry `org_id`/`provider`/counts only.


## 6. Edge cases & error handling

| Case | Behaviour |
|---|---|
| `churn_labels_enabled=False` (default) | Skip; zero rows; result dict identical to baseline (AC 7). |
| Enabled, config empty/absent/`None` | Skip — `discriminator_not_configured`. Default-deny survives the toggle. |
| `pipeline`/`Type` None/`""`/whitespace | `no_discriminator` (R5). Silence, not a false label. |
| Near-match (`"renewal"` vs `"Renewal"`); email not in `known_emails` | Deny — exact match only, no regex/normalization (M2); `unknown_customer`. |
| Missing/unparseable close date or id | Adapter → `None`; dropped, counted in `denied`. |
| Row exists in **any** status (incl. `rejected`) | Skip, never update. Re-harvest is a no-op, not a refresh. |
| Race → `IntegrityError` | Caught, SAVEPOINT rollback, `skipped_existing += 1`. DB UNIQUE is the guarantee. |
| Active `CustomerChurnEvent` | Skip. **Recovered** (`recovered_at` set) → not skipped. |
| Two contacts share a company/account | Fetch memoized once; shared deal dedups on the UNIQUE pre-check. |
| >`cap` candidates | Insert `cap`, count the rest, **WARNING** with `dropped_by_cap`. No silent caps. |
| Any harvest exception | Caught at the `_sync_org` seam, `logger.exception`, `status="error"`; enrichment + `last_sync_status='success'` intact (R4). |
| Writing `customer_churn_events` | **Forbidden.** count-before == count-after (AC 8). |


## 7. Agent execution notes

- **Raise D1/D2/D3 (§0) before writing code** — they change the spec's stated signature, drop a
  field it names, and pick between two sibling specs' config keys. If a reviewer rejects D1's
  `company_ids` kwarg, the only alternative is passing raw matched contacts — worse, it couples the
  harvester to two provider payload shapes.
- **Baseline is 150, not 159** (34/33/37/46). Verify before Phase 1 so a pre-existing failure is
  never mistaken for a regression. Zero deletions from that set.
- **Do not** refactor the CRM tasks to jira's `_body(integration_id, db, client=None)` shape (R4
  blast radius); `_sync_org(org_id, db, client)` is already the injectable seam. **Do not** touch
  `_pick_renewal_deal`, `_pick_renewal_opportunity`, `get_open_deals_for_company`, or
  `get_open_opportunities` — harvest is additive and runs after the enrichment loop.
- The harvester **never reads config** — it is handed a `frozenset`. Config reading lives in
  `_maybe_harvest` in each task file (provider-specific model).
- If harvest stalls the 03:15/03:45 beat, **split it to its own beat — never raise the cap silently.**
