# Implementation Plan — docs-tracking-changelog (aspect 5 of 5, close-out, runs last)

**Date**: 2026-08-15
**Feature**: `intercom-pull-replies-and-ratings` (prd.md R8 — docs & tracking are part of "done")
**Aspect**: `docs-tracking-changelog`
**Source**: `docs/planning/intercom-pull-replies-and-ratings/docs-tracking-changelog/spec.md` + `prd.md`
**Branch**: `feat/intercom-pull-replies-and-ratings` (worktree `.claude/worktrees/feat-intercom-pull-replies`)
**Sequencing**: **LAST** — execute only after the four code aspects (`client-conversation-parts`,
`adapter-reply-rating-extraction`, `pull-enrichment`, `reanalysis-seam`) have merged into the
branch. This plan's placeholders (merge sha, PR number, merge date) must be filled from the
merged state before any step runs, per the house rule ("correct the marker in the same
commit"; fill-facts follow-up commit precedent `5a9d547d`, `4b6284a9`).

## 0. Facts gathered (verified 2026-08-15, before writing)

| Fact | Value | Source |
|---|---|---|
| SELF_HOSTING honest-limits block | `docs/SELF_HOSTING.md:1927-1937` — `> ### Honest limits` blockquote under `### Connecting Intercom with OAuth (legacy)`; the false pair is :1931-1932 ("pull ingests the **first message**… Replies and ratings arrive through the webhook only") | read |
| Pull table row | `docs/SELF_HOSTING.md:1732` — `| Pull sync | Yes — every 15 minutes | Yes |` (OAuth cell claims pull; `sync_all_intercom` iterates `IntercomIntegration` rows only, intercom_sync.py:296-304) | read |
| OAuth "only ingestion path" claim | `docs/SELF_HOSTING.md:1890-1893` — "**This step is not optional — it is the only ingestion path.**" — **TRUE for OAuth orgs** (they have no pull), so it stays; the table fix makes the distinction explicit | read |
| Webhook topic lists | `docs/SELF_HOSTING.md:1816-1817` (access-token §3) and :1902-1907 (OAuth §5) — both list `conversation.user.created` / `.user.replied` / `.rating.added` | read |
| SELF_HOSTING Intercom intro | `docs/SELF_HOSTING.md:1724-1725` — "new conversations, replies, and ratings become feedback items, analyzed like any other source" | read |
| CHANGELOG insertion point | `## Unreleased` runs :8-510 (`## v1.0.0 — 2026-07-26` at :511); current top entry is the write-back entry at :10. **The token-paste "Added" entry (:314-345) and the self-hosting-guide entry (:387-405) are both in Unreleased — live, not historical** | read |
| CHANGELOG honest-limits claim to correct | `CHANGELOG.md:342-343` — "*Honest limits:* … the pull ingests the first message of a conversation, with replies and ratings arriving via webhook only" | read |
| CHANGELOG false claim, spec-adjacent | `CHANGELOG.md:396` — "*Known limitations, stated plainly in the guide:* Intercom is **webhook-only** — unlike Zendesk there is no polling fallback, so if you don't subscribe the webhook, nothing arrives at all." — stale since 2026-08-01 (pull shipped); **never corrected by the token-paste entry** (that entry's "Previously … no pull at all" at :327-330 is past-tense but does not correct :396) | read |
| Correction-note precedents | `**Correction (2026-08-14):**` :359-363 (appended to Unreleased entry, superseding claim); `**Correction (2026-08-15):**` :382-385 (two-way-sync claim, superseded by the write-back) — house form: append, don't rewrite | read |
| Pull-sync spec honest limits | `docs/planning/intercom-selfhost-ingestion/pull-sync/spec.md:63-68` — :65-66 "Only the first message of a conversation is ingested (replies are a webhook-only path today)" | read |
| Ingestion PRD out-of-scope bullet | `docs/planning/intercom-selfhost-ingestion/prd.md:309-310` — "Per-conversation-part ingestion, Intercom Articles/Tickets objects, status-sync back to Intercom, …"; :311 "Any claim of improved analysis or churn accuracy."; SHIPPED-pointer precedent `~~**Write-back to Intercom**~~ — **SHIPPED 2026-08-15**…` at :298-301 | read |
| AI-TRACKING Intercom row | `AI-TRACKING.md:66` — pull description: "**Conversation pull every 15 min** (`POST /conversations/search`, `updated_at >=` cursor …, 20-page/run cap, cursor never epoch so no historical backfill) routed through the **shared** `_find_matching_sources` / `_process_event_for_source` core, so pull and webhook share one dedup path." — needs the conversation-parts enrichment sentence | read |
| DEV-TRACKING deferred-v2 block | `### Deferred v2 — Intercom (opened 2026-08-01; 1 SHIPPED, 3 NOT STARTED)` at :504; the entry to flip `intercom-pull-replies-and-ratings` at :509-511; writeback SHIPPED bullet at :512-516 (the strikethrough-style model); remaining bullets :517-521 | read |
| DEV-TRACKING shipped-summary model | P2 `intercom-writeback-orphaned` FIXED block :591-616 (`> Shipped:` blockquote: what shipped → merge sha/PR/date → suite counts → follow-up lines) — **not** used for this feature's deferred bullet (deferred bullets carry a compact summary + follow-up, no suite counts, cf. :512-516) | read |
| Historical claim, leave alone | `DEV-TRACKING.md:315` "Intercom ingestion is **webhook-only in practice**" — inside P1 batch-2 entry superseded by its `> **CLOSED 2026-08-01**` blockquote (:288-302); history, not a live claim (mirror of the writeback plan's :1008/:1230 classification) | read |
| Landing page (flag-only) | `services/landing-web/lib/integrations.ts:136-171` — refreshed by `landing-intercom-entry-refresh` (#15): token-paste + pull + write-back; step 4 (:170) still says "Subscribe … `conversation.user.replied` and `conversation.rating.added`" — no first-message/webhook-only claim about replies or ratings anywhere on the landing surface | grep |
| Planning dir state | `docs/planning/intercom-pull-replies-and-ratings/` is **untracked** (`git status` → `??`); the four code aspects each hold only `spec.md` so far — their plans merge from their own commits; this aspect's doc edits target tracked files | git status |
| Live-claim grep surface (pre-plan) | "first message": SELF_HOSTING:1931, CHANGELOG:343, DEV-TRACKING:509 (deferred entry), AI-TRACKING: none; "webhook only": SELF_HOSTING:1932, CHANGELOG:343/:396, DEV-TRACKING:315 (historical); no matches in README or landing | grep |

## 1. Project setup checklist

Docs-only aspect. No scaffolding, no dependencies, no env, no tests. Files touched (exactly five):

1. `docs/SELF_HOSTING.md` — honest-limits rewrite + OAuth pull-cell truth fix (+ optional intro reword, Phase 1)
2. `CHANGELOG.md` — new Added entry + correction notes on :342-343 and (flagged) :396 (Phase 2)
3. `docs/planning/intercom-selfhost-ingestion/pull-sync/spec.md` — honest-limits amend (Phase 3)
4. `docs/planning/intercom-selfhost-ingestion/prd.md` — out-of-scope bullet flip with pointer (Phase 3)
5. `AI-TRACKING.md` — Intercom row amend (Phase 4)
6. `DEV-TRACKING.md` — deferred-v2 entry → SHIPPED + header count (Phase 5)

**Explicitly NOT edited:** `services/landing-web/lib/integrations.ts` (flag-only, verified clean),
`DEV-TRACKING.md:315` and any archived planning-doc body (historical record), the webhook topic
lists at `SELF_HOSTING.md:1816-1817`/`:1902-1907` (see ambiguity 2), `README.md` (zero Intercom
claims), `docs/planning/intercom-pull-replies-and-ratings/prd.md` (kept as the record).

## 2. Implementation phases

### Phase 1 — SELF_HOSTING.md: honest-limits rewrite + OAuth pull-cell truth fix

- **Goal:** the two false live claims — "replies and ratings arrive through the webhook only"
  and "OAuth gets pull sync" — flip to the new reality; honest limits match the PRD's list.
- **Prerequisites:** all code aspects merged (the feature exists to document).
- **File:** `docs/SELF_HOSTING.md`
- **Steps:**
  1. **Rewrite the honest-limits block (:1929-1937).** Replace the :1931-1932 pair with the
     new reality; keep the block's established `> -` rhythm. Draft (house voice, honest-limits
     from prd.md §Honest limits):

     ```
     > - The pull runs every 15 minutes, so without a webhook a conversation can take
     >   that long to appear.
     > - The pull ingests the **full conversation** for anything it re-sees: the first
     >   message plus every new reply, merged into the item's text, and the satisfaction
     >   rating stored on the item. The webhook is optional — for near-instant delivery
     >   of new conversations — and no longer the only way replies or ratings reach an
     >   item.
     > - Enrichment applies to conversations the pull **re-sees after this ships** —
     >   older conversations stay first-message-only until they get new activity. There
     >   is no backfill.
     > - A very large backlog drains over several runs rather than one — each run
     >   fetches up to 20 pages and the cursor resumes where it stopped.
     > - The webhook's `replied` / `rating.added` events are still dedup-inert today
     >   (flagged follow-up, not fixed) — the pull is what delivers replies and ratings.
     > - No claim is made about analysis quality beyond: the full thread is now scored
     >   instead of the first message. Sentiment, categorization and churn behave
     >   exactly as they do for every other source.
     ```

     (Fills spec.md:13-17: "no backfill for older conversations, enrichment applies to
     re-seen conversations, no analysis-quality claim beyond full-thread scoring".)
  2. **Fix the pull table row (:1732).** Change the OAuth cell from `Yes` to an honest
     `No` with the reason inline, per spec.md:18-21. Draft:
     `| Pull sync | Yes — every 15 minutes | No — access-token only |`
  3. **Add one clarifying sentence** directly below the table (after :1734, before the
     :1736 paragraph), stating the mechanism so the cell is not a bare contradiction of the
     legacy section's "only ingestion path" (:1890-1893):

     > Pull sync runs for access-token connections only: the sync job iterates
     > `IntercomIntegration` rows, which the token-paste path provisions. OAuth
     > connections have **no pull** — for those, the webhook is the only ingestion
     > path (step 5 below).

     This keeps the legacy section's "only ingestion path" claim true and scoped.
  4. **(Optional, flagged — see ambiguity 3.)** Tighten the section intro
     (:1724-1725) so "replies, and ratings become feedback items" does not overstate:
     `new conversations — including replies and the satisfaction rating — become feedback
     items, analyzed like any other source.` Lean: do it (one line, removes the same
     overstatement class this aspect exists to kill). If the executor or reviewer objects,
     drop it — the honest-limits block carries the precision either way.
  5. Do not touch the webhook topic lists (:1816-1817, :1902-1907) — see ambiguity 2;
     the honest-limits block now carries the inertness note.
- **Validation:** `git grep -n "arrive through the webhook only" docs/SELF_HOSTING.md` → 0;
  `git grep -n "first message" docs/SELF_HOSTING.md` → 0; `git grep -n "Pull sync" docs/SELF_HOSTING.md`
  → the corrected cell (`No — access-token only`); re-read the section — the table, the new
  sentence and the honest-limits block read coherently together.
- **Commit:** `docs(self-hosting): Intercom pull now ingests replies + ratings — honest limits + OAuth pull truth fix`

### Phase 2 — CHANGELOG: new Added entry + corrections of :342-343 and (flagged) :396

- **Goal:** the release notes record the feature and stop the two live false claims from
  standing as current truth.
- **Prerequisites:** Phase 1 (separate commit; the writeback precedent combined
  SELF_HOSTING+CHANGELOG in one commit — here they are split because the CHANGELOG work
  spans two corrections; combining is acceptable, see §7).
- **File:** `CHANGELOG.md`
- **Steps:**
  1. Insert at the **top of `## Unreleased`** (above :10), newest-first per house
     convention:
     `### Added — Intercom pull now ingests replies and ratings` with this body outline
     (house bullets; bold-led; *honest-limits* italic closer):

     - **Replies are merged into the item.** The 15-minute pull used to ingest the first
       message of each conversation only. It now enriches conversations it re-sees with
       their conversation parts: each new reply is appended to the item's text
       (HTML-stripped, with an author-attribution line) — one feedback item per
       conversation, as before, and merging is idempotent (re-running on the same
       conversation adds nothing).
     - **The satisfaction rating is captured** into `source_metadata` (`rating`,
       `remark`, `rated_at` when available) and surfaced on the feedback detail — not
       appended to the text, so "Rating: 5/5" never skews sentiment analysis.
     - **Changed items are re-analyzed** through the normal pipeline (sentiment,
       categories, churn, health). Unchanged items are not touched.
     - **The webhook stays optional** — for near-instant delivery of new conversations;
       replies and ratings no longer depend on it.
     - `*Honest limits:* enrichment applies to conversations the pull re-sees after this
       ships — older conversations stay first-message-only until they get new activity
       (no backfill); "the full thread is now scored" is the extent of the quality claim;
       the webhook's `replied`/`rating.added` events remain dedup-inert (a flagged
       follow-up, not fixed here).`

     Leave `<merge-sha>` / `<PR #>` markers out of the changelog entry (the changelog does
     not carry merge facts; DEV-TRACKING does).
  2. **Correction note on the token-paste entry (:342-343)**, mirroring the
     `**Correction (2026-08-14):**` precedent (:359-363) — append one paragraph after the
     entry's *Honest limits* line (:345):

     ```
     **Correction (2026-08-16):** the honest-limits line above said replies and ratings
     arrive "via webhook only" — and in practice that path was inert, so neither reached
     analysis in any install. The pull now ingests both directly (conversation-parts
     enrichment; see the Added entry above). The webhook remains optional, for latency.
     ```

     Append-only — the entry is the 2026-08-01 record; the correction supersedes it
     without rewriting it (the house way, cf. :382-385). See ambiguity 4.
  3. **Correction note on the self-hosting-guide entry (:396)** — flagged spec-adjacent
     (see ambiguity 1), lean: include. Append after :405 (end of that entry):

     ```
     **Correction (2026-08-16):** "webhook-only — no polling fallback" was true when the
     guide was written and stale since the token-paste pull shipped on 2026-08-01: the
     pull ingests conversations every 15 minutes with no webhook required, and now
     includes replies and ratings. See the pull entries above.
     ```
- **Validation:** the new entry sits above :10; `git grep -n "arriving via webhook only" CHANGELOG.md`
  → only :343 (the superseded claim, now followed by a Correction note); `git grep -n "webhook-only" CHANGELOG.md`
  → :35 (past-tense, "both stale since…") and :396 (superseded by Correction note); the new
  entry's *Honest limits* line names no-backfill, no-quality-claim and the inert webhook
  path.
- **Commit:** `docs(changelog): Intercom pull replies/ratings entry + honest-limits corrections`

### Phase 3 — Planning docs: pull-sync spec strike/amend + ingestion PRD out-of-scope flip

- **Goal:** the archived planning record for the old behavior is amended, not left standing;
  the out-of-scope bullet points to the shipped card.
- **Files:** `docs/planning/intercom-selfhost-ingestion/pull-sync/spec.md`,
  `docs/planning/intercom-selfhost-ingestion/prd.md`
- **Steps:**
  1. `pull-sync/spec.md:65-66` — strike the superseded sentence in the file's own
     resolution style (`~~…~~` + SHIPPED pointer, cf. the ingestion PRD's :298-301):

     ```
     Up to 15 minutes of latency. ~~Only the first message of a conversation is ingested
     (replies are a webhook-only path today).~~ — **SUPERSEDED 2026-08-16** by
     `intercom-pull-replies-and-ratings`: the pull now enriches replies and the
     satisfaction rating via conversation-parts for conversations it re-sees (no
     backfill). The 20-page cap means a very large backlog drains over
     several runs rather than one. No claim is made about analysis quality — this changes
     whether feedback arrives, nothing else.
     ```

     Keep the rest of the honest-limits block (:67-68) and the acceptance criteria
     (P5/P6 — the boundary re-fetch dedup invariant is untouched) as-is.
  2. `intercom-selfhost-ingestion/prd.md:309-310` — split the bullet, flipping only the
     per-conversation-part item with a pointer, keeping the rest out of scope:

     ```
     - ~~Per-conversation-part ingestion~~ — **SHIPPED 2026-08-16**
       (`intercom-pull-replies-and-ratings`): the pull enriches replies + the rating via
       conversation-parts, one item per conversation. See
       `docs/planning/intercom-pull-replies-and-ratings/`.
     - Intercom Articles/Tickets objects, status-sync back to Intercom, multiple Intercom
       workspaces per org, IdP-style app marketplace listing.
     ```

     `:311` "Any claim of improved analysis or churn accuracy." stays untouched.
- **Validation:** `git grep -n "SUPERSEDED 2026-08-16" docs/planning/intercom-selfhost-ingestion/pull-sync/spec.md` → 1;
  `git grep -n "SHIPPED 2026-08-16" docs/planning/intercom-selfhost-ingestion/prd.md` → 1;
  `git grep -n "first message of a conversation is ingested" docs/planning/` → only the
  struck (~~…~~) occurrence in pull-sync/spec.md plus this feature's own planning docs.
- **Commit:** `docs(planning): intercom pull replies/ratings — pull-sync spec amend + ingestion PRD out-of-scope flip`
  (the untracked `docs/planning/intercom-pull-replies-and-ratings/` dir — card/PRD/specs/this
  plan — lands in the feature's separate docs(planning) commit, mirroring the writeback
  feature's `docs(planning): card + PRD + specs + plans …` precedent; do not mix it here).

### Phase 4 — AI-TRACKING.md:66 Intercom row amend

- **Goal:** the integration row's pull description reflects the enrichment, honest tone kept.
- **File:** `AI-TRACKING.md`
- **Steps:**
  1. Insert one sentence into the row, after "…so pull and webhook share one dedup path."
     and before "**Per-workspace webhook signature verification**":

     ```
     **Replies + rating enrichment shipped 2026-08-16** (`intercom-pull-replies-and-ratings`):
     the pull fetches conversation parts for conversations it re-sees and merges new reply
     bodies into the item text (one item per conversation, idempotent by part id, admin
     replies never attributed as customer), with the satisfaction `rating`/`remark` in
     `source_metadata`; items that gained content are re-analyzed, unchanged items are
     not. No backfill — older conversations stay first-message-only until re-seen. The
     webhook `replied`/`rating.added` events remain dedup-inert (flagged follow-up, not
     fixed).
     ```

     Match the row's dense inline style; keep the existing pull sentence (cursor, cap,
     no-historical-backfill) untouched — the new sentence complements it.
- **Validation:** the row contains "conversation parts" / "replies" / "rating" and the
  honest "No backfill" + "flagged follow-up" phrases; `git grep -n "intercom-pull-replies-and-ratings" AI-TRACKING.md` → 1.
- **Commit:** with Phase 5 (`docs(tracking): …`, see §7).

### Phase 5 — DEV-TRACKING: deferred-v2 `intercom-pull-replies-and-ratings` → **SHIPPED**

- **Goal:** the deferred list reflects reality — two shipped, two open; the entry carries the
  shipped summary (merge facts as placeholders, filled post-merge) and the follow-up defect
  note for the latent webhook reply/rating path (flagged, not fixed — spec.md:31-33).
- **Prerequisites:** code aspects merged; merge sha / PR number known (or placeholders).
- **File:** `DEV-TRACKING.md`
- **Steps:**
  1. Header :504 count fix: `(opened 2026-08-01; 1 SHIPPED, 3 NOT STARTED)` →
     `(opened 2026-08-01; 2 SHIPPED, 2 NOT STARTED)`.
  2. Rewrite the :509-511 bullet in the house SHIPPED style (strikethrough + summary —
     the writeback bullet :512-516 is the model):

     ```
     - ~~**`intercom-pull-replies-and-ratings`** — the 15-minute pull ingests the **first
       message** of a conversation only. Replies and ratings arrive via the webhook path,
       so a pull-only install (no webhook wired) never sees them. Needs conversation-parts
       fetching.~~ — **SHIPPED 2026-08-16** via `feat/intercom-pull-replies-and-ratings`
       (merged <merge-sha>, PR <# pending>): the pull now enriches replies + the
       satisfaction rating via conversation-parts — new replies merged into the item text
       (one item per conversation, idempotent by part id), rating/remark in
       `source_metadata`, changed items re-analyzed, unchanged untouched. No backfill:
       older conversations stay first-message-only until the pull re-sees them.
       **Follow-up (defect, not fixed):** the webhook reply/rating path is still inert —
       the dedup key (`external_message_id` = conversation id for all three event types)
       and the `new_conversations`-only seed trigger make `conversation.user.replied` /
       `conversation.rating.added` events dedup into nothing; the pull covers the content
       today. See `docs/planning/intercom-pull-replies-and-ratings/`.
     ```

  3. The other two bullets (:517-521) stay NOT STARTED, untouched.
  4. **Post-merge fill (follow-up commit, do not amend):** once the PR merges, replace
     `<merge-sha>` / `<PR # pending>` with the real facts in a `docs(tracking): fill merge
     facts for intercom-pull-replies-and-ratings (PR #<N>, <sha>)` commit — the
     `5a9d547d` / `4b6284a9` precedent.
- **Validation:** `git grep -n "SHIPPED 2026-08-16" DEV-TRACKING.md` → ≥ 1; header count
  `2 SHIPPED, 2 NOT STARTED`; the entry names the follow-up defect; no other deferred bullet
  changed; no placeholders survive post-merge.
- **Commit:** `docs(tracking): intercom-pull-replies-and-ratings SHIPPED + AI-TRACKING row amend`

### Phase 6 — Grep sweep (final gate) + landing flag-only check

- **Goal:** prove the spec's acceptance criteria 1-4 — no surviving live claims, honest
  OAuth cell, defect noted, one-item invariant never contradicted.
- **Steps** (run from the repo root; re-locate targets by text if line numbers shifted):
  ```bash
  git grep -n "first message" docs/SELF_HOSTING.md CHANGELOG.md AI-TRACKING.md   # → 0
  git grep -n "first message" DEV-TRACKING.md    # → only :509-510 (struck ~~…~~ text) + historical blocks
  git grep -n "arrive through the webhook only\|arriving via webhook only" docs/SELF_HOSTING.md CHANGELOG.md  # → CHANGELOG:343 only, and it is superseded by the Correction note appended in Phase 2
  git grep -n "webhook-only" CHANGELOG.md        # → :35 (past-tense), :396 (superseded by Correction note)
  git grep -n "Pull sync" docs/SELF_HOSTING.md   # → "| Pull sync | Yes — every 15 minutes | No — access-token only |"
  git grep -n "2 SHIPPED, 2 NOT STARTED" DEV-TRACKING.md   # → 1
  git grep -n "SUPERSHED\|SUPERSEDED" docs/planning/intercom-selfhost-ingestion/pull-sync/spec.md  # → 1
  git grep -n "SHIPPED 2026-08-16" docs/planning/intercom-selfhost-ingestion/prd.md DEV-TRACKING.md AI-TRACKING.md  # → ≥1 each
  git grep -n "one item per conversation\|one-item" docs/SELF_HOSTING.md CHANGELOG.md DEV-TRACKING.md  # → no edited sentence claims separate items (D3 invariant intact; struck text in DEV-TRACKING excepted)
  # Landing (flag-only — spec.md:39): no edit; verify nothing claims first-message-only / webhook-only replies:
  git grep -n "first message\|webhook only\|webhook-only" services/landing-web/lib/integrations.ts  # → 0
  ```
  Remaining "first message"/"webhook-only" hits **outside** those targets are expected:
  `docs/planning/**` (archived record, incl. this feature's own card/PRD problem
  statements), `DEV-TRACKING.md:315` (P1 batch-2 history, superseded by its CLOSED
  blockquote), `CHANGELOG.md:35` (past-tense description of a stale claim). Do not edit.
- **Human review pass:** re-read `docs/SELF_HOSTING.md:1722-1956` end-to-end — table,
  intro, webhook sections and honest-limits block must read coherently; then re-read the
  two corrected CHANGELOG entries and the SHIPPED bullet.

## 3. File-by-file build order

| # | File | Edit type | Purpose |
|---|------|-----------|---------|
| 1 | `docs/SELF_HOSTING.md` | Modify | honest-limits rewrite (:1931-1932), OAuth pull cell "No — access-token only" (:1732) + mechanism sentence, optional intro reword (:1724-1725) (Phase 1) |
| 2 | `CHANGELOG.md` | Modify | `### Added — Intercom pull now ingests replies and ratings` at top of Unreleased + Correction notes on :342-343 and :396 (Phase 2) |
| 3 | `docs/planning/intercom-selfhost-ingestion/pull-sync/spec.md` | Modify | :65-66 strike/amend with SUPERSEDED pointer (Phase 3) |
| 4 | `docs/planning/intercom-selfhost-ingestion/prd.md` | Modify | :309-310 out-of-scope bullet split + SHIPPED pointer (Phase 3) |
| 5 | `AI-TRACKING.md` | Modify | :66 Intercom row — enrichment sentence + honest limits (Phase 4) |
| 6 | `DEV-TRACKING.md` | Modify | :509-511 → SHIPPED (strikethrough + summary + defect note); header :504 count → 2 SHIPPED, 2 NOT STARTED (Phase 5) |

## 4. Testing strategy

- **No new tests.** Docs-only; no code, no migration, no comment in a code file — so no
  backend/worker suite rerun is required (unlike the writeback aspect, which touched
  `models/integration.py`). The suite counts in the other aspects' FIXED entries are
  unaffected by this aspect.
- **Verification is the Phase 6 grep suite** plus the human re-read of the Intercom section
  and the corrected entries.

## 5. Environment & secrets

- None. No env vars, no running stack, no database. All edits are markdown in tracked files.

## 6. Edge cases & error handling

- **Merge facts unavailable** (this aspect runs last but before the final PR merges): keep
  `<merge-sha>` / `<PR # pending>` placeholders in the SHIPPED bullet and land the
  fill-facts follow-up commit after merge (the `5a9d547d` / `4b6284a9` precedent). Do **not**
  invent facts, and do **not** let placeholders reach the FIXED-block form (that form is for
  P-entries with suite counts — not used here).
- **A code aspect has not merged** (sibling dirs still spec-only, or PRs open): halt, surface
  to the lead, do not mark SHIPPED — the docs pass describes what shipped.
- **CHANGELOG has newer entries above :10 by execution time**: insert at the actual top of
  Unreleased, not at a line number.
- **SELF_HOSTING section reflowed** (someone reflowed the Intercom section between now and
  execution): re-locate by text ("Pull sync", "The pull ingests the first message", "Honest
  limits"), not by line number.
- **Correction-note date**: use the actual merge date, not a hardcoded 2026-08-16.
- **Reviewer objects to the :396 correction or the :1724 reword** (both flagged
  spec-adjacent): drop them and note the drop in the PR description — they are included on
  the claim-discipline lean, not on a spec mandate.
- **Post-merge fill lands after another change to the same bullet**: re-locate by text.

## 7. Agent execution notes

- Runs last. Two human checkpoints: (1) before Phase 1 — confirm every code aspect is merged
  on the branch, fill the merge-facts table; (2) before the Phase 5 commit — a reviewer signs
  off the SHIPPED bullet's summary against the actual diff.
- Commit sequence (house style — do not amend; push and open the PR per house workflow):
  1. `docs(self-hosting): Intercom pull now ingests replies + ratings — honest limits + OAuth pull truth fix` (Phase 1)
  2. `docs(changelog): Intercom pull replies/ratings entry + honest-limits corrections` (Phase 2)
  3. `docs(planning): intercom pull replies/ratings — pull-sync spec amend + ingestion PRD out-of-scope flip` (Phase 3)
  4. `docs(tracking): intercom-pull-replies-and-ratings SHIPPED + AI-TRACKING row amend` (Phases 4-5)
  5. Post-merge: `docs(tracking): fill merge facts for intercom-pull-replies-and-ratings (PR #<N>, <sha>)`
  (Phases 1+2 may be combined into one `docs(self-hosting): … + CHANGELOG` commit if the
  executor prefers — the writeback precedent did exactly that.)
- The landing flag lives only in the Phase 6 verification output / PR description — no
  landing edit, no separate tracking entry (spec.md:39).
- The webhook-reply/rating defect is recorded **inside the SHIPPED bullet** as a follow-up
  note (spec.md:31-33) — do not create a separate DEV-TRACKING P-entry in this aspect.

## 8. Spec ambiguities flagged

1. **CHANGELOG.md:396 "Intercom is webhook-only — no polling fallback"** — a live false
   claim in Unreleased that the spec does not name (spec.md in-scope lists only :342-343).
   The spec's own acceptance-criterion grep ("no surviving 'webhook only' claims") would
   surface it. **Lean: correct it** with a Correction note in Phase 2, on the house
   precedent. Droppable if reviewers object.
2. **Webhook topic lists (:1816-1817, :1902-1907) still instruct subscribing to
   `conversation.user.replied` / `conversation.rating.added`, which are dedup-inert
   post-fix.** The spec names the lists as an input but not as an edit. **Decided: leave the
   lists** — they are the factual set of topics Rereflect accepts and the honest-limits
   rewrite carries the inertness note (the pull is now the delivery path). Alternative
   (append one clarifying line to §3) is available if reviewers want the guidance to stop
   implying the topics matter.
3. **SELF_HOSTING:1724-1725 intro ("replies, and ratings become feedback items")** — with
   the fix, replies are analyzed as part of the item but ratings ride in metadata, not as
   items; the sentence is a soft overstatement. **Lean: one-line reword** (Phase 1 step 4);
   droppable — the honest-limits block carries the precision.
4. **Correction mechanics on :342-343** — "flip" vs "append correction note". The entry is
   in Unreleased (line 8-510), so a direct edit is legal; **chosen: append-only** (the
   :359-363 / :382-385 precedents, and the entry is the 2026-08-01 record — a correction
   preserves it). The new Added entry carries the live claims.
5. **OAuth cell wording** — spec allows "token-paste-only pull" or honest "No" with reason;
   **chosen: `No — access-token only`** plus the mechanism sentence after the table, which
   keeps the legacy section's "only ingestion path" (:1890-1893) true and scoped.
6. **DEV-TRACKING:315 "webhook-only in practice"** — inside the P1 batch-2 entry superseded
   by its `> **CLOSED 2026-08-01**` blockquote (:288-302); classified as history and left
   untouched (mirrors the writeback plan's handling of :1008/:1230). The spec's acceptance
   criterion 1 targets SELF_HOSTING + CHANGELOG live entries, which this classification
   respects.
7. **SHIPPED date** — hardcoded as 2026-08-16 in this plan's drafts; use the actual merge
   date (house rule, cf. the writeback bullet which used the real date).
