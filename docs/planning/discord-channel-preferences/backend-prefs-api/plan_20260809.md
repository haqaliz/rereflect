# Plan — backend-prefs-api

**Slug:** `discord-channel-preferences` · **Date:** 2026-08-09
**Spec:** `../backend-prefs-api/spec.md` · **PRD refs:** R1, R2
**Branch:** `feat/discord-channel-preferences` · Worktree root: repo root of this worktree

## Project setup checklist

- Service: `services/backend-api` only.
- No new packages, no new env keys.
- DB: one new Alembic migration (chained onto `8114adde5d96`).
- Run everything from `services/backend-api` with the worktree's venv
  (`./start.sh` creates it if missing, or `python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt`).

## Implementation phases

### Phase 1: RED — failing tests for the new API behavior

- **Goal:** Tests that pin the absent-field sentinel and the round-trip land first.
- **Prerequisites:** none.
- **Files to create/modify:**
  - `services/backend-api/tests/test_notifications.py` — extend `TestUpdatePreferences`.
  - `services/backend-api/tests/test_health_alert_preferences.py` — extend the
    health-drop prefs tests if the injected default is asserted there.
- **Implementation steps:**
  1. In `tests/test_notifications.py` add (all expected to FAIL before impl):
     - `test_put_without_channel_discord_preserves_stored_value`: create a pref
       row with `channel_discord=False` (via PUT with the field false), then PUT
       the same type **without** `channel_discord` in the payload; assert GET
       still returns `channel_discord: false`.
     - `test_put_without_channel_discord_preserves_stored_true`: same with
       stored `true` → still `true`.
     - `test_put_channel_discord_false_round_trips`: PUT `channel_discord: false`
       → GET returns `false`.
     - `test_put_new_row_without_channel_discord_defaults_true`: PUT a type with
       no existing row, omit `channel_discord` → GET returns `true`.
     - `test_get_preferences_returns_channel_discord_true`: GET returns
       `channel_discord: true` for all types on a fresh DB (column default).
  2. Run `pytest tests/test_notifications.py -v` — new tests fail (missing field),
     existing tests must still pass.
- **Validation:** new tests RED, rest of the notifications suite green.
- **Commit message:** `test(notifications): pin channel_discord round-trip + absent-field sentinel (RED)`

### Phase 2: migration + model column

- **Goal:** schema carries `channel_discord`, default true.
- **Prerequisites:** Phase 1.
- **Files to create/modify:**
  - `services/backend-api/alembic/versions/<new>_add_channel_discord_to_alert_prefs.py` (new)
  - `services/backend-api/src/models/user_alert_preference.py`
- **Implementation steps:**
  1. Copy the shape of `d3e4f5g6h7i8_add_channel_intercom_to_alert_prefs.py`:
     `revision` = unique id, `down_revision = '8114adde5d96'` (verify with
     `alembic heads` before writing), upgrade =
     `op.add_column('user_alert_preferences', sa.Column('channel_discord', sa.Boolean(), nullable=False, server_default='true'))`,
     downgrade = `op.drop_column('user_alert_preferences', 'channel_discord')`.
  2. Model: add `channel_discord = Column(Boolean, default=True, nullable=False)`
     after `channel_intercom` (line ~16 of
     `src/models/user_alert_preference.py`).
  3. `alembic upgrade head` against a scratch DB (or rely on CI) — verify the
     column exists with default true.
- **Validation:** `alembic heads` → exactly one head; `alembic history` chains
  through the new revision.
- **Commit message:** `feat(models): channel_discord column on user_alert_preferences (default true)`

### Phase 3: GREEN — GET/PUT wiring

- **Goal:** API round-trips the field; sentinel honored.
- **Prerequisites:** Phase 2.
- **Files to modify:** `services/backend-api/src/api/routes/notifications.py`
- **Implementation steps:**
  1. `AlertPreferenceItem` (`:82-91`): add `channel_discord: bool = True` after
     `channel_intercom`.
  2. GET construction (`:354-364`): add `channel_discord=p.channel_discord`.
  3. GET injected `customer_health_drop` default (`:368-378`): add
     `channel_discord=True`.
  4. `AlertPreferenceUpdate` (`:98-107`): add `channel_discord: Optional[bool] = None`.
  5. PUT update loop (`:396-403`): after `pref.channel_intercom = ...` add
     `if item.channel_discord is not None: pref.channel_discord = item.channel_discord`.
  6. PUT create branch (`:405-415`): add
     `channel_discord=item.channel_discord if item.channel_discord is not None else True,`.
  7. Run `pytest tests/test_notifications.py -v` → all green.
- **Validation:** Phase 1 tests pass; `pytest tests/ -v` in `services/backend-api`
  fully green.
- **Commit message:** `feat(api): channel_discord in preferences GET/PUT with None = unchanged`

### Phase 4: sweep

- **Goal:** no other backend surface reads prefs assuming the old shape.
- **Implementation steps:** grep for `channel_intercom` usages in
  `services/backend-api/src` and `services/backend-api/tests` — any place that
  builds a `UserAlertPreference` or `AlertPreferenceItem`/`AlertPreferenceUpdate`
  without the new field is fine (sentinel covers it); flag anything that copies
  dicts of channel fields.
- **Validation:** full backend suite + single alembic head.
- **Commit message:** (none — include any fixups in Phase 3 commit or a `fix:` if needed)

## File-by-file build order

1. `services/backend-api/alembic/versions/*_add_channel_discord_to_alert_prefs.py` (new)
2. `services/backend-api/src/models/user_alert_preference.py`
3. `services/backend-api/src/api/routes/notifications.py`
4. `services/backend-api/tests/test_notifications.py`
5. `services/backend-api/tests/test_health_alert_preferences.py` (only if injected-default assertions need the field)

## Testing strategy

- Unit: the Phase 1 tests (round-trip, absent-field sentinel ×2, new-row default, GET default).
- Integration: `pytest tests/test_notifications.py -v` then the full backend suite.
- Manual (optional): `GET/PUT /api/v1/notifications/preferences` via Swagger at
  http://localhost:8000/docs after `alembic upgrade head`.

## Environment & secrets

- None new. Requires the worktree backend venv + a reachable PostgreSQL
  (the service `.env` was copied into the worktree in Phase 0).

## Edge cases & error handling

- **Absent vs false:** the whole point of the sentinel — `None` must never be
  written to the column.
- **New-row creation:** default `True` when omitted (DB default parity).
- **Multi-org:** prefs are user-scoped via `current_user` — unchanged.

## Agent execution notes

- Checkpoint after Phase 3 (GREEN): run the full backend suite before moving to
  `worker-dispatch`.
- Do not touch the worker mirror or the frontend in this aspect.
