# Plan — docs

**Slug:** `discord-channel-preferences` · **Date:** 2026-08-09
**Spec:** `../docs/spec.md` · **PRD refs:** S1
**Branch:** `feat/discord-channel-preferences` · Worktree root: repo root of this worktree

## Project setup checklist

- Repo-root files only (`docs/SELF_HOSTING.md`, `CHANGELOG.md`). No services,
  no packages, no env, no migrations.

## Implementation phases

### Phase 1: SELF_HOSTING.md — replace the coupling callout

- **Goal:** operators read the new per-type behavior, not the limitation.
- **Files to modify:** `docs/SELF_HOSTING.md`
- **Implementation steps:**
  1. Locate the "Discord alerts" section (`:678-710`).
  2. Replace the block from "**⚠️ Discord currently rides on the Slack toggle.**"
     through "A dedicated `channel_discord` preference is a schema change and is
     not in this release." with new copy stating:
     - Settings → Notifications now has a per-alert-type **Discord** channel
       switch (default on, matching the old behavior of riding Slack).
     - Slack and Discord are routed independently — a type can be Discord-only
       (Slack off) or Slack-only (Discord off).
     - Discord delivery still requires an active Discord integration.
     - Keep the two consequences' replacement: turning both off silences chat
       delivery for that type.
  3. Keep intact: the custom-webhook warning ("Why you cannot just point a
     custom webhook at Discord") and the "Not covered by Discord in this
     release:" list (automation-rule notifications etc. — still accurate).
- **Validation:** `rg -n "rides on the Slack toggle|schema change and is not in this release" docs/SELF_HOSTING.md` → no matches.
- **Commit message:** `docs(self-hosting): Discord alerts route per-type off channel_discord`

### Phase 2: CHANGELOG.md — Unreleased entry

- **Goal:** the change is announced under Unreleased.
- **Files to modify:** `CHANGELOG.md`
- **Implementation steps:**
  1. Under `## Unreleased`, add a section (before or after the existing security
     entry — keep reverse-chronological order with newest first):
     - Title: e.g. "Notifications — Discord now has its own per-type channel
       preference".
     - Body: per-type Discord switch in Settings → Notifications (default on,
       so existing delivery is preserved); Slack and Discord route independently;
       the one behavior change: a type with Slack off and Discord on now
       delivers to Discord; automations `send_notification` still cannot target
       Discord (unchanged).
- **Validation:** entry present under `## Unreleased`.
- **Commit message:** `docs(changelog): per-type Discord channel preference`

### Phase 3: sweep

- **Goal:** no other public surface still describes the coupling.
- **Implementation steps:**
  1. `rg -rn "rides on the Slack toggle|piggyback" docs/ README.md services/landing-web/` → no matches.
  2. `rg -rn "channel_discord" docs/ README.md` → only the new docs text.
- **Validation:** both greps clean.
- **Commit message:** (none unless fixups)

## File-by-file build order

1. `docs/SELF_HOSTING.md`
2. `CHANGELOG.md`

## Testing strategy

- No code tests — grep validations above; final branch check:
  `cd services/backend-api && pytest tests/ -v` ·
  `cd services/worker-service && pytest tests/ -v` ·
  `cd services/frontend-web && npm run test && npm run lint` (all already green
  from prior aspects — re-run once to confirm the branch is green end-to-end).

## Environment & secrets

- None.

## Edge cases & error handling

- The "Not covered by Discord" list mentions the automations channel limitation
  — that limitation still holds (PRD out-of-scope), so keep it.

## Agent execution notes

- Last aspect; run after the three code aspects are green.
- If the SELF_HOSTING.md section drifted during the code work, re-read the live
  text before editing.
