# Plan — worker-dispatch

**Slug:** `discord-channel-preferences` · **Date:** 2026-08-09
**Spec:** `../worker-dispatch/spec.md` · **PRD refs:** R3, R4
**Branch:** `feat/discord-channel-preferences` · Worktree root: repo root of this worktree

## Project setup checklist

- Service: `services/worker-service` only.
- No new packages, no new env keys.
- No new migration (the `channel_discord` column ships in `backend-prefs-api`'s
  migration — the worker mirror is a code-only model copy).
- Run everything from `services/worker-service` with the worktree venv.

## Implementation phases

### Phase 1: RED — rework coupling tests to assert independence

- **Goal:** the tests pin the new independent-dispatch behavior first.
- **Prerequisites:** `backend-prefs-api` merged onto the branch (worker suite runs
  against a migrated DB with the new column).
- **Files to create/modify:**
  - `services/worker-service/tests/test_discord_dispatch.py`
  - `services/worker-service/tests/test_discord_health_dispatch.py`
- **Implementation steps:**
  1. In `test_discord_dispatch.py`, replace
     `TestDispatchAlertTriggersDiscord::test_dispatch_alert_calls_discord_dispatch_when_slack_channel_enabled`
     (`:213-251`) with a class of four tests (each builds a pref row and asserts
     the two mocked dispatchers):
     - `channel_slack=True, channel_discord=True` → slack + discord both called.
     - `channel_slack=True, channel_discord=False` → slack called,
       **discord not called** (this is the RED test — fails on current code).
     - `channel_slack=False, channel_discord=True` → discord called,
       **slack not called** (RED).
     - no pref row → both called (default `channel_discord=True`).
     - Also assert `counts["discord"]` in the returned dict.
  2. Same split in `test_discord_health_dispatch.py::TestDispatchHealthDropAlertTriggersDiscord`
     (`:230-271`): slack-only, discord-only, both, no-pref defaults, plus
     `counts["discord"]`.
  3. Check `test_health_dispatch.py` for exact counts-dict equality assertions;
     if any `counts == {...}` exact matches exist, extend them with
     `"discord": N` in the same commit so they stay green (or confirm they use
     `>=` per-key assertions).
  4. Run `pytest tests/test_discord_dispatch.py tests/test_discord_health_dispatch.py -v`
     — the new independent cases FAIL on current code; everything else passes.
- **Validation:** RED for the four independent-dispatch tests.
- **Commit message:** `test(discord): pin independent Slack/Discord dispatch on both pipes (RED)`

### Phase 2: worker model mirror

- **Goal:** worker can read the new column.
- **Prerequisites:** Phase 1 (RED).
- **Files to modify:** `services/worker-service/src/models/__init__.py`
- **Implementation steps:**
  1. In `UserAlertPreference` (`:379-395`), after `channel_slack` add:
     - `channel_discord = Column(Boolean, default=True, nullable=False)`
     - `channel_intercom = Column(Boolean, default=False, nullable=False)`
     (matching the backend model — the `channel_intercom` back-fill closes the
     pre-existing mirror drift).
- **Validation:** worker imports still load; suite unaffected by the columns alone.
- **Commit message:** `feat(worker): channel_discord + channel_intercom on UserAlertPreference mirror`

### Phase 3: GREEN — decouple the main pipe

- **Goal:** `dispatch_alert` dispatches Discord off its own pref.
- **Prerequisites:** Phase 2.
- **Files to modify:** `services/worker-service/src/notification_dispatch.py`
- **Implementation steps:**
  1. `counts = {"inapp": 0, "slack": 0, "email": 0}` → add `"discord": 0`
     (`:571`); update the docstring return note (`:567`) to
     `{inapp, slack, discord, email}`.
  2. Per-user loop (`:597-621`): after `channel_slack` add
     `channel_discord = pref.channel_discord if pref else True`; in the channel
     block add `if channel_discord: counts["discord"] += 1`.
  3. Dispatch block (`:625-630`): replace with two independent ifs:
     ```python
     if counts["slack"] > 0:
         _dispatch_slack_alert(org_id, alert_type, title, message, link)
     if counts["discord"] > 0:
         _dispatch_discord_alert(org_id, alert_type, title, message, link)
     ```
     Remove the piggyback comment.
  4. Run the RED tests → all green.
- **Validation:** `pytest tests/test_discord_dispatch.py -v` green.
- **Commit message:** `feat(worker): dispatch_alert dispatches Discord off its own channel_discord pref`

### Phase 4: GREEN — decouple the health-drop pipe

- **Goal:** `dispatch_health_drop_alert` dispatches Discord off its own pref.
- **Prerequisites:** Phase 3.
- **Files to modify:** `services/worker-service/src/notification_dispatch.py`
- **Implementation steps:**
  1. `counts = {"inapp": 0, "slack": 0, "email": 0}` → add `"discord": 0`
     (`:333`); docstring return note (`:329`) updated.
  2. Per-user loop (`:408-439`): add
     `channel_discord = pref.channel_discord if pref else True` and an
     `any_discord` flag: `if channel_discord: counts["discord"] += 1; any_discord = True`.
  3. Dispatch block (`:443-468`): keep the Slack block under `if any_slack:`;
     move the Discord embeds + `_dispatch_discord_health_alert` into their own
     `if any_discord:` block (the `build_discord_health_alert_embeds` call moves
     with it).
  4. Run the health-drop tests → all green.
- **Validation:** `pytest tests/test_discord_health_dispatch.py tests/test_health_dispatch.py -v` green.
- **Commit message:** `feat(worker): health-drop pipe dispatches Discord off its own channel_discord pref`

### Phase 5: full suite + boundary check

- **Goal:** branch green end-to-end.
- **Implementation steps:**
  1. `pytest tests/ -v` in `services/worker-service` — full suite green.
  2. Grep the four `dispatch_alert` callers (`insights.py:82`, `anomaly.py:185`,
     `alerts.py:315`, `alerts.py:749`) — none consume counts keys by exact shape;
     report if any do.
  3. `rg -n "channel_discord" services/worker-service/` — appears in model +
     dispatch + tests only.
- **Validation:** full worker suite green.
- **Commit message:** (none unless fixups — fold into Phase 3/4 commits)

## File-by-file build order

1. `services/worker-service/src/models/__init__.py`
2. `services/worker-service/src/notification_dispatch.py`
3. `services/worker-service/tests/test_discord_dispatch.py`
4. `services/worker-service/tests/test_discord_health_dispatch.py`
5. `services/worker-service/tests/test_health_dispatch.py` (only if counts-shape assertions need extending)

## Testing strategy

- Unit: the four-way dispatch matrix on both pipes + no-pref defaults + counts.
- Integration: full worker suite; the callers' existing tests
  (`test_store_analysis_result.py`, `test_anomaly_integration.py`,
  `test_alerts.py`) already exercise `dispatch_alert` — they stay green.

## Environment & secrets

- None new. Worker venv in the worktree; a migrated PostgreSQL with the new
  column (run `alembic upgrade head` in `services/backend-api` first).

## Edge cases & error handling

- **No pref row:** defaults `channel_discord=True` (matches column default) —
  behavior preserved.
- **Counts consumers:** the `"discord"` key is additive; the four callers treat
  the dict opaquely (verify in Phase 5).
- **Mirror drift closed:** worker now also carries `channel_intercom`; nothing
  reads it at dispatch — behavior-neutral.

## Agent execution notes

- Checkpoint after Phase 4: run the full worker suite before `frontend-page`.
- Do not touch backend routes/models or the frontend in this aspect.
- Remember the repo rule: no bare `try/except` around worker imports.
