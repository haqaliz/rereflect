# Implementation Plan — customers-bulk-ui (2026-07-09)

**Aspect spec:** `./spec.md` · **PRD:** `../prd.md` · **Sequence:** LAST (needs both backend aspects).
**Service touched:** `services/frontend-web` only.

## 1. Setup
- No new deps (checkbox exists at `components/ui/checkbox.tsx`; `sonner` toasts; TanStack Query already used).
- Files: `customers/page.tsx`, `components/shared/data-table.tsx` (small `getRowId` add), new dialogs under
  `components/customers/`, `lib/api/customers.ts`, `lib/api/playbooks.ts`, `SegmentBadge`/tags display.

## 2. Phases

### Phase 1 — API client + types
- **Files:** `lib/api/customers.ts`, `lib/api/playbooks.ts`, tests.
- **Steps:**
  1. Add `tags?: string[]` and `cs_owner?: {id:number; email:string} | null` to
     `CustomerListItem` + `CustomerProfileData`.
  2. Add client fns: `exportCustomers(params)` (build query, hit `/customers/export`, return Blob/trigger
     download), `bulkTag(cohort, tags, mode)` → `POST /customers/bulk/tags`,
     `bulkAssignOwner(cohort, userId|null)` → `POST /customers/bulk/assign-owner`. Define a shared `Cohort`
     TS type mirroring backend (`{emails?:string[]} | {filter?:{segment?;risk_level?;search?;include_archived?}}`).
  3. Extend `playbooks.ts` `runPlaybookBatch` filter type with `emails?`, `segment?`.
  4. RED tests on payload builders (cohort serialization: filter-mode sends `filter`, not `emails`).
- **Validation:** `npm run test` (payload builders), `npm run lint`.
- **Commit:** `feat(segment-actions): customers bulk API client + cohort types`

### Phase 2 — Row selection (checkbox column + getRowId)
- **Files:** `components/shared/data-table.tsx`, `app/(dashboard)/customers/page.tsx`.
- **Steps:**
  1. Add optional `getRowId?: (row)=>string` prop to shared `DataTable`, pass into `useReactTable`
     (backward-compatible — default undefined keeps index keys). **Grep other shared-DataTable consumers**
     first; confirm no break.
  2. Add a `select` checkbox column to customers `columns` (mirror `feedbacks/columns.tsx:91-112`).
  3. Wire `rowSelection`/`onRowSelectionChange` state; pass `getRowId={r=>r.customer_email}`; derive
     `selectedEmails` from selection keys.
- **Validation:** `npm run test` + `npm run lint`; manual: checking rows populates the existing "Mark N as
  churned" button.
- **Commit:** `feat(segment-actions): row selection on customers table (getRowId=email)`

### Phase 3 — Cohort mode + bulk toolbar
- **Files:** `customers/page.tsx`.
- **Steps:**
  1. Cohort state: `{mode:'emails', emails} | {mode:'filter', filter}`. "Select all N matching this filter"
     banner/menu item switches to filter mode (builds `filter` from active `queryParams`).
  2. Bulk-actions toolbar/`DropdownMenu` in the header (near L409-420): Export CSV, Tag, Assign owner, Run
     playbook. Visible when a cohort is active.
- **Validation:** `npm run lint` + `npm run test`.
- **Commit:** `feat(segment-actions): cohort mode + bulk-actions toolbar`

### Phase 4 — Dialogs
- **Files:** `components/customers/BulkTagDialog.tsx`, `BulkAssignOwnerDialog.tsx`,
  `BulkRunPlaybookDialog.tsx`; `customers/page.tsx` (mount + export handler).
- **Steps:** Each mirrors `BulkMarkChurnedDialog` props `{open,onOpenChange,cohort,onSuccess}`; `sonner`
  toast; `queryClient.invalidateQueries(['customers'])` on success. Tag dialog: input + add/remove toggle +
  affected-count. Assign dialog: org-member picker (fetch via existing team API) + Unassign. Run-playbook
  dialog: `listPlaybooks()` picker + affected-count (call run-batch `count_only`) + **warn/block when count >
  500**. Export: anchor/`window.location` download with current filters (no dialog, or a confirm for large).
- **Validation:** `npm run test` (dialog→client-fn call with correct cohort), `npm run lint`.
- **Commit:** `feat(segment-actions): bulk tag/assign/run-playbook dialogs + CSV export`

### Phase 5 — Surface tags + cs_owner
- **Files:** `customers/page.tsx` columns, `customers/[email]/page.tsx`, a small tags-chips component.
- **Steps:** add `tags` chips (Sunset-Horizon `color-mix` theming) + `cs_owner` column/badge on list; show
  both on profile. Unassigned owner renders cleanly.
- **Validation:** `npm run lint` + `npm run build`.
- **Commit:** `feat(segment-actions): show tags + CS owner on customers list/profile`

### Phase 6 — Docs
- **Files:** `docs/SELF_HOSTING.md` (operator note on bulk actions).
- **Commit:** `docs(segment-actions): SELF_HOSTING note for customer bulk actions`

## 3. File build order
1. `lib/api/customers.ts`, `lib/api/playbooks.ts` — client + types.
2. `components/shared/data-table.tsx` — `getRowId`.
3. `customers/page.tsx` — selection, cohort mode, toolbar, dialog mounts.
4. `components/customers/BulkTagDialog.tsx`, `BulkAssignOwnerDialog.tsx`, `BulkRunPlaybookDialog.tsx`.
5. `customers/[email]/page.tsx` + tags-chips — surfacing.
6. `docs/SELF_HOSTING.md`.

## 4. Testing
- Vitest for payload builders + dialog interactions where supported; otherwise assert on client-fn payloads
  and rely on `npm run lint` + `npm run build`. Manual: `npm run dev`, select cohort, run each action against
  a local backend; confirm toast + refetch + backend effect.

## 5. Env & secrets
- None new. Needs backend running (`NEXT_PUBLIC_API_URL`).

## 6. Edge cases
- Filter-mode must send `filter` (not thousands of emails) — assert in test.
- Export respects active filters; large export → optional confirm.
- Run-playbook count > 500 → block with the cap message (from backend 422 too — handle both).
- `getRowId` change must not break other shared-DataTable consumers (grep + verify).
- Theming: CSS variables only, no hardcoded colors (`SegmentBadge` precedent).

## 7. Agent notes
- Build only after both backend aspects are green (contracts fixed).
- Keep `CohortFilter` key names identical to backend (`segment`, `risk_level`, `search`, `include_archived`).
- Checkpoint after Phase 2 (shared DataTable change) for human review.
