# Plan — dispatch-ordering (2026-09-25)

## Phase 1 — Backend engine (`services/backend-api`)
- **RED:** add `test_run_playbook_commits_before_send_task` to `tests/test_automation_engine_run_playbook.py`,
  mirroring `tests/test_automation_engine_send_customer_email.py:445-484` (spy `db.commit`,
  `send_task.side_effect` appends; assert order; assert returned `execution_id` == committed row id).
  Run → FAIL (commit only at `_evaluate_rule:203`, after send_task).
- **GREEN:** `automation_engine.py:826-827` replace `self.db.flush()` with a `COMMIT BEFORE PUBLISH`
  comment + `self.db.commit()`.
- Verify: `venv/bin/python -m pytest tests/test_automation_engine*.py tests/test_automations_api*.py tests/test_health_score*.py -q`.
- Commit: `fix(automations): commit run_playbook execution before publishing (backend engine)`.

## Phase 2 — Worker mirrors (`services/worker-service`, `SENTRY_DSN=""`)
- **RED:** `test_run_playbook_commits_before_enqueueing` in `tests/test_automation_churn_trigger.py`
  and `tests/test_automation_usage_trend_trigger.py`, mirroring
  `tests/test_automation_email_delivery.py:388-416`. Both FAIL.
- **GREEN:** `automation_churn_trigger.py:290` and `automation_usage_trend_trigger.py:333` flush → commit, same comment.
- Verify: full worker suite `venv/bin/python -m pytest tests/ -q`.
- Commit: `fix(automations): commit run_playbook execution before enqueueing (worker mirrors)`.

## Edge cases
Error branches return before the add → no commit/publish change. Shadow skips actions. Nested
`trigger_automation` → pinned mid-run-commit test must stay green.
