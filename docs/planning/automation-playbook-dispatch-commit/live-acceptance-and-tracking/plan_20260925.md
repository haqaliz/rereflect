# Plan — live-acceptance-and-tracking (2026-09-25)

1. Preconditions: `pg_isready`, `redis-cli ping`. Generate Fernet key.
2. `createdb rr_dispatch_scratch`; `DATABASE_URL=postgresql:///rr_dispatch_scratch LLM_ENCRYPTION_KEY=… venv/bin/alembic upgrade head` (backend venv).
3. Seed via a script (scratchpad): org, active playbook with a `tag` action, active
   `churn_probability_threshold` rule (threshold low, cooldown 1h) with `run_playbook`, a `CustomerHealth`
   row with high churn probability.
4. Start a worker from the worktree: `DATABASE_URL=… REDIS_URL=redis://localhost:6379/9 CELERY_BROKER_URL=… venv/bin/celery -A src.celery_app worker -Q <queues> --loglevel=info` (check the env names in `src/celery_app.py`/config).
5. Call `evaluate_churn_probability_triggers` from a script using the same env; wait; read execution status + worker log.
6. Repeat with master's `automation_churn_trigger.py` (via `git show origin/master:...` into a temp copy or a checkout of the file, then restore) to capture the failing baseline. Clear the Redis cooldown between runs.
7. Stop worker, `dropdb`. Write `evidence.md`.
8. Docs: DEV-TRACKING entry (+ out-of-scope follow-ups), AI-TRACKING annotations, CHANGELOG `Fixed`.
