# Plan — source-events-dispatch (2026-09-25)

- **RED:** in the existing source-events test file (find via `grep -l process_source_event tests/`),
  add `test_auto_import_commits_before_enqueueing_analysis`: spy `db.commit` / patch
  `src.tasks.analysis.analyze_single_feedback` `.delay` side_effect; assert order + id. FAIL on master.
- **GREEN:** remove the inline `.delay` at `source_events.py:469-470`; after `db.commit()` at `:127`,
  publish `analyze_single_feedback.delay(fid)` for each result with `status == "feedback_created"`,
  each in try/except that logs (sweeper `process_unanalyzed_feedback` recovers). Comment why.
- Verify: full worker suite.
- Commit: `fix(ingestion): commit webhook feedback before enqueueing analysis`.
