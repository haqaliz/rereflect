# Implementation Plan — `write-scope` (2026-07-06)

**Aspect spec:** `./spec.md` · **PRD:** `../prd.md` · **Branch:** `feat/public-api-write-crud`
Small, self-contained aspect. Register the `write` scope end-to-end (backend allowlist + docs + frontend picker/type/badge). No DB migration.

## Services touched
- `services/backend-api` — scope allowlist + doc strings.
- `services/frontend-web` — scope type, picker, badge.
- `docs/API.md` — public scope list.

## Phase 1 — Backend: allow the `write` scope (TDD)

**Files:**
- `services/backend-api/src/api/routes/api_keys.py` (`_VALID_SCOPES` @ :31)
- `services/backend-api/src/models/api_key.py` (comment @ :22)
- `services/backend-api/tests/test_api_keys.py` (find/create)

**Steps:**
1. **RED** — add tests: (a) create key with `scopes=["write"]` → 201, persisted scope string contains `write`; (b) create with `scopes=["read","write"]` → sorted `read,write`; (c) unknown scope still → 422 (regression); (d) empty scopes → 422 (regression).
2. **GREEN** — `_VALID_SCOPES = frozenset({"read", "ingest", "write"})`. Update the `# comma list: read,ingest` comment on `api_key.py:22` → `read,ingest,write`.
3. Run: `cd services/backend-api && pytest tests/test_api_keys.py -v`.

**Validation:** all four assertions green.
**Commit:** `feat(api-keys): allow "write" scope on public API keys`

## Phase 2 — Backend: document the scope in the public OpenAPI + docstring

**Files:** `services/backend-api/src/api/routes/public_api.py` (module docstring `:8–11`, OpenAPI `info.description` `:610–611`)

**Steps:**
1. **RED** — test hitting `GET /api/public/v1/openapi.json` asserts `"write"` appears in `info.description`. (Reuse the existing public-API test client; see `tests/test_public_api.py`.)
2. **GREEN** — extend the scope sentence to mention `**write**` (e.g. "…and **write** (PATCH endpoints for mutating existing feedback)."). Update the module docstring scope table similarly.
3. Run: `pytest tests/test_public_api.py -v`.

**Validation:** OpenAPI description test green.
**Commit:** `docs(public-api): document the write scope in OpenAPI + docstring`

## Phase 3 — Frontend: scope type, picker, badge (TDD)

**Files:**
- `services/frontend-web/lib/api/api-keys.ts` (`ApiKeyScope` @ :5)
- `services/frontend-web/app/(dashboard)/settings/api-keys/page.tsx` (scope array @ :152, description ternary @ :167–171, `ScopeBadge` map @ :53–65)
- A colocated test (Vitest) for the scope-description mapping + badge color.

**Steps:**
1. `ApiKeyScope = 'read' | 'ingest' | 'write'`.
2. Add `'write'` to the `(['read','ingest'] as const)` array → `(['read','ingest','write'] as const)`.
3. **Replace the binary description ternary** (`:167–171`) with a lookup object, e.g.
   ```ts
   const SCOPE_DESCRIPTIONS: Record<ApiKeyScope, string> = {
     read: 'Read feedback, customers, and analytics via the public API',
     ingest: 'Submit new feedback and enqueue it for AI analysis',
     write: 'Update existing feedback (status, category/sentiment corrections) via the public API',
   }
   ```
   Render `SCOPE_DESCRIPTIONS[scope]`.
4. Add a `write` entry to the `ScopeBadge` color map (a distinct theme color — use an existing CSS-variable-backed Tailwind class already in that map's palette; do **not** hardcode a new hex). Confirm no default gray fallthrough.
5. **RED→GREEN** — Vitest: assert `SCOPE_DESCRIPTIONS.write` is defined + non-empty, and `ScopeBadge` renders a non-default class for `write`. (Follow the nearest existing component test pattern; if the page has no test yet, extract the map/`ScopeBadge` enough to unit-test, or test via render.)
6. Run: `cd services/frontend-web && npm run test && npm run lint`.

**Validation:** type-checks; tests + lint green; `write` shows its own description and badge color.
**Commit:** `feat(api-keys-ui): add write scope option (picker, type, badge)`

## Phase 4 — User docs

**Files:** `services/frontend-web/`… none; `docs/API.md` (`:68`, `:76–77`)

**Steps:**
1. `docs/API.md:68` — drop "read-only"; describe the API as read + ingest + write.
2. `:76–77` — list scopes `read`, `ingest`, `write` with one-line each; note write endpoints are documented under the feedback-mutation section (added by the other aspect).

**Validation:** manual read; no "read-only" left.
**Commit:** `docs(api): list the write scope in docs/API.md`

## File-by-file build order
1. `api_keys.py` + `test_api_keys.py` — allowlist (P1)
2. `models/api_key.py` — comment (P1)
3. `public_api.py` docstrings + `test_public_api.py` — OpenAPI (P2)
4. `lib/api/api-keys.ts` — type (P3)
5. `settings/api-keys/page.tsx` (+ test) — picker/badge (P3)
6. `docs/API.md` — scope list (P4)

## Edge cases
- Unknown scope must still 422 (don't loosen validation).
- Existing keys with `read,ingest` unaffected (additive).
- `ScopeBadge` must not fall through to gray for `write`.

## Agent execution notes
- Phases 1–2 (backend) and 3 (frontend) are independent → parallelizable across two agents; Phase 4 is trivial docs.
- Checkpoint: none needed; low risk. This aspect can merge before `feedback-write-endpoint` uses the scope.
