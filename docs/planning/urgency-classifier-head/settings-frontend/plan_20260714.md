# Implementation Plan — settings-frontend (Next.js)

**Aspect:** `settings-frontend` · **Feature:** `urgency-classifier-head` · **Date:** 2026-07-14
**Spec:** `docs/planning/urgency-classifier-head/settings-frontend/spec.md`
**Service:** `services/frontend-web`
**Sequence:** Wave 2. **Blocked by data-and-config** (settings API must return/accept `urgency_classifier_mode`).

## 0. Summary

Add an off/shadow/auto urgency mode toggle to Settings → AI (General) and an urgency accuracy card — a
mechanical copy of the category head. Grounded against the frontend dig (exact refs):
- `lib/api/ai-settings.ts` — `AISettings` (`:19` has `category_classifier_mode`), `AISettingsUpdate` (`:31`).
- `components/settings/AISettingsGeneral.tsx` — category `<Select>` mode card (`:217-263`), state
  (`:28-29`), `handleCategoryClassifierMode` (`:82-96`), `CLASSIFIER_MODE_LABELS` (`:10-14`), icon import (`:7`).
- `components/settings/ClassifierAccuracyCard.tsx` — `classifierType` prop (default `'sentiment'`, `:106`),
  `TYPE_COPY` record (`:28-40`); fetch/rollback already type-generic.
- `lib/api/classifier-accuracy.ts` — `getClassifierAccuracy(type)` / `rollbackClassifier(type)` already
  accept any type string → **zero change**.
- `app/(dashboard)/settings/ai/page.tsx` — sentiment card (`:485`), category card (`:488`),
  `CORRECTION_TYPE_LABELS` (`:47-52`).

## 1. Project Setup

- No new deps. Node/Vitest in `services/frontend-web`.

## 2. Implementation Phases (strict TDD — Vitest)

### Phase 1 — Types

- **Files:** `lib/api/ai-settings.ts`; test `lib/api/__tests__/ai-settings.urgency.test.ts` (mirror
  `ai-settings.category.test.ts`).
- **RED:** test that `AISettings` includes `urgency_classifier_mode` and `AISettingsUpdate` accepts it
  (compile + client round-trip with a mocked axios).
- **GREEN:** add `urgency_classifier_mode: string` to `AISettings` (after `:19`) and
  `urgency_classifier_mode?: string | null` to `AISettingsUpdate` (after `:31`).
- **Commit:** `feat(urgency-fe): urgency_classifier_mode on AISettings types`

### Phase 2 — Mode toggle card

- **Files:** `components/settings/AISettingsGeneral.tsx`; test
  `__tests__/settings/AISettingsGeneral.urgencyClassifier.test.tsx` (mirror the category test).
- **RED:** test: urgency `<Select>` renders with off/shadow/auto; changing it calls
  `aiSettingsAPI.update({ urgency_classifier_mode: <value> })`; shows saved state; renders error on failure.
- **GREEN:**
  - add state `urgencyClassifierSaving` / `urgencyClassifierError` (mirror `:28-29`),
  - add `handleUrgencyClassifierMode` (copy `handleCategoryClassifierMode` `:82-96`, guard
    `settings.urgency_classifier_mode`, call `aiSettingsAPI.update({ urgency_classifier_mode })`),
  - add a 4th `<Select>` card copied verbatim from the category card (`:217-263`): swap icon to
    `AlertTriangle` (import from `lucide-react` at `:7`), title "Self-Improving Urgency Classifier", honest
    copy, and bind to `settings.urgency_classifier_mode ?? 'off'` / `handleUrgencyClassifierMode` /
    `urgencyClassifierSaving` + the error line + `aria-label="Urgency classifier mode"`.
  - Use CSS variables only (the copied card already does).
- **Commit:** `feat(urgency-fe): off/shadow/auto urgency classifier mode card (AI Settings General)`

### Phase 3 — Accuracy card + page wiring

- **Files:** `components/settings/ClassifierAccuracyCard.tsx`, `app/(dashboard)/settings/ai/page.tsx`;
  tests `__tests__/settings/ClassifierAccuracyCard.test.tsx` (extend),
  `__tests__/settings/AISettingsAIPage.accuracyTab.test.tsx` (extend).
- **RED:** test: `<ClassifierAccuracyCard classifierType="urgency" />` renders urgency copy (from
  `TYPE_COPY.urgency`, NOT the generic fallback) and calls `getClassifierAccuracy("urgency")` /
  `rollbackClassifier("urgency")`.
- **GREEN:**
  - add an `urgency` entry to `TYPE_COPY` (`:28-40`) with honest copy: trained on the org's urgency
    corrections; promoted only when it beats the keyword urgency heuristic on held-out data; note it's
    add-only in auto (escalates, never de-escalates) so operators aren't surprised.
  - add `<ClassifierAccuracyCard classifierType="urgency" isAdminOrOwner={isAdminOrOwner} />` after the
    category card (~`:488`) in the accuracy tab.
  - add `urgency: 'Urgency'` to `CORRECTION_TYPE_LABELS` (`:47-52`) (S-1, cosmetic — "Corrections by Type").
- **Commit:** `feat(urgency-fe): urgency ClassifierAccuracyCard + TYPE_COPY + corrections-by-type label`

## 3. File-by-File Build Order

| # | File | Purpose | Depends on |
|---|---|---|---|
| 1 | `lib/api/ai-settings.ts` | types | data-and-config API |
| 2 | `components/settings/AISettingsGeneral.tsx` | mode toggle card | types |
| 3 | `components/settings/ClassifierAccuracyCard.tsx` | urgency copy | — |
| 4 | `app/(dashboard)/settings/ai/page.tsx` | card wiring + label | card |
| — | `lib/api/classifier-accuracy.ts` | **no change** (type-generic) | — |

## 4. Testing Strategy

- `npm run test` (Vitest) + `npm run lint` in `services/frontend-web`.
- Mirror the existing category test files listed above.
- Regression: sentiment + category cards/toggles unaffected.

## 5. Environment & Secrets

None. Reads/writes go through the existing `aiSettingsAPI` (JWT). Accuracy card lights up once the backend
accuracy endpoint reports urgency (post worker-trainer).

## 6. Edge Cases & Error Handling

- `?? 'off'` null-guard on the mode value — older API responses may omit the field.
- Accuracy card empty-state (no promoted urgency model yet) — reuse the existing card's empty state; the
  honest copy should say the head is collecting corrections (ties to R-4 sparsity).
- Admin/owner gating already handled by the card's `isAdminOrOwner` prop + the settings route guard.

## 7. Agent Execution Notes

- **Blocked by data-and-config** — the settings API must already return/accept `urgency_classifier_mode`
  (verify a GET returns the field) before this is testable end-to-end; the unit tests mock the API so they
  can land first, but manual verification needs data-and-config merged.
- Keep copy honest (OSS brand): "your model, trained on your corrections", "beats the keyword heuristic",
  "add-only in auto". No fabricated accuracy numbers.
- Do NOT touch `classifier-accuracy.ts` — it's already type-generic.
