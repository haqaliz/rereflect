# Implementation Plan — data-and-config (backend + worker mirror)

**Aspect:** `data-and-config` · **Feature:** `urgency-classifier-head` · **Date:** 2026-07-14
**Spec:** `docs/planning/urgency-classifier-head/data-and-config/spec.md`
**Services:** `services/backend-api` (model + migration + settings API), `services/worker-service` (mirror + resolver map)
**Sequence:** Wave 1 (independent of urgency-core & capture-seam). Blocks predict-seam & settings-frontend.

## 0. Summary

Add `urgency_classifier_mode` (off/shadow/auto) to `OrgAIConfig`, exposed via the AI settings GET/PATCH,
and register it in the worker's resolver map — a **mechanical copy of the `category_classifier_mode`
plumbing**. Grounded against source:
- `VALID_CLASSIFIER_MODES = {"off","shadow","auto"}` (`ai_settings.py:45`).
- Model columns `classifier_mode` / `category_classifier_mode` = `Column(String(20), nullable=True,
  server_default='off', default='off')` (`org_ai_config.py:24,30`).
- Category PATCH block `ai_settings.py:551-572` includes a `_classifier_deps_available()` sklearn guard for
  shadow/auto — **urgency must mirror this**.
- Resolver map `MODE_COLUMN_BY_CLASSIFIER_TYPE` (`worker-service .../services/classifier_resolver.py:47-50`).
- **Alembic: single head `3e26b38cbd15`** (`add_feedback_source_lookup_index`) — verified; NO merge needed.
  (The category migration `b3c4d5e6f7a8` is the template; it chained off `v2w3x4y5z6a7`.)

## 1. Project Setup

- One new Alembic migration in `services/backend-api/alembic/versions/`.
- No new deps, no `.env`. Backend + worker venvs for tests.

## 2. Implementation Phases (strict TDD)

### Phase 1 — Model columns (backend + worker mirror)

- **Goal:** `urgency_classifier_mode` column on both `OrgAIConfig` definitions.
- **Files:**
  - `services/backend-api/src/models/org_ai_config.py`
  - the worker mirror (grep `category_classifier_mode` under `services/worker-service/src` — same class)
  - tests: `services/backend-api/tests/` (model import/attr test) + worker equivalent
- **RED:** a test asserting `OrgAIConfig().urgency_classifier_mode`-related metadata: column exists with
  `String(20)`, `server_default='off'`. Mirror any existing `category_classifier_mode` model test.
- **GREEN:** add directly under `category_classifier_mode` (line 30) in both files:
  ```python
  # 'off' | 'shadow' | 'auto'. Independent of classifier_mode (sentiment) and
  # category_classifier_mode — the urgency head (mirrors is_urgent boolean).
  urgency_classifier_mode = Column(String(20), nullable=True, server_default='off', default='off')
  ```
- **Validation:** backend `pytest tests/ -k org_ai_config -v`; worker equivalent.
- **Commit:** `feat(urgency-data): urgency_classifier_mode column on OrgAIConfig (both mirrors)`

### Phase 2 — Alembic migration

- **Goal:** additive column migration, single-head chain.
- **Files:** `services/backend-api/alembic/versions/e6f7a8b9c0d1_add_urgency_classifier_mode.py` (new; copy
  `b3c4d5e6f7a8_add_category_classifier_mode.py` verbatim and swap names).
- **Steps:**
  1. FIRST run `alembic heads` in the backend venv to CONFIRM the single head is still `3e26b38cbd15`
     (do not hardcode blindly — verify). If more than one head appears, STOP and report (do not
     auto-merge).
  2. `revision = 'e6f7a8b9c0d1'`, `down_revision = '3e26b38cbd15'`.
  3. `upgrade()`: `op.add_column("org_ai_config", sa.Column("urgency_classifier_mode", sa.String(20),
     nullable=True, server_default="off"))`. `downgrade()`: `op.drop_column(...)`.
- **Validation:** against a scratch/test DB: `alembic upgrade head` applies cleanly; `alembic downgrade -1`
  reverts. Confirm `alembic heads` shows a single head after.
- **Commit:** `feat(urgency-data): alembic e6f7a8b9c0d1 — urgency_classifier_mode column (down_revision 3e26b38cbd15)`

### Phase 3 — Settings API GET/PATCH

- **Goal:** GET returns `urgency_classifier_mode`; PATCH validates + persists it independently, with the
  sklearn guard for shadow/auto.
- **Files:** `services/backend-api/src/api/routes/ai_settings.py`; tests in
  `services/backend-api/tests/` (mirror the category settings test).
- **RED:** tests:
  - GET `/api/v1/settings/ai` includes `urgency_classifier_mode` default `"off"`.
  - PATCH `{urgency_classifier_mode:"shadow"}` persists (admin/owner); invalid value → 422; unchanged when
    field omitted; other mode fields untouched when only urgency sent.
  - shadow/auto without sklearn deps → the same error path as category (`_classifier_deps_available` guard).
- **GREEN:**
  - `AISettingsResponse` (after :70): `urgency_classifier_mode: str = "off"`.
  - `AISettingsUpdate` (after :81): `urgency_classifier_mode: Optional[str] = None`.
  - `_build_settings_response` (after :232): `urgency_classifier_mode = getattr(config,
    "urgency_classifier_mode", None) or "off" if config else "off"`; pass into the response (after :241).
  - `update_ai_settings`: **copy the category block (:551-572)** verbatim for `urgency_classifier_mode`
    (same `VALID_CLASSIFIER_MODES` + `_classifier_deps_available()` guard + `hasattr` persist).
- **Validation:** `pytest tests/ -k ai_settings -v` (backend).
- **Commit:** `feat(urgency-data): urgency_classifier_mode on AI settings GET/PATCH (off|shadow|auto, sklearn guard)`

### Phase 4 — Worker resolver map

- **Goal:** the predict resolver can read the urgency mode column.
- **Files:** `services/worker-service/src/services/classifier_resolver.py`; test mirror.
- **RED:** `MODE_COLUMN_BY_CLASSIFIER_TYPE["urgency"] == "urgency_classifier_mode"`; and
  `resolve_classifier(org, "urgency", db)` returns None when mode 'off', honors shadow/auto (mirror the
  existing category resolver test).
- **GREEN:** add `"urgency": "urgency_classifier_mode"` to the map (`:47-50`).
- **Validation:** worker `pytest -k classifier_resolver -v`.
- **Commit:** `feat(urgency-data): register urgency in MODE_COLUMN_BY_CLASSIFIER_TYPE`

## 3. File-by-File Build Order

| # | File | Purpose | Depends on |
|---|---|---|---|
| 1 | `backend .../models/org_ai_config.py` | urgency mode column | — |
| 2 | `worker .../models/...` (OrgAIConfig mirror) | same column | — |
| 3 | `backend .../alembic/versions/e6f7a8b9c0d1_*.py` | column migration | single head verified |
| 4 | `backend .../api/routes/ai_settings.py` | GET/PATCH field + validation | model col |
| 5 | `worker .../services/classifier_resolver.py` | resolver map entry | model col |

## 4. Testing Strategy

- Backend: `pytest tests/ -k "ai_settings or org_ai_config" -v`.
- Worker: `pytest -k classifier_resolver -v`.
- Migration: apply/revert on a scratch DB; `alembic heads` single-head assertion.
- Regression: existing category/sentiment settings tests stay green (independent field).

## 5. Environment & Secrets

- A DB to run the migration against (local Postgres per CLAUDE.md). No new secrets.

## 6. Edge Cases & Error Handling

- Invalid mode value → 422 (mirror category).
- shadow/auto without sklearn → guarded error (mirror category `_classifier_deps_available`).
- `getattr`-defensive read so older configs without the column default to `"off"`.
- Multi-tenancy: settings route already org-scoped via `current_org`; no change.

## 7. Agent Execution Notes

- **Checkpoint:** confirm `alembic heads` == single `3e26b38cbd15` BEFORE writing the migration; if the tree
  is multi-head, STOP and report (do not merge without approval).
- Keep backend + worker `OrgAIConfig` mirrors byte-consistent.
- Do NOT implement any consumption of the mode here (that's worker-trainer/predict-seam) — this aspect is
  column + API + resolver map only.
