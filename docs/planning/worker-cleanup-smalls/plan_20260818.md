# Implementation plan — worker-cleanup-smalls (2026-08-18)

Source: `docs/planning/worker-cleanup-smalls/prd.md`. House commit style.

## Phase 1 — Delete the orphans (RED first via the sweep test pattern)

- **Files:**
  - `services/worker-service/src/tasks/anomaly.py` — delete `_send_anomaly_slack`
    (:238-310), `_send_anomaly_discord` (:313-363), `_send_anomaly_email`
    (:223-235), the `send_discord_message_webhook` import (:16 — verify it's
    unused elsewhere in the module), and the `_decrypt` helper (:27-33 — verify
    `notification_dispatch.py` has its own; the orphans were its only users).
  - `services/worker-service/src/email.py` — delete `send_anomaly_alert_email`
    (:160) + its now-unused imports.
  - `services/worker-service/tests/test_discord_anomaly.py` — delete (7 tests pin
    the orphans).
  - `services/worker-service/tests/test_anomaly_email.py` — delete (tests the
    deleted sender).
  - `services/worker-service/tests/test_discord_alerts.py:7` — trim the docstring
    coverage list.
- **RED-first:** before deleting, write a characterization pin? No — the deletion
  itself is the change; the house pattern is the grep-sweep proof + the live-pipe
  tests staying green (`test_anomaly_integration.py::TestDispatchAnomalyAlerts`
  :345-409 pins the live delegation — it must stay green and UNCHANGED).
- **Sweep proof:** `grep -rn "_send_anomaly_slack\|_send_anomaly_discord\|_send_anomaly_email\|send_anomaly_alert_email" services/`
  → zero hits.
- **Commit:** `refactor(worker): delete dead anomaly alert senders (slack/discord/email orphans)`.

## Phase 2 — Docs + tracking

- DEV-TRACKING P6 (:211-219) → **FIXED** with the shipped summary (all three
  orphans + dead callee deleted; the main pipe covers Slack + Discord; anomaly
  email was unreachable — stated honestly) + `(merged <merge-sha>, PR <# pending>)`
  placeholder; the `docs/planning/discord-notifications/` docs referencing the
  orphans get pointer notes (no history rewriting); CHANGELOG cleanup entry
  (delivery unchanged via the main pipe; anomaly email removed as unreachable).
- **Commits:** `docs(...)` per house style.

## Verification

- Full worker suite green (`venv/bin/pytest tests/ -q`); the live anomaly tests
  unchanged; grep sweep zero.
