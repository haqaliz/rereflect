# Implementation Plan — docs-and-tracking (2026-07-07)

**Feature:** ai-drafted-issue-content · **Aspect:** docs-and-tracking · **Execution:** last, after both code aspects land & are green.

## Scope
Documentation + tracking updates only — no code. Mirrors how every prior integration shipped with docs.

## Files to modify
1. **`docs/SELF_HOSTING.md`** — add a short note: AI drafting appears as "Draft with AI" in the create-issue/
   task wizard when an LLM is configured (cloud BYOK **or** local Ollama/OpenAI-compatible); absent otherwise.
   Place near the existing AI/LLM configuration section.
2. **`docs/API.md`** — document `POST /api/v1/feedback/{id}/issue-draft`: auth (JWT, admin/owner), body
   `{target, tone?}`, `200 {title, body}`, `404` cross-org, `409` no LLM, `502` bad/failed draft. Match the
   existing internal-route documentation style.
3. **OpenAPI** — if the repo maintains a committed OpenAPI/Swagger doc for internal routes, add the endpoint
   there too (confirm whether FastAPI auto-generates vs a committed file; only edit if committed).
4. **`AI-TRACKING.md`** — mark the Asana "AI-drafted content" deferred-v2 item done (line ~60) and, if a Jira
   row lists it, likewise; add a short "Current AI Capabilities" row for AI-drafted issue/task content.
5. **`DEV-TRACKING.md`** — flip the Jira deferred-v2 "AI-drafted issue content" (`:197`) and Asana AI-drafted
   content items to shipped, with the branch/date.

## Validation
- Docs only; no tests. Proofread endpoint contract against the shipped route (statuses, body shape) so docs
  don't drift from `feedback_issue_draft.py`.

## Commit
`docs(issue-draft): SELF_HOSTING + API.md + tracking for AI-drafted issue/task content`

## Notes
- Do this **after** the code is merged/green so the documented contract is final.
- Honesty: describe it as an editable **draft** (human-in-the-loop), quality tracks the configured model —
  consistent with the OSS/BYOK brand.
