# Implementation Plan — docs-changelog-tracking (aspect 7, close-out)

**Date**: 2026-08-14
**Source**: `docs/planning/per-org-churn-model/docs-changelog-tracking/spec.md` + `prd.md`
**Branch**: `feat/per-org-churn-model`

## 1. Project setup checklist

- **Files** (docs only — no code):
  - `CHANGELOG.md` (Unreleased section)
  - `docs/SELF_HOSTING.md` (upgrade callout)
  - `AI-TRACKING.md` (M5.3 markers + gate caveat 542-553 + M5.2/M5.4 rows if needed)
  - `DEV-TRACKING.md` (FIXED markers for both registration bugs; the TRACKING.md note)
  - `services/worker-service/src/tasks/usage_metrics.py` NOTE already removed in aspect 1
    (verify with grep — no stale claim remains)
  - `docs/planning/per-org-churn-model/prd.md` (Decision Record: gate verdict,
    abort/continue outcome, aspect statuses)

## 2. Implementation phases

### Phase 1 — CHANGELOG + SELF_HOSTING

- **Steps**: CHANGELOG Unreleased entries in the repo's honest-limits voice: beat
  registration fix (churn calibration + classifier refit — "previously these beat tasks
  were never dispatched (NotRegistered); probabilities always came from the identity
  fallback"), the gate-study verdict (with the actual number and the "simulation is a
  bound" line), the churn classifier head (mode, shadow A/B, +0.02 promotion, rollback,
  heuristic fallback, honest limits).
  SELF_HOSTING: upgrade callout for `churn_classifier_mode`/`churn_autopromote_hold`
  settings, the readiness/gate number if it moved, the "no HF downloads — JSON artifacts
  only" note for the air-gapped crowd.
- **Validation**: `git grep -c "churn_classifier_mode" CHANGELOG.md docs/SELF_HOSTING.md` ≥ 1 each.
- **Commit**: `docs(changelog): M5.3 churn model + beat-registration fix; SELF_HOSTING callout`

### Phase 2 — AI-TRACKING + DEV-TRACKING + planning close-out

- **Steps**:
  1. AI-TRACKING: M5.3 status line (440) updated per the actual verdict — COMPLETE, or
     PARTIAL with the honest reason if the gate stayed unvalidated; the gate-caveat block
     (542-553) appended with the study's decision; M5.2/M5.4 rows untouched.
  2. DEV-TRACKING: FIXED markers for `churn-calibration-beat-notregistered` and
     `classifier-retrain-notregistered`; TRACKING.md item (a) closed ("handled in a
     dedicated fix pass" → done).
  3. `docs/planning/per-org-churn-model/prd.md` Decision Record: gate verdict + abort/
     continue outcome; each aspect's spec/plan marked with its shipped status.
- **Validation**: `git grep -rn "undecorated\|NotRegistered\|never run" services/worker-service/src/tasks/`
  → only the fixed code (no stale NOTE); AI-TRACKING M5.3 no longer reads "planned".
- **Commit**: `docs(tracking): M5.3 status + gate decision; FIXED markers for beat registration`

## 3. File-by-file build order

| # | File | Purpose |
|---|------|---------|
| 1 | `CHANGELOG.md` | Feature + fix entries |
| 2 | `docs/SELF_HOSTING.md` | Upgrade callout |
| 3 | `AI-TRACKING.md` | M5.3 status + gate caveat |
| 4 | `DEV-TRACKING.md` / `TRACKING.md` | FIXED markers |
| 5 | `docs/planning/per-org-churn-model/prd.md` | Decision record + statuses |

## 4. Testing strategy

- N/A (docs). Verification is grep + human review.

## 5. Environment & secrets

- None.

## 6. Edge cases & error handling

- If the gate verdict is "no defensible threshold", AI-TRACKING M5.3 gets PARTIAL + the
  honest sentence; do not mark COMPLETE against the facts.
- The `usage_metrics.py` NOTE was deleted in aspect 1 — verify, don't re-delete.

## 7. Agent execution notes

- Runs last, after every code aspect is merged on the branch. Requires the gate verdict
  (aspect 2) and the fixed registration state (aspect 1).
- No aspect depends on this one — it is the final commit sequence before PR.
