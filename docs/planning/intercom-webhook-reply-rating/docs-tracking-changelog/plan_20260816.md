# Implementation Plan — docs-tracking-changelog (aspect 4 of 4, close-out, runs last)

**Date**: 2026-08-16
**Feature**: `intercom-webhook-reply-rating` (prd.md R7 — docs & tracking are part of "done")
**Aspect**: `docs-tracking-changelog`
**Source**: `docs/planning/intercom-webhook-reply-rating/docs-tracking-changelog/spec.md` + `prd.md`
**Branch**: `feat/intercom-webhook-reply-rating` (worktree `.claude/worktrees/feat-intercom-webhook-reply`)
**Sequencing**: **LAST** — execute only after the three code aspects (`core-branch-dispatch`,
`webhook-enrich-module`, `golden-fixtures-route-pins`) have merged into the branch. This
plan's placeholders (merge sha, PR number, merge date) must be filled from the merged state
before any step runs, per the house rule ("correct the marker in the same commit"; fill-facts
follow-up commit precedents `5a9d547d`, `4b6284a9`, `d04f4579`).

## 0. Facts gathered (verified 2026-08-16, before writing)

| Fact | Value | Source |
|---|---|---|
| SELF_HOSTING dedup-inert line (flip target) | `docs/SELF_HOSTING.md:1956-1957` — "> - The webhook's `replied` / `rating.added` events are still dedup-inert today (flagged follow-up, not fixed) — the pull is what delivers replies and ratings." Inside the `> ### Honest limits` blockquote under `### Connecting Intercom with OAuth (legacy)` | read |
| SELF_HOSTING honest-limits block bounds | `docs/SELF_HOSTING.md:1932-1960` — `> ### Honest limits` → `> - No claim is made about analysis quality…`; the middle bullets (pull full-conversation :1936-1940, no-backfill :1941-1943, backlog/estimate :1944-1955) all stay accurate post-fix | read |
| Claims that become accurate (verify only) | `:1724-1725` "new conversations — including replies and the satisfaction rating — become feedback items"; `:1752` "Three conversation topics are handled"; `:1821-1822` + `:1907-1910` subscribe-all-three topic lists — all TRUE once replied/rating events enrich items; no edit needed | read |
| CHANGELOG insertion point | `## Unreleased` runs :8-556 (`## v1.0.0` at :557); top entry is the backlog-estimate entry at :10; **the #16 pull entry (:25-44) is live, not historical** | grep |
| CHANGELOG dedup-inert claim to correct | `CHANGELOG.md:43-44` — "*Honest limits:* … the webhook's `replied`/`rating.added` events remain dedup-inert (a flagged follow-up, not fixed here)" inside the `### Added — Intercom pull now ingests replies and ratings` entry (:25-44) | read |
| Correction-note precedents | `**Correction (2026-08-15):**` :383-386 (superseding the "via webhook only" honest-limits line) and :448-451 (superseding the "webhook-only — no polling fallback" claim) — house form: **append, don't rewrite** | read |
| DEV-TRACKING follow-up defect note | `DEV-TRACKING.md:518-522` — "**Follow-up (defect, not fixed):** the webhook reply/rating path is still inert — … See `docs/planning/intercom-pull-replies-and-ratings/`", inside the #16 SHIPPED bullet (:509-522) of the Deferred v2 block | read |
| DEV-TRACKING follow-up-resolution model | `DEV-TRACKING.md:629` — "**Follow-up `landing-intercom-entry-refresh` — DONE** (chore, merged …)" — the `**Follow-up … — DONE/FIXED**` inline pattern inside a shipped bullet | read |
| DEV-TRACKING deferred header | `DEV-TRACKING.md:504` — "### Deferred v2 — Intercom (opened 2026-08-01; 3 SHIPPED, 1 NOT STARTED)" — **count unchanged** (this feature is not a deferred bullet; it closes a follow-up inside the #16 bullet) | read |
| Planning pointers (no history editing) | `docs/planning/intercom-pull-replies-and-ratings/prd.md` — :166 (R5 risk row mitigation "NOT fixed in this card"), :183 (out-of-scope bullet "Fixing the latent webhook reply/rating path"), :214 (self-critique "separate card that should follow"); its `docs-tracking-changelog/spec.md` — :31-33 (flag-only note "not fixed here") and :37 (out-of-scope "separate defect") | read |
| Out-of-scope-flip house pattern | `intercom-pull-replies-and-ratings` plan Phase 3: strike the superseded bullet + `**SHIPPED …**` pointer (`~~…~~`), keeping sibling bullets untouched (ingestion prd.md:309-310 precedent) | read |
| AI-TRACKING live dedup-inert claim (spec gap) | `AI-TRACKING.md:66` — "The webhook `replied`/`rating.added` events remain dedup-inert (flagged follow-up, not fixed)." — added by the pull feature's Phase 4; a **live** doc, not named by this spec's in-scope list | read |
| Landing (flag-only) | `services/landing-web/lib/integrations.ts:170` — step 4 instructs "Subscribe conversation.user.created, conversation.user.replied and conversation.rating.added" — becomes **accurate** post-fix; no claim about webhook replies/ratings being dead anywhere on the landing surface | grep |
| Live-claim grep surface (pre-plan) | "dedup-inert"/"still inert": SELF_HOSTING:1956-1957, CHANGELOG:43-44, AI-TRACKING:66, DEV-TRACKING:518-522 (all targets of this plan); the rest are `docs/planning/**` archives (incl. the pull plan's drafts and this PRD/spec) — expected, not live | grep |
| Planning dir state | `docs/planning/intercom-webhook-reply-rating/` is **untracked** (`git status` → `??`); the three code aspects hold only `spec.md` so far; this aspect holds `spec.md` + this plan | git status |
| Merge-facts model | The #16 bullet already carries real facts (`merged 873a9c1, PR #16`); **this** feature's merge facts are unknown until merge — use `(merged <merge-sha>, PR <# pending>)` placeholders, filled post-merge per house rule (`5a9d547d` / `4b6284a9` / `d04f4579` precedents) | git log |

## 1. Project setup checklist

Docs-only aspect. No scaffolding, no dependencies, no env, no tests. Files touched (exactly five):

1. `docs/SELF_HOSTING.md` — honest-limits flip (:1956-1957) + honest-limit bullets (Phase 1)
2. `CHANGELOG.md` — new Added entry + correction note on :43-44 (Phase 2)
3. `docs/planning/intercom-pull-replies-and-ratings/prd.md` — webhook-path pointer flips :166/:183/:214 (Phase 3)
4. `docs/planning/intercom-pull-replies-and-ratings/docs-tracking-changelog/spec.md` — pointer flips :31-33/:37 (Phase 3)
5. `AI-TRACKING.md` — :66 dedup-inert sentence flip (Phase 4, flagged gap — see ambiguity 1)
6. `DEV-TRACKING.md` — follow-up note :518-522 → **FIXED** (Phase 5)

**Explicitly NOT edited:** `services/landing-web/lib/integrations.ts` (flag-only, verified
accurate post-fix), the `intercom-oauth-path-retirement` marker, the SELF_HOSTING claim lines
:1724-1725/:1752/:1821-1822/:1907-1910 (verified accurate — see ambiguity 3), the
Deferred-v2 header count, and any archived planning-doc body beyond the pointer lines above.

## 2. Implementation phases

### Phase 1 — SELF_HOSTING.md: honest-limits flip + verify the become-accurate claims

- **Goal:** the last live "dedup-inert" claim in SELF_HOSTING flips to the new reality with
  the honest limits; the claims that this fix makes accurate are verified and left alone.
- **Prerequisites:** all three code aspects merged (the feature exists to document).
- **File:** `docs/SELF_HOSTING.md`
- **Steps:**
  1. **Replace the dedup-inert line (:1956-1957)** with the new reality + honest limits
     (house voice; prd.md §Honest limits):
     ```
     > - For webhook-wired installs, the webhook's `replied` / `rating.added` events now
     >   enrich the conversation's item in **real time** (when the item exists): a reply
     >   is merged into the item's text and the rating stored on it — no longer waiting
     >   on the pull. The pull remains the guaranteed fallback for every install.
     > - Webhook enrichment applies only to conversations whose items already exist — no
     >   backfill (same rule as the pull); a reply on a conversation Rereflect never
     >   created stays unseen. Payloads without conversation parts fall back to a
     >   `GET /conversations/{id}` detail fetch.
     ```
     (Covers spec.md:14-16 and :20-21: real-time enrichment when the item exists, pull as
     fallback, existing-item-only, no backfill, fallback fetch.)
  2. **Verify, do not edit** the become-accurate claims: :1724-1725 (replies + rating now
     genuinely reach feedback items via the webhook path), :1752 ("Three conversation
     topics are handled" — all three now handled), :1821-1822/:1907-1910 (subscribe-all-three
     instructions now have real effect). Read them once; if any drifted into falsehood since
     this plan was written, adjust only that line and note it in the PR description.
  3. Keep intact: the middle honest-limits bullets (:1936-1955, pull full-conversation,
     no-backfill, backlog/estimate, drain mechanics) — all still accurate; the "No claim is
     made about analysis quality" closer (:1958-1960) — still true.
- **Validation:** `git grep -n "dedup-inert\|still inert" docs/SELF_HOSTING.md` → 0; re-read
  :1932-1960 end-to-end — the flipped pair reads coherently with the pull bullets above it.
- **Commit:** `docs(self-hosting): Intercom webhook replies/ratings now enrich items in real time — honest limits`

### Phase 2 — CHANGELOG: new Added entry + correction of the #16 entry's :43-44

- **Goal:** the release notes announce the webhook-path fix and stop the #16 entry's
  dedup-inert line from standing as current truth.
- **Prerequisites:** Phase 1 (separate commit — mirror of the pull plan's Phase 1/2 split).
- **File:** `CHANGELOG.md`
- **Steps:**
  1. Insert at the **top of `## Unreleased`** (above :10), newest-first per house convention:
     `### Added — Intercom webhook replies & ratings now enrich items in real time` with this
     body outline (house bullets; bold-led; *honest-limits* italic closer):
     ```
     - **The webhook path is no longer inert.** `conversation.user.replied` and
       `conversation.rating.added` deliveries used to be silently dropped (dedup'd
       against the conversation's created row — the #16 follow-up). They now enrich the
       conversation's existing feedback item in real time: a reply is merged into the
       item's text (idempotent by part id), the satisfaction rating lands on the item.
     - **Payload-first, with a fallback fetch.** Real payloads are conversation-wrapped
       and read payload-first; a payload without conversation parts falls back to a
       `GET /conversations/{id}` detail fetch.
     - **Re-analysis once, after commit.** Text-changing enrichments dispatch exactly one
       re-analysis; rating-only changes dispatch none.
     - **The pull stays the guaranteed fallback** — unchanged; the create path is
       untouched (webhook enrichment never creates items).
     - `*Honest limits:* webhook enrichment applies only to conversations whose items
       already exist (no backfill — same rule as the pull); payloads without parts fall
       back to a detail fetch; no analysis-quality claim beyond what the pull already
       makes.`
     ```
     Leave `<merge-sha>` / `<PR #>` markers out of the changelog entry (the changelog does
     not carry merge facts; DEV-TRACKING does).
  2. **Correction note on the #16 entry (:43-44)**, appended after :44 (end of that entry's
     honest-limits block), mirroring the `**Correction (2026-08-15):**` precedent (:383-386):
     ```
     **Correction (2026-08-16):** the honest-limits line above said the webhook's
     `replied`/`rating.added` events remain dedup-inert (a flagged follow-up). That
     follow-up is now shipped — see the Added entry above: webhook replied/rating
     deliveries enrich the conversation's existing item in real time (payload-first,
     existing-item-only, no backfill; the pull stays the fallback).
     ```
     Append-only — the entry is the 2026-08-15 record; the correction supersedes it without
     rewriting it (the house way, cf. :383-386).
- **Validation:** the new entry sits above :10; `git grep -n "dedup-inert" CHANGELOG.md` →
  only :43 (the superseded claim, now followed by a Correction note); the new entry's
  *Honest limits* names existing-item-only, no-backfill and the fallback fetch.
- **Commit:** `docs(changelog): Intercom webhook reply/rating enrichment entry + #16 dedup-inert correction`

### Phase 3 — Planning pointers: `intercom-pull-replies-and-ratings` prd + spec

- **Goal:** the archived "not fixed here" record points to the shipped card — no history
  editing (spec.md:28-30).
- **Files:** `docs/planning/intercom-pull-replies-and-ratings/prd.md`,
  `docs/planning/intercom-pull-replies-and-ratings/docs-tracking-changelog/spec.md`
- **Steps:**
  1. `prd.md:166` (R5 risk row) — append the pointer to the mitigation cell (cell stays the
     record; pointer flips its "not fixed" reading):
     ```
     | R5 | **Latent webhook reply/rating path** (… unchanged) | Flagged as a separate defect —
       NOT fixed in this card (scope discipline); the docs pass states the honest current state.
       **— SHIPPED 2026-08-16** by `intercom-webhook-reply-rating`: the webhook replied/rating
       path now enriches the conversation's existing item in real time (existing-item-only). |
     ```
  2. `prd.md:183` (out-of-scope bullet) — strike + SHIPPED pointer (the house out-of-scope
     flip pattern, cf. ingestion prd.md:309-310):
     ```
     - ~~**Fixing the latent webhook reply/rating path** (flagged as a separate defect; the
       docs pass states the honest current state instead of claiming webhook parity).~~
       — **SHIPPED 2026-08-16** (`intercom-webhook-reply-rating`): the webhook replied/rating
       path now enriches the conversation's existing item in real time (existing-item-only,
       no backfill; the pull stays the fallback). See
       `docs/planning/intercom-webhook-reply-rating/`.
     ```
     Sibling out-of-scope bullets (:181-182, :185-190) untouched.
  3. `prd.md:214` (self-critique 🟡) — append the pointer sentence:
     ```
     …the actual webhook defect is a separate card that should follow — **shipped
     2026-08-16** as `intercom-webhook-reply-rating`.
     ```
  4. `spec.md:31-33` (flag-only note) — append the pointer:
     ```
     - **Flag-only (no edit):** … not fixed here (prd.md out-of-scope). — **SHIPPED
       2026-08-16** as `intercom-webhook-reply-rating` (see that card's planning dir).
     ```
  5. `spec.md:37` (out-of-scope) — append the pointer:
     ```
     - Fixing the webhook reply/rating path (separate defect). — now shipped as
       `intercom-webhook-reply-rating` (2026-08-16).
     ```
- **Validation:** `git grep -n "intercom-webhook-reply-rating" docs/planning/intercom-pull-replies-and-ratings/prd.md` → 3
  (:166/:183/:214); same in its spec → 2 (:31-33/:37); no body text rewritten (only appended
  pointers + the one struck bullet).
- **Commit:** `docs(planning): intercom-pull-replies-and-ratings — webhook-path pointers to the shipped card`
  (the untracked `docs/planning/intercom-webhook-reply-rating/` dir — card/PRD/specs/this
  plan — lands in this feature's separate `docs(planning)` commit, mirroring the writeback
  precedent; do not mix it here).

### Phase 4 — AI-TRACKING.md:66 dedup-inert sentence flip (flagged gap)

- **Goal:** no live "dedup-inert" claim survives anywhere (spec acceptance criterion 1's
  intent; grep sweep below proves it). See ambiguity 1.
- **File:** `AI-TRACKING.md`
- **Steps:**
  1. Replace the sentence inside the Intercom row (:66): "The webhook `replied`/`rating.added`
     events remain dedup-inert (flagged follow-up, not fixed)." →
     ```
     **Webhook reply/rating enrichment shipped 2026-08-16**
     (`intercom-webhook-reply-rating`): the webhook `replied`/`rating.added` events now
     enrich the conversation's existing item in real time (payload-first parts + rating,
     detail-fetch fallback when the payload lacks parts; existing-item-only, no backfill;
     the pull stays the guaranteed fallback).
     ```
     Match the row's dense inline style; leave the pull-enrichment sentence it follows
     untouched.
- **Validation:** the row contains "no longer inert"/"now enrich" and no "dedup-inert";
  `git grep -n "intercom-webhook-reply-rating" AI-TRACKING.md` → 1.
- **Commit:** with Phase 5 (`docs(tracking): …`, see §7).

### Phase 5 — DEV-TRACKING: follow-up defect note (:518-522) → **FIXED**

- **Goal:** the #16 SHIPPED bullet's follow-up note reads FIXED with a shipped summary and
  merge-facts placeholders, filled post-merge per house rule (spec.md:25-27).
- **Prerequisites:** code aspects merged; merge sha / PR number known (or placeholders).
- **File:** `DEV-TRACKING.md`
- **Steps:**
  1. Replace the `**Follow-up (defect, not fixed):**` note (:518-522) inside the #16 SHIPPED
     bullet, using the `**Follow-up … — FIXED**` inline resolution pattern (cf. :629):
     ```
     **Follow-up (defect) — FIXED** via `feat/intercom-webhook-reply-rating` (merged
     <merge-sha>, PR <# pending>): the webhook reply/rating path is no longer inert — a
     `conversation.user.replied` / `conversation.rating.added` delivery now enriches the
     conversation's existing item in real time (payload-first parts + rating; detail-fetch
     fallback when the payload lacks parts; `enriched` event-log status so the created
     path's dedup is untouched; re-analysis dispatched once after commit for text changes,
     rating-only changes dispatch none). Existing-item-only — no backfill; the pull stays
     the guaranteed fallback. See `docs/planning/intercom-webhook-reply-rating/`.
     ```
  2. The Deferred-v2 header count (:504, "3 SHIPPED, 1 NOT STARTED") is **unchanged** — this
     feature closes a follow-up inside the #16 bullet, not a deferred-v2 bullet.
  3. **Post-merge fill (follow-up commit, do not amend):** once the PR merges, replace
     `<merge-sha>` / `<PR # pending>` with the real facts in a
     `docs(tracking): fill merge facts for intercom-webhook-reply-rating (PR #<N>, <sha>)`
     commit — the `5a9d547d` / `4b6284a9` / `d04f4579` precedent.
- **Validation:** `git grep -n "FIXED" DEV-TRACKING.md` → the follow-up line reads
  `**Follow-up (defect) — FIXED**`; no placeholders survive post-merge; the rest of the #16
  bullet and the sibling deferred bullets (:523-542) unchanged.
- **Commit:** `docs(tracking): intercom-webhook-reply-rating follow-up FIXED + AI-TRACKING dedup-inert flip`

### Phase 6 — Grep sweep (final gate) + landing flag-only check

- **Goal:** prove the spec's acceptance criteria 1-4 — no surviving live "dedup-inert"/"still
  inert" claims, honest limits stated, follow-up reads FIXED, no landing claims affected.
- **Steps** (run from the repo root; re-locate targets by text if line numbers shifted):
  ```bash
  git grep -n "dedup-inert\|still inert" docs/SELF_HOSTING.md CHANGELOG.md AI-TRACKING.md DEV-TRACKING.md  # → 0
  git grep -n "dedup-inert\|still inert" docs/    # → only docs/planning/** archives (pull plan drafts, this PRD/spec)
  git grep -n "remains dedup-inert\|remain dedup-inert\|still dedup-inert" . --exclude-dir=.git   # → 0 outside planning archives
  git grep -n "real time" docs/SELF_HOSTING.md CHANGELOG.md AI-TRACKING.md  # → the flipped/enriched lines present
  git grep -n "existing-item-only\|no backfill" docs/SELF_HOSTING.md CHANGELOG.md  # → honest limits present
  git grep -n "intercom-webhook-reply-rating" docs/planning/intercom-pull-replies-and-ratings/  # → pointer lines (3 prd + 2 spec)
  # Landing (flag-only — spec.md:35): verify the subscribe-all-three instruction is accurate, no edit:
  git grep -n "conversation.user.replied\|rating.added" services/landing-web/lib/integrations.ts  # → :170 (now-accurate instruction)
  ```
  Remaining "dedup-inert"/"still inert" hits **outside** the live targets are expected:
  `docs/planning/**` (archived record, incl. the pull feature's own plan drafts and this
  PRD/spec problem statements). Do not edit.
- **Human review pass:** re-read `docs/SELF_HOSTING.md:1722-1960` end-to-end — intro,
  shipped-scope list, webhook sections, honest-limits block must read coherently; then the
  new CHANGELOG entry + the #16 correction note, and the DEV-TRACKING FIXED follow-up.

## 3. File-by-file build order

| # | File | Edit type | Purpose |
|---|------|-----------|---------|
| 1 | `docs/SELF_HOSTING.md` | Modify | honest-limits flip (:1956-1957) + honest-limit bullets; verify-only on :1724-1725/:1752/:1821-1822/:1907-1910 (Phase 1) |
| 2 | `CHANGELOG.md` | Modify | `### Added — Intercom webhook replies & ratings now enrich items in real time` at top of Unreleased + Correction note on :43-44 (Phase 2) |
| 3 | `docs/planning/intercom-pull-replies-and-ratings/prd.md` | Modify | webhook-path pointer flips :166/:183/:214 (Phase 3) |
| 4 | `docs/planning/intercom-pull-replies-and-ratings/docs-tracking-changelog/spec.md` | Modify | pointer flips :31-33/:37 (Phase 3) |
| 5 | `AI-TRACKING.md` | Modify | :66 dedup-inert sentence → enrichment flip (Phase 4, flagged gap) |
| 6 | `DEV-TRACKING.md` | Modify | :518-522 follow-up → **FIXED** + shipped summary + merge-facts placeholders (Phase 5) |

## 4. Testing strategy

- **No new tests.** Docs-only; no code, no migration, no comment in a code file — so no
  backend/worker suite rerun is required.
- **Verification is the Phase 6 grep suite** plus the human re-read of the Intercom section,
  the two CHANGELOG entries and the FIXED follow-up.
- Note for the eventual FIXED-block (if any future aspect adds suite counts): none are added
  here — this is a docs aspect with no test delta.

## 5. Environment & secrets

- None. No env vars, no running stack, no database. All edits are markdown in tracked files.

## 6. Edge cases & error handling

- **Merge facts unavailable** (this aspect runs last but before the final PR merges): keep
  `<merge-sha>` / `<PR # pending>` placeholders in the FIXED follow-up and land the fill-facts
  follow-up commit after merge (the `5a9d547d` / `4b6284a9` / `d04f4579` precedent). Do **not**
  invent facts.
- **A code aspect has not merged** (sibling dirs still spec-only, or PRs open): halt, surface
  to the lead, do not mark FIXED — the docs pass describes what shipped.
- **CHANGELOG has newer entries above :10 by execution time**: insert at the actual top of
  Unreleased, not at a line number.
- **SELF_HOSTING section reflowed**: re-locate by text ("still dedup-inert", "Honest limits",
  "Three conversation topics are handled"), not by line number.
- **Correction-note date**: use the actual merge date, not a hardcoded 2026-08-16.
- **Reviewer objects to the AI-TRACKING flip (ambiguity 1)**: drop it and note the drop in the
  PR description — it is included on the no-live-false-claims lean, not on a spec mandate.
- **Post-merge fill lands after another change to the same bullet**: re-locate by text.

## 7. Agent execution notes

- Runs last. Two human checkpoints: (1) before Phase 1 — confirm every code aspect is merged
  on the branch, fill the merge-facts table; (2) before the Phase 5 commit — a reviewer signs
  off the FIXED follow-up's summary against the actual diff.
- Commit sequence (house style — do not amend; push and open the PR per house workflow):
  1. `docs(self-hosting): Intercom webhook replies/ratings now enrich items in real time — honest limits` (Phase 1)
  2. `docs(changelog): Intercom webhook reply/rating enrichment entry + #16 dedup-inert correction` (Phase 2)
  3. `docs(planning): intercom-pull-replies-and-ratings — webhook-path pointers to the shipped card` (Phase 3)
  4. `docs(tracking): intercom-webhook-reply-rating follow-up FIXED + AI-TRACKING dedup-inert flip` (Phases 4-5)
  5. Post-merge: `docs(tracking): fill merge facts for intercom-webhook-reply-rating (PR #<N>, <sha>)`
  (Phases 1+2 may be combined into one `docs(self-hosting): … + CHANGELOG` commit if the
  executor prefers — the writeback precedent did exactly that.)
- The landing check lives only in the Phase 6 verification output / PR description — no
  landing edit (spec.md:35).
- The `intercom-oauth-path-retirement` marker stays untouched (spec.md out-of-scope).
- The untracked `docs/planning/intercom-webhook-reply-rating/` dir lands in this feature's
  separate `docs(planning)` commit (card + PRD + specs + this plan), not in the Phase 3
  planning commit which only touches the pull feature's docs.

## 8. Spec ambiguities flagged

1. **AI-TRACKING.md:66 carries a live "dedup-inert" claim the spec does not name.** The
   pull feature's Phase 4 added "The webhook `replied`/`rating.added` events remain
   dedup-inert (flagged follow-up, not fixed)." to the Intercom row — a **live** doc, while
   spec.md in-scope lists only SELF_HOSTING + CHANGELOG + DEV-TRACKING + the pull planning
   docs. The spec's own acceptance-criterion grep (no "dedup-inert"/"still inert" in live
   docs) would surface it. **Lean: flip it** in Phase 4, on the no-live-false-claims lean
   (same class as the pull plan's ambiguity #1). Droppable if reviewers object.
2. **DEV-TRACKING header count (:504) unaffected** — this feature closes a follow-up inside
   the #16 bullet, not a deferred-v2 bullet, so "3 SHIPPED, 1 NOT STARTED" stays. Confirmed
   against spec.md:25-27 (only the follow-up note flips). If a reviewer wants a deferred-v2
   presence for visibility, that is a new SHIPPED bullet — out of spec scope; note, don't add.
3. **SELF_HOSTING claim lines (:1724-1725/:1752/:1821-1822/:1907-1910)** — spec.md:17-19 says
   "verify the phrasing needs no change beyond the honest-limits block." With the fix all
   four are genuinely accurate (replies/ratings reach items, three topics handled, subscribing
   all three has real effect). **Decided: verify-only, no edit**; the intro "including replies
   and the satisfaction rating — become feedback items" is a soft shorthand (they enrich
   items, not become separate ones) but it predates both features and is not false in the
   merge-into-item sense — leaving it matches "adjust only what's false."
4. **Correction mechanics on CHANGELOG :43-44** — "flip" vs "append correction note". The #16
   entry is in Unreleased (live), so a direct edit is legal; **chosen: append-only** (the
   :383-386 / :448-451 precedents, and the entry is the 2026-08-15 record — a correction
   preserves it). The new Added entry carries the live claims.
5. **"FIXED" vs "SHIPPED" marker for the DEV-TRACKING follow-up** — spec.md:25-27 says
   "→ **FIXED**"; the deferred-bullet SHIPPED style (:512-516) is for deferred bullets, not
   follow-ups; the `**Follow-up … — DONE**` precedent (:629) is the resolution pattern.
   **Chosen: `**Follow-up (defect) — FIXED**`** per spec, with the shipped summary and
   placeholders.
6. **SHIPPED/FIXED date** — hardcoded as 2026-08-16 in this plan's drafts; use the actual
   merge date (house rule, cf. the writeback and pull bullets which used the real date).
