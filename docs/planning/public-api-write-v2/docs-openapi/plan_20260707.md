# Implementation Plan — docs-openapi (2026-07-07)

**Feature:** public-api-write-v2 · **Aspect:** `docs-openapi` · **Service:** `services/backend-api` (docs) + repo-root tracking · **Migrations:** none

## 0. Impact

- Docs only — no behavior change, no new tests (an OpenAPI snapshot assertion is optional).
- Runs **after** `patch-tags-urgent` + `public-delete-feedback` are green, so docs match shipped code.

## Phase 1 — OpenAPI / route docstrings

- **File:** `services/backend-api/src/api/routes/public_api.py`
  1. `:8-12` module scope table → `write` line: "PATCH (workflow status, corrections, tags, is_urgent) + DELETE feedback".
  2. `:753-758` `info.description` → update the `write` clause to mention tags/is_urgent edits + DELETE.
  3. PATCH `description=` (`:334-339`) → add tags/is_urgent (if not already done in `patch-tags-urgent`).
  4. DELETE route `summary`/`description` (added in that aspect) → confirm present and accurate.
- **Validation:** start API, `GET /api/public/v1/openapi.json` (or the docs route in `public_openapi()`) reflects the new fields + DELETE verb. Optional: a test asserting the DELETE path + PATCH `tags` field appear in the generated schema.

## Phase 2 — `docs/API.md`

- **File:** `docs/API.md`
  - Under the public write section, document:
    - PATCH new fields: `tags` (replace; `[]` clears, omit unchanged; ≤20 tags, ≤50 chars each, trimmed+deduped), `is_urgent` (bool).
    - `extra="forbid"` → unknown fields 422.
    - Non-atomicity note (combined PATCH best-effort).
    - `DELETE /api/public/v1/feedback/{id}` → 204, `write` scope, org-scoped 404, example curl.

## Phase 3 — Tracking + changelog

- **File:** `AI-TRACKING.md`
  - `:288` write-scope bullet → append tags/is_urgent/DELETE shipped 2026-07-07; move them out of the "Deferred (v2)" list.
- Changelog: add an entry if a changelog file/flow exists (check `BLOG-TRACKING.md`/`DEV-TRACKING.md` conventions); otherwise note in `DEV-TRACKING.md`.
- **Commit:** `docs(public-api-write): document tags/is_urgent edits + DELETE (API.md, OpenAPI, tracking)`

## Agent execution notes

- Small; the integrator may fold Phases 1 & 2 into the two code aspects' commits rather than a separate agent.
- Keep doc examples numerically consistent with the actual validators (20 / 50).
