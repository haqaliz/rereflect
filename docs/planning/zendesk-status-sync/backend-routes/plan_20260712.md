# Plan — backend-routes (2026-07-12)

**Aspect:** `backend-routes` · **Spec:** `./spec.md` · **PRD:** `../prd.md`
**Sequencing:** after reconcile-core-and-model. Parallel with poll/webhook. Blocks frontend.
**Strict TDD.** Blueprint: `services/backend-api/src/api/routes/jira_integration.py` L546 (toggle), L595 (sync).

## Service touched
- `services/backend-api` (`src/api/routes/zendesk_integration.py`).

## Phase 1 — Toggle + validation

**File:** `services/backend-api/src/api/routes/zendesk_integration.py`
- Add request schema `ZendeskStatusSyncUpdateRequest(BaseModel)`: `enabled: bool`, `status_mapping: Optional[Dict[str, str]] = None`.
- Add `_validate_status_mapping(mapping)` — keys ∈ `ZENDESK_STATUSES` (import from `src.services.zendesk_status_core`), values ∈ `VALID_STATUSES` (from `status_sync_core`); raise 422 otherwise.
- `PATCH /status-sync` (`dependencies=[Depends(require_admin_or_owner)]`): load org integration (404 if none); validate mapping; set `row.status_sync_enabled` (+ optional `row.status_mapping`); commit; return `ZendeskStatusResponse`.

**Tests (RED):** `services/backend-api/tests/test_zendesk_status_sync_routes.py` (TestClient)
- `{enabled:true}` → 200 + persisted; mapping `{"solved":"resolved"}` persisted; `{"bogus":"resolved"}`→422; `{"solved":"bogus"}`→422; no integration→404; member role→403.

**Commit:** `feat(zendesk-status-sync): PATCH /status-sync toggle + mapping validation`

## Phase 2 — Manual trigger

- `POST /status-sync/sync` (`require_admin_or_owner`, `status_code=202`, `response_model=…{status:str}`): require active integration (400 if none); `_get_celery_app().send_task("src.tasks.zendesk_status_sync.sync_zendesk_status_org", args=[integ.id])`; broker failure → 502 (never 500). Distinct sub-path from the existing ingestion `POST /sync`.

**Tests (RED):**
- active org → 202 `{status:"queued"}`, `send_task` called with the status task name + `[id]`; no active integration → 400; broker raises → 502; member → 403.

**Commit:** `feat(zendesk-status-sync): POST /status-sync/sync manual trigger (202/502)`

## Phase 3 — Status response fields

- Extend `ZendeskStatusResponse` + its builder (`GET /status`) with `status_sync_enabled`, `status_mapping`, `last_status_synced_at`, `last_status_sync_error`. Leave ingestion fields (`last_synced_at`/`last_sync_status`/`last_error`) untouched.

**Tests (RED):** `GET /status` returns the 4 new fields; ingestion fields unchanged.
**Validation:** `pytest tests/test_zendesk_status_sync_routes.py -v`.
**Commit:** `feat(zendesk-status-sync): expose status-sync fields on GET /status`

## Edge cases / notes
- Task-name string couples to poll-task; dispatch is by string so this can be written ahead of the worker task.
- Reuse `_get_active_integration` + `_get_celery_app` helpers already in the route module (dig confirmed present).

## Agent execution notes
- Mirror the exact Jira route error taxonomy (404/400/422/502) so the frontend clone behaves identically.
