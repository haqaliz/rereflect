# Plan — frontend (2026-07-12)

**Aspect:** `frontend` · **Spec:** `./spec.md` · **PRD:** `../prd.md`
**Sequencing:** LAST — after backend-routes.
**Strict TDD (Vitest + RTL).** Blueprint: `components/settings/JiraStatusSyncCard.tsx` (+ its tests).

## Service touched
- `services/frontend-web`.

## Phase 1 — API client + types

**File:** `services/frontend-web/lib/api/zendesk.ts`
- Extend `ZendeskConnectionStatus`: `status_sync_enabled: boolean`, `status_mapping: Record<string,string> | null`, `last_status_synced_at: string | null`, `last_status_sync_error: string | null`.
- Add module-scope exports (mirror `jira.ts` L151-165):
  - `patchZendeskStatusSync(enabled: boolean, statusMapping?: Record<string,string>)` → `apiClient.patch('/api/v1/integrations/zendesk/status-sync', { enabled, ...(statusMapping ? { status_mapping: statusMapping } : {}) })`.
  - `triggerZendeskStatusSync()` → `apiClient.post('/api/v1/integrations/zendesk/status-sync/sync')`.

**Tests (RED):** `lib/api/__tests__/zendesk.test.ts` (mirror `jira.test.ts:155-194`)
- `patchZendeskStatusSync(true)` PATCHes the right URL with `{enabled:true}`; with mapping includes `status_mapping`.
- `triggerZendeskStatusSync()` POSTs the status-sync path.

**Commit:** `feat(zendesk-status-sync): frontend api client + types for status-sync`

## Phase 2 — ZendeskStatusSyncCard

**File (new):** `services/frontend-web/components/settings/ZendeskStatusSyncCard.tsx` — clone `JiraStatusSyncCard.tsx`:
- Props `{ status: ZendeskConnectionStatus, onStatusChange }`; null if `!status.connected`.
- Toggle: optimistic flip → `patchZendeskStatusSync(checked)`; revert + `toast.error(err?.response?.data?.detail)` on failure.
- "Sync Now": `triggerZendeskStatusSync()` → `toast.success`; 502 → "background worker is unavailable" message.
- Indicator: `Last synced {last_status_synced_at ? timeAgo(...) : 'Never'}`; error line when `last_status_sync_error`.
- Use CSS variables / existing UI primitives (Switch, Button, Loader2/RefreshCw) — no hardcoded colors.

**Tests (RED):** `components/settings/__tests__/ZendeskStatusSyncCard.test.tsx` (mirror Jira card test)
- null when disconnected; switch reflects `status_sync_enabled`; toggle calls `patchZendeskStatusSync(true)`, reverts + toast on reject; "Last synced Never" vs relative; error line only when present; Sync-now 202 success toast / 502 error toast.

**Commit:** `feat(zendesk-status-sync): ZendeskStatusSyncCard (toggle + indicator + Sync Now)`

## Phase 3 — Mount on the Zendesk page

**File:** `services/frontend-web/app/(dashboard)/settings/integrations/zendesk/page.tsx`
- Import + mount `<ZendeskStatusSyncCard status={status} onStatusChange={setStatus} />` after the connection card (~L505), gated `{status?.connected && isAdminOrOwner && (...)}` (mirror jira/page.tsx L409-412).
- Seed the 4 new fields in `handleConnect`'s `setStatus({...})` (mirror jira/page.tsx L109-110).
- Keep the EXISTING ingestion "Sync Now" (`handleSync`/`triggerSync`); relabel it "Sync tickets" and the card's button stays "Sync Now" (status) — distinct labels (spec/critique gap #3).

**Tests (RED):** update `app/(dashboard)/settings/integrations/zendesk/__tests__/ZendeskPage.test.tsx`
- add the 4 fields to `connectedStatus`/`disconnectedStatus` fixtures + the `zendeskAPI` mock; assert the card renders for admin/owner + connected, and the existing ingestion Sync-now test still passes.

**Validation:** `npm run test` + `npm run lint` in `services/frontend-web` — both green.
**Commit:** `feat(zendesk-status-sync): mount status-sync card on Zendesk settings page`

## Edge cases / notes
- Two sync buttons on one tile — label clearly ("Sync tickets" = ingestion, "Sync Now" in the status card = status). Add a one-line helper caption on the status card.
- Mapping override is API-only for now (no editor UI) — the toggle sends `enabled` only.

## Agent execution notes
- Read `JiraStatusSyncCard.tsx` + its test verbatim first; the clone should differ only in module names, endpoint paths, and field names (`last_status_synced_at`/`last_status_sync_error`).
