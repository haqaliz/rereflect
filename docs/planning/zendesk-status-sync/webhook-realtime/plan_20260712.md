# Plan — webhook-realtime (2026-07-12)

**Aspect:** `webhook-realtime` · **Spec:** `./spec.md` · **PRD:** `../prd.md`
**Sequencing:** after reconcile-core-and-model; land after/with poll-task (reuse reconcile shape).
**Strict TDD.** ⚠️ Contains the PRD's R5 unknown (event shape) — Phase 0 spike is mandatory.

## Service touched
- `services/backend-api` (the existing Zendesk webhook receiver route + backend reconcile port).

## Phase 0 — Spike: map the current receiver + confirm event shape (READ-ONLY, no code)

**Goal:** de-risk gap #1 before writing code.
1. Read the existing `POST /api/v1/webhooks/zendesk/events` handler: how it verifies HMAC, how it parses the body, what event type(s) it currently branches on (dig: ingestion `ticket.created` path), and how it resolves the org/integration (subdomain? secret? header?).
2. Determine the exact event type + payload Zendesk sends for a status change (`ticket.updated` / a webhook "event" object carrying `ticket.id` + `status`, vs a legacy trigger-based webhook). Document what fields are reliably present.
3. **Decision:** if operators must reconfigure their Zendesk webhook/trigger to emit update events, capture the exact setup steps for `docs/SELF_HOSTING.md` and confirm the **poll remains the guaranteed path** so webhook is purely additive.
4. Write findings into this aspect dir as `dig.md`. If the event shape can't be pinned confidently, STOP and flag — ship poll-first, defer webhook (per critique).

**Checkpoint:** human review of `dig.md` before Phase 1.

## Phase 1 — Backend reconcile port

**Goal:** backend-side reconcile that reuses backend `zendesk_status_core` + `workflow_service.apply_status_change`.
**Files:**
- Use `services/backend-api/src/services/zendesk_status_core.py` (from reconcile aspect).
- Add `services/backend-api/src/services/zendesk_status_reconcile.py`:
  - `reconcile_ticket(db, integ, ticket_id, fetched_status) -> str` — look up feedback (`source='zendesk'`, `source_external_id=ticket_id`, org-scoped); read `FeedbackZendeskSync`; `decide_update`; on `changed` → `resolve_target_status` → apply via `apply_status_change([feedback], target, organization_id=…, actor_id=None, actor_label="zendesk")` **with `source=zendesk` metadata** and **without** `dispatch_status_webhooks` (outbound deferred). Upsert sidecar on seed+changed. Use the SAME conditional-update guard idea (apply_status_change is no-op if equal; add a guard if needed for the race).

**Tests (RED):** `services/backend-api/tests/test_zendesk_status_reconcile.py`
- seed / noop / changed(solved→resolved) with sidecar effects + one event; unknown-status→None no apply; no linked feedback → no-op.

**Commit:** `feat(zendesk-status-sync): backend reconcile port (shared mapping + apply, source-tagged)`

## Phase 2 — Extend the webhook receiver

**Files:** the Zendesk webhook route module (from Phase 0).
- Keep HMAC verify + fail-closed on missing secret UNCHANGED. Keep the `ticket.created` ingestion branch UNCHANGED (characterization test).
- Add a status-change branch (per Phase 0 event type): resolve integration/org → gate on `is_active AND status_sync_enabled` (if off → ACK 200, ignore) → extract `ticket_id` + `status` → `reconcile_ticket(...)` → ACK 200.
- Never 500 on a well-formed verified event; malformed → 400 as today.

**Tests (RED):** extend the webhook route tests:
- verified `status=solved` event, sync ON, prior sidecar `open` → feedback `resolved`, one event, sidecar updated, 200.
- sync OFF → 200, no change, no event.
- first observation via webhook → seed, no apply, 200.
- event for unlinked ticket → 200 no-op.
- bad/missing HMAC → rejected as today; ingestion `ticket.created` unchanged (characterization).
- **idempotency:** webhook then poll (or vice versa) on same change → single event total (conditional-update guard + sidecar).

**Commit:** `feat(zendesk-status-sync): real-time status reconcile on Zendesk ticket-update webhook`

## Phase 3 — Docs
- Update `docs/SELF_HOSTING.md` + landing copy: how to enable status-sync, the webhook trigger setup from Phase 0, and that poll is the fallback. (Promoted to must-have per critique.)
**Commit:** `docs(zendesk-status-sync): status-sync + webhook trigger setup (SELF_HOSTING + landing)`

## Edge cases / notes
- Do NOT let the status branch reach the ingestion/dedup path (distinct branch by event type) — prevents spoofed ingestion.
- Cross-service parity: backend reconcile must match the worker `reconcile_feedback` behavior exactly (same mapping module values, same seed/change-gate). A shared characterization fixture (same inputs → same action/target) keeps them aligned.

## Agent execution notes / checkpoints
- **Hard checkpoint after Phase 0** — do not write webhook code until the event shape is confirmed in `dig.md`. If unconfirmable, ship poll-first and defer this aspect.
