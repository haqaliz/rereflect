# Implementation Plan: channel-preference

**Date:** 2026-09-02 · **Aspect spec:** `docs/planning/teams-notifications/channel-preference/spec.md`
**Services:** `services/backend-api` (migration + model + API), `services/worker-service` (mirror), `services/frontend-web` (toggle UI).

## 1. Setup checklist

- **One new Alembic migration** in `services/backend-api/alembic/versions/` — `channel_teams` Boolean NOT NULL default True on `user_alert_preferences`.
- Baseline: `alembic heads` prints **exactly one** head (CI asserts this); the current head per recent history is the `playbook_tasks_table` revision (`0bf182dad44d` — verify with `alembic heads` before writing the migration and chain off whatever is the single head).
- Baseline suites: backend, worker, frontend green.
- Precedent to mirror exactly: the `channel_discord` column + round-trip sentinel landed by `discord-channel-preferences` (2026-08-09).

## 2. Implementation phases

### Phase 1: Migration + backend model (TDD)

- **Goal:** `UserAlertPreference.channel_teams` exists, defaults true, existing rows backfill true.
- **Files:**
  - New: `services/backend-api/alembic/versions/<rev>_add_channel_teams.py`
  - `services/backend-api/src/models/user_alert_preference.py` (:5-17 shape; add `channel_teams = Column(Boolean, default=True, nullable=False)` beside `channel_discord` :17)
- **Steps:**
  1. RED: model test asserting the column exists with default True.
  2. `alembic revision -m "add channel_teams to user alert preferences"`; edit: `op.add_column("user_alert_preferences", sa.Column("channel_teams", sa.Boolean(), nullable=False, server_default=sa.true()))`; keep `server_default` (backfill) — do **not** drop it after (matches the discord-column migration; verify that one's shape first with `grep -rl "channel_discord" alembic/versions/`).
  3. `alembic upgrade head`; GREEN.
- **Validation:** `alembic heads` (one head) + `alembic upgrade head` + model test
- **Commit:** `feat(teams): user alert preference channel_teams column + migration (TDD)`

### Phase 2: Notifications API round-trip (TDD)

- **Goal:** the preferences API exposes `channel_teams` with the `None = unchanged` sentinel so a stale client PUT can't flip Teams off.
- **Files:** `services/backend-api/src/api/routes/notifications.py`
- **Steps:**
  1. RED: extend the notifications API tests — GET returns `channel_teams: true` by default; PUT with `channel_teams: false` persists; PUT with `channel_teams: null` leaves the stored value unchanged; PUT without the key unchanged.
  2. Mirror the `channel_discord` handling exactly:
     - Request schema field `channel_teams: Optional[bool] = None` (:106 shape).
     - Response/detail schema `channel_teams: bool = True` (:89 shape).
     - Serialization `channel_teams=p.channel_teams` (:363 shape).
     - Default-construction `channel_teams=True` (:378 shape).
     - Apply loop: `if item.channel_teams is not None: pref.channel_teams = item.channel_teams` (:406-407 shape).
     - Any "defaults for missing" site (:419 shape) gains the field.
  3. GREEN; full backend suite.
- **Validation:** `pytest tests/ -v` (backend)
- **Commit:** `feat(teams): notifications API channel_teams round-trip with sentinel (TDD)`

### Phase 3: Worker mirror column (TDD)

- **Goal:** the worker `UserAlertPreference` mirror gains the column — the drift class this repo has been bitten by twice (P5 note: "worker mirror also gained the previously-missing `channel_intercom` column (drift closed)").
- **Files:** `services/worker-service/src/models/__init__.py` (:390-408 shape; add `channel_teams = Column(Boolean, default=True, nullable=False)` beside `channel_discord` :402)
- **Steps:**
  1. RED: a drift-pin test — instantiate the worker mirror and assert `channel_teams` attribute exists (mirror whatever pins `channel_discord` in the worker tests: `grep -rl "channel_discord" services/worker-service/tests/`).
  2. Add the column.
  3. GREEN; full worker suite.
- **Validation:** `pytest tests/ -v` (worker)
- **Commit:** `feat(teams): worker UserAlertPreference mirror gains channel_teams (TDD)`

### Phase 4: Frontend toggle (TDD)

- **Goal:** Settings → Notifications shows a Teams toggle per alert type, default on, saving `channel_teams`.
- **Files:**
  - `services/frontend-web/lib/api/notifications.ts` — `AlertPreference` interface (:21-32) gains `channel_teams: boolean`.
  - `services/frontend-web/app/(dashboard)/settings/notifications/page.tsx` — `DEFAULT_PREFERENCES` (:83-92) gains `channel_teams: true`; `CHANNEL_CONFIG` (:120-126) gains a Teams entry (key `teams`, label "Teams"); `renderChannelIcon` (:276-287) handles `teams` (TeamsIcon when available, else a generic icon); customize-dialog channel list (:452-471) renders the row.
- **Steps:**
  1. RED: extend `__tests__/settings/NotificationsSettingsPage.test.tsx` — defaults include `channel_teams: true` (:75-91 shape); save payload includes `channel_teams` (:317-337 shape); toggling Teams off sends `channel_teams: false`.
  2. Implement.
  3. GREEN: `npm run test && npm run lint`.
- **Validation:** `npm run test && npm run lint`
- **Commit:** `feat(teams): settings notifications channel_teams toggle (TDD)`

## 3. File-by-file build order

| File | Purpose |
|---|---|
| `services/backend-api/alembic/versions/<rev>_add_channel_teams.py` | migration, server_default true |
| `services/backend-api/src/models/user_alert_preference.py` | column |
| `services/backend-api/src/api/routes/notifications.py` | request/response schema + sentinel apply |
| `services/worker-service/src/models/__init__.py` | mirror column |
| `services/frontend-web/lib/api/notifications.ts` | interface field |
| `services/frontend-web/app/(dashboard)/settings/notifications/page.tsx` | toggle row + config + icon |

## 4. Testing strategy

- Backend: API round-trip incl. sentinel; migration up/down.
- Worker: mirror drift-pin.
- Frontend: defaults + save payload + toggle.
- Suites per service as above.

## 5. Environment & secrets

- None new. Postgres for migration + backend suite.

## 6. Edge cases

- Existing rows → `server_default` true backfill (no code path may read a None).
- Stale client PUT without the key → unchanged (sentinel).
- Toggle off → `worker-dispatch` gating must honor it — this aspect owns the column; `worker-dispatch` Phase 2/3 reads it. Land this aspect **before** `worker-dispatch`'s gating reads (or the `getattr` fallback in `worker-dispatch` covers the gap).

## 7. Agent execution notes

- `alembic heads` must be one before and after. If the current head is not `0bf182dad44d`, chain off whatever `alembic heads` reports.
- Mirror the `channel_discord` migration file exactly (find it first) — including whether it used `server_default` + `batch_alter_table`.
- Checkpoint after Phase 1.