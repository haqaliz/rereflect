# Implementation Plan: docs-landing

**Date:** 2026-09-02 · **Aspect spec:** `docs/planning/teams-notifications/docs-landing/spec.md`
**Services:** `services/landing-web` + repo docs. No production code.

## 1. Setup checklist

- No deps, no env, no migration.
- Do this aspect **last** — it documents behavior the other six aspects shipped. Verify each claim against the code as merged on this branch, not the spec prose.

## 2. Implementation phases

### Phase 1: Landing integration entry (verify-first)

- **Files:** `services/landing-web/lib/integrations.ts`
- **Steps:**
  1. Copy the Slack entry (:96-134) as the shape template; write the Teams entry with:
     - `slug: 'teams'`, `status: 'available'` (do NOT use `'coming_soon'` — nothing may claim "coming soon" for Teams after this PR).
     - `color`/`gradient`/`hoverBorder`/`hoverShadow` from the Microsoft brand (`#6264A7` — the frontend tile uses the same value).
     - `heroMessage`, `howItWorks` (connect a webhook URL), `features` (health-drop alerts, automation notifications, playbook notify, per-user toggle), `useCases`, `faqs` (reuse `SHARED_FAQS` like Slack :126), `setupSteps` (create an Incoming Webhook in Teams → paste URL → Test) — honest limits in the copy: webhook-only, no OAuth, MessageCard format.
  2. Verify `getAvailableIntegrations()` (:448-450) picks it up automatically — no registry edit.
  3. If the landing page has a notification-provider sentence anywhere else (grep `Slack` in `services/landing-web/app/` + `components/landing/`), update it to name Teams.
- **Validation:** `pnpm build` in `services/landing-web` (or the repo-root workspace build); grep confirms no remaining "coming soon" for Teams.
- **Commit:** `feat(teams): landing integration entry (available)`

### Phase 2: README + SELF_HOSTING.md

- **Files:** `README.md`, `docs/SELF_HOSTING.md`
- **Steps:**
  1. README.md:62 "Sources & integrations" row — outbound clause `alerts and digests via Slack and Discord` gains `and Teams`.
  2. SELF_HOSTING.md:
     - Privacy table (:142): add Teams to the integrations list.
     - New `### Teams alerts` section mirroring `### Discord alerts` (:763-808): connect steps (classic Incoming Webhook URL or Workflows URL), the per-type channel toggle, and the honest limits — MessageCard only (no Adaptive Cards), no OAuth, `send_notification` + playbook `notify` support, and the note that automation rules can reach Teams only via an API-created rule or the playbook editor (no channels editor).
     - Automations notify claim (:1004-1007): name Teams alongside Slack/Discord.
  3. Grep the whole repo for provider-set claims that now go stale (`grep -rn "Slack and Discord" --include="*.md" .` + landing `lib/blog-posts/`; fix only non-blog docs; blog posts are dated artifacts — leave them, note in commit).
- **Validation:** grep for "Teams" in both files; no contradicting sentence remains.
- **Commit:** `docs(teams): README + SELF_HOSTING Teams alerts section`

### Phase 3: Changelog + tracking (repo convention)

- **Files:** `CHANGELOG.md`, `AI-TRACKING.md`, `DEV-TRACKING.md`
- **Steps:**
  1. `CHANGELOG.md`: add a "Teams notifications" entry under the current version/unreleased section, naming the shipped surface + honest limits (webhook-only, MessageCard, no inbound source).
  2. `AI-TRACKING.md`: extend the notification/integration capability row (or add a line to the integrations block near the Intercom/SSO rows) — Teams shipped 2026-09-02, outbound-only, webhook URL connect, per-user `channel_teams` preference; note the inbound source (`feedback_source.py:16` names `teams`) stays out of scope.
  3. `DEV-TRACKING.md`: close P7's "adding a fifth provider means another full round" framing — the Teams slice shipped with the bounded-sender decision (sibling senders, no provider-abstraction refactor); P7 itself stays NOT STARTED with the Teams note updated to "Teams shipped via sibling senders (2026-09-02); the duplication count is now 5×/3×-plus, refactor still deferred".
- **Validation:** grep the three files for the Teams entry.
- **Commit:** `docs(teams): changelog + tracking close-out`

### Phase 4 (optional): Blog post

- Only if a slot is open in `BLOG-TRACKING.md` (check the schedule first). Precedent: the Slack post `services/landing-web/lib/blog-posts/batch5.ts:7-75`. If no slot: skip, and do not claim a post anywhere.

## 3. File-by-file build order

| File | Purpose |
|---|---|
| `services/landing-web/lib/integrations.ts` | Teams entry |
| `README.md` | outbound provider row |
| `docs/SELF_HOSTING.md` | privacy table + Teams alerts section + automations claim |
| `CHANGELOG.md` | feature entry |
| `AI-TRACKING.md` / `DEV-TRACKING.md` | capability row + P7 close-out |

## 4. Testing strategy

- Landing build; grep-based verification (no stale claims). No unit tests for docs.

## 5. Environment & secrets

- None.

## 6. Edge cases

- Landing `status` field drives the grid — `'available'` is load-bearing; typo = invisible entry.
- Blog posts are dated artifacts — never edit historical posts to retrofit Teams.

## 7. Agent execution notes

- Verify each claim against the branch's merged code (run the backend/worker suites from the other aspects first or at least read the final diffs).
- Checkpoint after Phase 1 (entry) and Phase 3 (tracking).