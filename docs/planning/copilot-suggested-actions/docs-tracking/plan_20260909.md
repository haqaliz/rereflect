# Implementation Plan — docs-tracking

**Slug:** `copilot-suggested-actions` · **Aspect:** `docs-tracking`
**Date:** 2026-09-09 · **Branch:** `feat/copilot-suggested-actions`
**Sources:** `prd.md` (Rollout & Documentation) · `docs-tracking/spec.md`
**Build order note:** **Last.** Depends on the shipped behaviour of every other aspect — write it once the behaviour is final so every claim is true. It is a docs-only aspect: no code, no tests (the repo has no docs-lint guard for these files).

## 1. Project Setup Checklist

- **Files modified (root, in the worktree):**
  - `CHANGELOG.md`
  - `AI-TRACKING.md`
  - `README.md`
  - `docs/SELF_HOSTING.md`
- **Verification target (no code change):** `DEV-TRACKING.md` already records the six adjacent copilot defects under *"Found while doing the copilot-suggested-actions dig (2026-09-06)"* — confirm they are present; do **not** re-add them.
- **No backend/frontend/worker changes, no deps, no migration.**

## 2. Implementation Phases

### Phase H — Tracking files (`CHANGELOG.md`, `AI-TRACKING.md`)

**Goal:** the roadmap's source of truth reflects what shipped, with the honest limits.

**Prerequisites:** `action-registry`, `deterministic-proposer`, and `frontend-actions-ui` all merged on the branch.

**Files:** `CHANGELOG.md`, `AI-TRACKING.md`.

**Steps:**
1. `CHANGELOG.md` — a behaviour entry under **Added**, in the house style of the other copilot-era entries:
   - The new `actions` item in `structured_data`: after a `data`/`analysis` query whose result carries a customer-email column, the copilot appends one **Tag these N customers…** action; clicking it opens a dialog where the operator types the tag and confirms.
   - The execute route (`POST /api/v1/copilot/actions/execute`) and the `BulkActionSummary` outcome (`matched`/`updated`/`skipped`/`errors[]`).
   - **Honest limits (spec's over-claim warning, `docs-tracking/spec.md:59`):** exactly one action type (`tag_customers`); `data`/`analysis` queries only (never `general`/`report`); admin/owner only; each proposal is **one-shot** (a second execution returns the first outcome); proposals are **server-derived from the result shape, not model-authored**, so they behave identically on a keyless local model; the cohort is the frozen email list from the answer shown, capped at 200 — a larger result offers no action; a 403 member reaching the endpoint gets a clear refusal.
2. `AI-TRACKING.md` — three edits:
   - **Current AI Capabilities table:** add a row for "AI Copilot Suggested Actions" (backend Yes / frontend Yes / Unlocked (OSS)) describing the slice in the row's dense house style.
   - **M2.2 follow-on marker:** append to the existing M2.2 follow-on note (lines 160-169) a sentence recording that the four aspects shipped on `feat/copilot-suggested-actions`, so `PRD-AI-COPILOT.md:33`'s "no action execution" non-goal is now **partially** closed — one action type, deterministic proposer only. Match the `scheduled-ai-reports` follow-on format (dates + branch).
   - **Strategic-decision row at `AI-TRACKING.md:25`:** annotate as **partially delivered** — read + suggest is live for one action type from a deterministic proposer; **do not** mark it done.

**Validation:** grep `AI-TRACKING.md` for the new row + marker + annotation; `CHANGELOG.md` renders under `## Unreleased`.

**Commit message:** `docs(copilot-actions): CHANGELOG entry + AI-TRACKING partial-close markers`

### Phase I — Operator docs (`README.md`, `docs/SELF_HOSTING.md`)

**Goal:** a self-hoster learns what the copilot can do and — just as importantly — what it cannot.

**Prerequisites:** Phase H.

**Files:** `README.md`, `docs/SELF_HOSTING.md`.

**Steps:**
1. `README.md` — in the copilot/feature section, one or two sentences: the copilot can now propose **one** concrete action (tag the customers in an answer) and execute it on the operator's confirmation; state the limits plainly (admin/owner; result-set cohort capped at 200; proposals are server-derived and work identically on a keyless local model).
2. `docs/SELF_HOSTING.md` — a short subsection under the copilot area (match the doc's existing structure): what the action is, who can execute it (admin/owner), the one-shot rule, and that it is computed from the result shape rather than the model — the keyless-local-model claim belongs here explicitly (it is a BYOK/self-host selling point).

**Validation:** grep both files for "suggested action" / "tag"; no sentence claims any action type beyond `tag_customers` or any behaviour not shipped.

**Commit message:** `docs(copilot-actions): README + SELF_HOSTING copilot action limits`

## 3. File-by-File Build Order

| Filepath | Purpose | Notes |
|---|---|---|
| `CHANGELOG.md` | Behaviour entry | House format; honest limits listed |
| `AI-TRACKING.md` | Capability row + M2.2 marker + `:25` annotation | **Partially delivered, never "done"** |
| `README.md` | Capability + limits | No over-claim |
| `docs/SELF_HOSTING.md` | Operator-facing copilot action section | Keyless-local-model claim explicit |

## 4. Testing Strategy

- No automated tests. Verification is **grep/read**:
  - `grep -rn "copilot" AI-TRACKING.md README.md docs/SELF_HOSTING.md CHANGELOG.md` — the new content present.
  - Manual read for over-claim: the only executable claim anywhere is `tag_customers`, admin/owner, one-shot, result-set cohort ≤ 200, `data`/`analysis` only.
- Confirm `DEV-TRACKING.md` already carries the six adjacent defects (it does — added by the dig). If for any reason they are missing, add them in this phase (spec in-scope) rather than leaving the dig unrecorded.

## 5. Environment & Secrets

- None.

## 6. Edge Cases & Error Handling

- **Over-claiming** is the only real risk (spec's open question): the true statement is "the copilot can propose one specific, admin-only, reversible action on the result set of a query, and execute it on click" — not "the AI Copilot can now take actions." Write every sentence against that.
- `AI-TRACKING.md` plan-gating tables are pre-pivot and stale by design — do not touch them (spec out-of-scope).
- Do not fix the adjacent defects this phase documents.

## 7. Agent Execution Notes

- Run this aspect only after the other three are merged and the branch is green end-to-end.
- The branch's final commit set for this feature should read: backend aspects (registry → proposer → route), frontend aspect, then this docs pass — so the tracking claims are true at the moment they are written.