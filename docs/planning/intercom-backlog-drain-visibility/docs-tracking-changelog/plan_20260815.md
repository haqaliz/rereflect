# Implementation Plan — docs-tracking-changelog (aspect 5 of 5, close-out, runs last)

**Date**: 2026-08-15
**Feature**: `intercom-backlog-drain-visibility` (prd.md R7 — docs & tracking are part of "done")
**Aspect**: `docs-tracking-changelog`
**Source**: `docs/planning/intercom-backlog-drain-visibility/docs-tracking-changelog/spec.md` + `prd.md`
**Branch**: `feat/intercom-backlog-drain-visibility` (worktree `.claude/worktrees/feat-intercom-backlog-drain`)
**Sequencing**: **LAST** — execute only after the four code aspects (`client-total-count`,
`sync-estimate`, `db-status-api`, `frontend-remaining-row`) have merged into the branch.
Merge facts are **placeholders** in this plan (`(merged <merge-sha>, PR <# pending>)`),
filled post-merge in a dedicated fill commit per house rule (precedents `4b6284a9`,
`5a9d547d`, `d04f4579`). Placeholders must be resolved from the merged state before the
DEV-TRACKING commit lands.

## 0. Facts gathered (verified 2026-08-15, before writing)

| Fact | Value | Source |
|---|---|---|
| Branch state | `feat/intercom-backlog-drain-visibility` has **zero commits ahead of master** at plan time (`git log master..HEAD` empty; `git status` → `docs/planning/_card/card.md` modified + `docs/planning/intercom-backlog-drain-visibility/` untracked). The four code aspects are spec-only — **every merge fact below is a placeholder** | git status |
| Deferred v2 block header | `DEV-TRACKING.md:504` — `### Deferred v2 — Intercom (opened 2026-08-01; 2 SHIPPED, 2 NOT STARTED)` → must become `3 SHIPPED, 1 NOT STARTED` | read |
| Entry to flip | `DEV-TRACKING.md:528-529` — `- **\`intercom-backlog-drain-visibility\`** — a large backlog drains over several 20-page runs with no operator-visible progress; the settings page shows a count but not "N remaining".` | read |
| SHIPPED style model | `DEV-TRACKING.md:509-522` (pull-replies: strikethrough + `— **SHIPPED <date>** via \`feat/…\` (merged <sha>, PR <#>)` + compact shipped summary + follow-up lines) and :523-527 (writeback: same shape, real facts) — deferred bullets carry no suite counts | read |
| Post-merge fill precedent | `d04f4579` `docs(tracking): fill merge facts for intercom-pull-replies-and-ratings (#16, 873a9c1)`; `5a9d547d`; `4b6284a9` — fill commit message form: `docs(tracking): fill merge facts for <slug> (#<PR>, <sha>)` | git log |
| SELF_HOSTING honest-limits block | `docs/SELF_HOSTING.md:1932-1950` — `> ### Honest limits` blockquote under the OAuth section's `### Verify`; the pull bullets (:1936-1945) were added by the replies/ratings enrichment. **Anchor for the estimate:** the backlog bullet :1944-1945 ("A very large backlog drains over several runs rather than one — each run fetches up to 20 pages and the cursor resumes where it stopped.") | read |
| SELF_HOSTING "Shipped scope:" list | `docs/SELF_HOSTING.md:1747-1757` — token-paste pull bullet, topics, customer-email bullet, write-back pointer. The estimate is **not** listed here today; the honest-limits block carries it (spec: "the pull bullet + honest-limits additions") | read |
| SELF_HOSTING settings-page description | `docs/SELF_HOSTING.md:1918-1919` — "**Settings → Integrations → Intercom** shows connection status and the workspace name once connected." No sentence today describes the row set (workspace/last-synced/last-error/ingested count) — no stale "count but not remaining" claim exists in live docs | read |
| CHANGELOG insertion point | `CHANGELOG.md` — `## Unreleased` at :8; current top :10 `### Added — Intercom pull now ingests replies and ratings`; newest-first. Tone models: :10-29 and :31-51 (bold-led bullets + `*Honest limits:*` italic closer). CHANGELOG entries do **not** carry merge facts (DEV-TRACKING does) | read |
| Stale-claim grep surface (pre-plan) | "count but not 'N remaining'": `DEV-TRACKING.md:529` (the entry being flipped) + planning record (`_card/card.md:10-11,21-22`, `prd.md:15-16`) — both historical, **not** edited. Landing: `services/landing-web/lib/integrations.ts` — grep for `remaining|backlog|ingested` → **zero matches** in the Intercom entry (flag-only, no edit; spec.md:30) | grep |
| AI-TRACKING Intercom row | `AI-TRACKING.md:66` — pull description (cursor, 20-page cap, enrichment). **Not in this spec's scope** (the pull-replies aspect amended it because its spec named it; this one does not). Chosen: no AI-TRACKING edit — flag-only | read + grep |
| Alembic head / migration | `db-status-api` aspect chains off single head `e4f5a6b7c8d9`; the new migration's revision is a **merge-fact placeholder** (`<migration-revision>`) — optional in the SHIPPED summary, filled post-merge | spec.md db-status-api |

## 1. Project setup checklist

Docs-only aspect. No scaffolding, no dependencies, no env, no tests. Files touched (exactly three):

1. `docs/SELF_HOSTING.md` — honest-limits block: estimate semantics bullets (Phase 1)
2. `CHANGELOG.md` — new Added entry at the top of Unreleased (Phase 2)
3. `DEV-TRACKING.md` — deferred-v2 entry → SHIPPED + header count (Phase 3)

**Explicitly NOT edited:** `services/landing-web/lib/integrations.ts` (flag-only, verified
clean), `AI-TRACKING.md` (out of scope, flag-only), the planning record (`_card/card.md`,
`prd.md`, the four code aspects' specs — historical problem statements), any archived
planning-doc body, `docs/planning/intercom-selfhost-ingestion/**` (no pointer edits named
by this spec).

## 2. Implementation phases

### Phase 1 — SELF_HOSTING.md: estimate semantics into the honest-limits block

- **Goal:** the operator-facing docs state the estimate semantics exactly as shipped —
  token-paste-only, computed from `total_count` for the run's window, an estimate not a
  queue count, absent until a completed run, reset on error, mechanics unchanged
  (spec.md:12-15; prd.md "Honest limits").
- **Prerequisites:** all code aspects merged (the feature exists to document).
- **File:** `docs/SELF_HOSTING.md`
- **Steps:**
  1. **Anchor on the backlog bullet (:1944-1945)** in the honest-limits block — keep it
     verbatim (it describes the drain mechanics, which are unchanged).
  2. **Append the estimate bullet(s) directly after it**, in the block's established
     `> -` rhythm. Draft (house voice; the "≈ N conversations left to sync" phrasing is
     the shipped card copy per frontend-remaining-row spec.md:17-19):

     ```
     > - After a completed run, the settings page shows **"≈ N conversations left to
     >   sync"** — an **estimate** computed from Intercom's `total_count` for that run's
     >   sync window, not a queue count: conversations updated during the drain shift
     >   later windows, and the boundary conversation re-counts itself. The row appears
     >   only when there is something left to show (a non-empty window) and only for
     >   token-paste connections — OAuth has no pull, so it never shows there.
     > - The estimate is absent until a completed run, and a failed run **clears it**
     >   rather than leaving a stale number.
     > - The drain mechanics (20-page cap, cursor resume) are unchanged — the estimate
     >   reports progress, it does not speed it up.
     ```

     (Fills spec.md:12-15: token-paste-only, computed from `total_count` for the run's
     window, estimate-not-queue-count, absent until completed run, reset on error, drain
     mechanics unchanged. This is also the "settings-page description in docs matches the
     shipped row" half of spec.md:23 — the copy states the row's presence conditions
     exactly as `frontend-remaining-row` ships them: non-null AND > 0, token-paste only.)
  3. Do **not** touch the `### Verify` line (:1918-1919) or the "Shipped scope:" list
     (:1747-1757) — the honest-limits block is the home for the semantics (see ambiguity 1).
- **Validation:** `git grep -n "total_count" docs/SELF_HOSTING.md` → ≥ 1 (the new bullet);
  `git grep -n "estimat" docs/SELF_HOSTING.md` → ≥ 1; the block still reads coherently
  end-to-end; no sentence anywhere in the Intercom section claims the settings page lacks
  a remaining count.
- **Commit:** `docs(self-hosting): Intercom backlog-drain estimate — honest-limits semantics`

### Phase 2 — CHANGELOG: new Added entry

- **Goal:** the release notes record the remaining-backlog estimate with the honest
  framing — "estimate, not a queue count" is stated, not buried (spec.md:16-17).
- **Prerequisites:** Phase 1 (separate commit; house style `docs(changelog): …`).
- **File:** `CHANGELOG.md`
- **Steps:**
  1. Insert at the **top of `## Unreleased`** (above :10 at plan time; re-locate at
     execution if newer entries landed), newest-first per house convention, in the voice
     of the recent Intercom entries (:10-51):

     ```
     ### Added — Intercom settings page shows the remaining-backlog estimate

     - **"≈ N remaining" on the Connection card.** After a completed pull run, the
       Intercom settings page shows how much of a large backlog is left ("≈ N
       conversations left to sync — estimate, drains over runs"), computed from
       Intercom's `total_count` for that run's sync window. A first connect with a big
       history now shows real drain progress instead of an opaque ingested count.
     - **Absent when there is nothing honest to show** — no row when the window is
       empty, the run failed (the estimate is cleared, never left stale), the
       connection is disconnected, or the organization connects via OAuth (no pull).
     - *Honest limits:* the number is an **estimate, not a queue count** — conversations
       updated during the drain shift later windows and the boundary conversation
       re-counts itself; token-paste connections only; present only after a completed
       run; the 20-page/run drain mechanics are unchanged.
     ```

     Leave merge facts out of the changelog entry (house rule — DEV-TRACKING carries them).
- **Validation:** the new entry sits at the actual top of Unreleased; the entry's body
  names estimate-not-queue-count, token-paste-only, post-run presence and reset-on-error;
  `git grep -n "remaining-backlog estimate" CHANGELOG.md` → 1.
- **Commit:** `docs(changelog): Intercom backlog-drain estimate entry + honest framing`

### Phase 3 — DEV-TRACKING: deferred-v2 `intercom-backlog-drain-visibility` → **SHIPPED**

- **Goal:** the deferred list reflects reality — three shipped, one open; the entry
  carries the shipped summary with merge-fact placeholders (filled post-merge).
- **Prerequisites:** code aspects merged; merge sha / PR number known — or the
  placeholders kept per the house rule (see §6).
- **File:** `DEV-TRACKING.md`
- **Steps:**
  1. **Header :504 count fix:** `(opened 2026-08-01; 2 SHIPPED, 2 NOT STARTED)` →
     `(opened 2026-08-01; 3 SHIPPED, 1 NOT STARTED)`.
  2. **Rewrite the :528-529 bullet** in the house SHIPPED style — strikethrough of the
     original text + shipped summary (model: :509-522; the estimate semantics wording
     matches prd.md's honest limits):

     ```
     - ~~**`intercom-backlog-drain-visibility`** — a large backlog drains over several 20-page
       runs with no operator-visible progress; the settings page shows a count but not "N remaining".~~
       — **SHIPPED 2026-08-<merge-day>** via `feat/intercom-backlog-drain-visibility`
       (merged <merge-sha>, PR <# pending>): the Intercom settings page Connection card
       now shows **"≈ N conversations left to sync"** after a completed pull run — an
       estimate computed from Intercom's `total_count` for the run's window (an estimate,
       not a queue count: arrivals during the drain shift later windows, the boundary
       conversation re-counts itself). Token-paste connections only; absent until a
       completed run and cleared on error — a failed run never leaves a stale number.
       Drain mechanics (20-page cap, cursor resume) unchanged. Backend
       `intercom_integrations.backlog_remaining` (migration <migration-revision>) + status
       field + model-parity type-tuple entry + client 3-tuple `total_count` surface.
     ```

     The `(merged <merge-sha>, PR <# pending>)` and `<migration-revision>` tokens stay
     **placeholders until merge** — do not invent facts (house rule: "correct the marker
     in the same commit"; fill-facts follow-up precedent `d04f4579`).
  3. The remaining bullet `intercom-oauth-path-retirement` (:530-532) stays NOT STARTED,
     untouched (spec.md:25-27 — gated on evidence of use).
  4. **Post-merge fill (follow-up commit, do not amend):** once the PR merges, replace
     `<merge-sha>` / `<PR # pending>` / `<merge-day>` (and `<migration-revision>` if kept)
     with the real facts in a `docs(tracking): fill merge facts for
     intercom-backlog-drain-visibility (#<PR>, <sha>)` commit — the `d04f4579` precedent.
- **Validation:** `git grep -n "SHIPPED 2026-08-" DEV-TRACKING.md` → ≥ 1 for this entry;
  header count reads `3 SHIPPED, 1 NOT STARTED`; `git grep -n "count but not" DEV-TRACKING.md`
  → only the struck (~~…~~) text (historical); no other deferred bullet changed; no
  placeholders survive post-merge.
- **Commit:** `docs(tracking): intercom-backlog-drain-visibility SHIPPED`

### Phase 4 — Grep sweep (final gate) + landing flag-only check

- **Goal:** prove spec.md's acceptance criteria — no surviving claim that the settings
  page lacks a remaining count; docs state the estimate semantics; the settings-page
  description matches the shipped row; landing page verified flag-only.
- **Steps** (run from the repo root; re-locate targets by text if line numbers shifted):

  ```bash
  git grep -n "count but not" DEV-TRACKING.md _card.md docs/planning/_card/card.md   # → only struck text in the SHIPPED entry (~~…~~) + planning record (_card.md, prd.md) — historical, do not edit
  git grep -n "N remaining" DEV-TRACKING.md         # → only the struck (~~…~~) text
  git grep -n "remaining" docs/SELF_HOSTING.md      # → the new estimate bullets (Phase 1)
  git grep -n "total_count" docs/SELF_HOSTING.md    # → ≥ 1 (estimate semantics stated)
  git grep -n "3 SHIPPED, 1 NOT STARTED" DEV-TRACKING.md   # → 1
  git grep -n "estimat" CHANGELOG.md                # → new entry (Phase 2)
  # Landing (flag-only — spec.md:30; no edit):
  git grep -ni "remaining\|backlog\|ingested" services/landing-web/lib/integrations.ts   # → 0
  ```

  Remaining hits **outside** those targets are expected and intentional: the planning
  record (`_card/card.md`, `prd.md`, the code aspects' specs, this plan) — historical
  problem statements, do not edit.
- **Human review pass:** re-read `docs/SELF_HOSTING.md:1722-1956` end-to-end — the
  honest-limits block must read coherently with the new estimate bullets; then re-read the
  CHANGELOG entry and the SHIPPED bullet against the merged diff.

## 3. File-by-file build order

| # | File | Edit type | Purpose |
|---|------|-----------|---------|
| 1 | `docs/SELF_HOSTING.md` | Modify | honest-limits block (:1932-1950): estimate semantics bullets after the backlog bullet :1944-1945 (Phase 1) |
| 2 | `CHANGELOG.md` | Modify | `### Added — Intercom settings page shows the remaining-backlog estimate` at top of Unreleased (Phase 2) |
| 3 | `DEV-TRACKING.md` | Modify | :528-529 → SHIPPED (strikethrough + summary + placeholders); header :504 → 3 SHIPPED, 1 NOT STARTED (Phase 3) |

## 4. Testing strategy

- **No new tests.** Docs-only; no code, no migration, no comment in a code file — no
  backend/worker/frontend suite rerun is required (unlike the writeback aspect, which
  touched `models/integration.py`). The suite counts in the code aspects' test runs are
  unaffected by this aspect.
- **Verification is the Phase 4 grep suite** plus the human re-read of the Intercom
  section, the CHANGELOG entry, and the SHIPPED bullet against the actual merged diff.

## 5. Environment & secrets

- None. No env vars, no running stack, no database. All edits are markdown in tracked files.

## 6. Edge cases & error handling

- **Merge facts unavailable** (this aspect runs last but before the final PR merges):
  keep `<merge-sha>` / `<PR # pending>` / `<merge-day>` placeholders in the SHIPPED
  bullet and land the fill-facts follow-up commit after merge (the `d04f4579` /
  `5a9d547d` / `4b6284a9` precedent). Do **not** invent facts, and do **not** let
  placeholders survive the post-merge fill.
- **A code aspect has not merged** (sibling dirs still spec-only, or PRs open): halt,
  surface to the lead, do not mark SHIPPED — the docs pass describes what shipped.
- **CHANGELOG has newer entries above :10 by execution time**: insert at the actual top
  of Unreleased, not at a line number.
- **SELF_HOSTING section reflowed** (someone reflowed the Intercom section between now
  and execution): re-locate by text ("A very large backlog drains over several runs",
  "Honest limits"), not by line number.
- **SHIPPED date**: use the actual merge date, not a hardcoded 2026-08-15.
- **Post-merge fill lands after another change to the same bullet**: re-locate by text.

## 7. Agent execution notes

- Runs last. Two human checkpoints: (1) before Phase 1 — confirm every code aspect is
  merged on the branch, fill the merge-facts table; (2) before the Phase 3 commit — a
  reviewer signs off the SHIPPED bullet's summary against the actual diff.
- Commit sequence (house style — do not amend; push and open the PR per house workflow):
  1. `docs(self-hosting): Intercom backlog-drain estimate — honest-limits semantics` (Phase 1)
  2. `docs(changelog): Intercom backlog-drain estimate entry + honest framing` (Phase 2)
  3. `docs(tracking): intercom-backlog-drain-visibility SHIPPED` (Phase 3)
  4. Post-merge: `docs(tracking): fill merge facts for intercom-backlog-drain-visibility (#<PR>, <sha>)`
- The landing check lives only in the Phase 4 verification output / PR description — no
  landing edit, no separate tracking entry (spec.md:30).
- This feature's own planning docs (`_card/card.md`, `prd.md`, the four specs, this plan)
  are untracked and land in the feature's separate `docs(planning)` commit — do not mix
  them into the three tracked-file commits above.

## 8. Spec ambiguities flagged

1. **"the pull bullet" anchor (spec.md:12)** — the pull bullets live inside the honest-limits
   block under the OAuth `### Verify` heading (:1932-1950), not in the access-token
   section or the "Shipped scope:" list (:1747-1757). **Chosen:** anchor on the backlog
   bullet :1944-1945 and append the estimate bullets inside the same honest-limits block
   (the block is where the recent enrichment edits already put pull behavior). If
   reviewers prefer the "Shipped scope:" list instead, the same bullets move there —
   droppable either way, the semantics are unchanged.
2. **"the settings-page description in docs matches the shipped row" (spec.md:23)** — no
   live doc sentence currently describes the settings page's row set (SELF_HOSTING:1918-1919
   says only "connection status and the workspace name"). Interpreted as: the *new* docs
   copy must describe the row exactly per shipped behavior (non-null AND > 0, token-paste
   only, post-completed-run, cleared on error). Pinned by the Phase 1 draft + Phase 4
   sweep; no pre-existing sentence to correct was found.
3. **No AI-TRACKING.md amend in scope** — the pull-replies aspect amended AI-TRACKING:66
   because its spec named it; this spec's in-scope list names only SELF_HOSTING,
   CHANGELOG, DEV-TRACKING. **Chosen:** no AI-TRACKING edit — the estimate is a
   status/UI surface feature, not an ingestion-mechanics change; the row's description
   already carries the pull mechanics. Flag-only.
4. **`<migration-revision>` in the SHIPPED summary** — the writeback SHIPPED bullet named
   its migration revision; this bullet's summary can skip it or carry it as a merge-fact
   placeholder. **Chosen:** include as `<migration-revision>` placeholder, filled
   post-merge with the other facts; droppable if the executor prefers a shorter bullet.
5. **No pointer edits to `docs/planning/intercom-selfhost-ingestion/**`** — the writeback
   and pull-replies aspects both added SHIPPED/SUPERSEDED pointers there; this spec's
   in-scope list does not include them. **Chosen:** out of scope, no pointer. Flag-only
   in case reviewers want the ingestion PRD's pull bullet to reference the estimate.
