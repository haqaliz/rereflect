# Implementation plan — fail-closed-flip (2026-08-17)

Feature: `slack-email-signature-enforcement` · Aspect: `fail-closed-flip`
Source: `docs/planning/slack-email-signature-enforcement/prd.md` + `spec.md`.
House commit style: `feat(api): ... (TDD)` / `test(api): RED — ...` / `docs(...)`.

## Phase 1 — RED: empty the sweep allowlist

- **Goal:** the guard turns RED, proving the verifiers still fail open.
- **Files:** `services/backend-api/tests/test_webhook_verifiers_fail_closed.py`
  (:47-50 remove both entries; :130-133 → `assert SHADOW_ALLOWLIST == set()`).
- **Steps:**
  1. Remove the two allowlist entries.
  2. Update the allowlist pin to the empty set.
  3. Run `venv/bin/pytest tests/test_webhook_verifiers_fail_closed.py -v` — expect
     RED: `test_verifier_rejects_when_secret_unset` fails for slack + email (result is
     True, expected False).
- **Validation:** the two failures name exactly
  `source_webhooks.verify_slack_signature` and `email_webhooks._verify_webhook_signature`.
- **Commit:** `test(api): RED — sweep guard: no verifier may accept an unset secret`.

## Phase 2 — GREEN: flip both verifiers

- **Goal:** both verifiers fail closed with a non-shadow warning.
- **Files:**
  - `services/backend-api/src/api/routes/source_webhooks.py:108-115` —
    replace the `SECURITY-SHADOW` block: non-shadow `logger.warning` (wording mirroring
    the Intercom fail-closed branch at :339-341, adjusted for Slack) + `return False`.
  - `services/backend-api/src/api/routes/email_webhooks.py:45-52` — identical shape
    for `RESEND_INBOUND_WEBHOOK_SECRET`; keep the two warning strings in agreement.
- **Steps:** implement; run the sweep test — expect GREEN (all seven `False`).
- **Validation:** `venv/bin/pytest tests/test_webhook_verifiers_fail_closed.py -v`.
- **Commit:** `feat(api): slack + email webhook verifiers fail closed on unset secret (TDD)`.

## Phase 3 — Flip the shadow tests

- **Files:**
  - `services/backend-api/tests/test_slack_webhook.py:31-80` —
    `TestVerifySlackSignatureShadowMode::test_missing_secret_accepts_in_shadow_but_warns`
    → asserts `result is False` (no SECURITY-SHADOW marker assertion);
    `TestSlackWebhookShadowMode::test_unset_secret_accepts_request_and_logs_marker` →
    asserts HTTP 401 (drop marker). Rename the test classes/methods to the fail-closed
    semantics (e.g. `test_missing_secret_rejects` / `TestSlackWebhookFailClosed`),
    update the module docstring's shadow rationale (:1-9).
  - `services/backend-api/tests/test_email_webhooks.py:367-397` —
    `test_missing_secret_accepts_in_shadow_but_warns` → asserts `False` / 401 without
    the marker; update the docstring (:375-386).
- **Validation:** both files green; configured-secret tests (:82-122 Slack, :350-365
  email) untouched.
- **Commit:** `test(api): flip slack + email shadow tests to fail-closed expectations (TDD)`.

## Phase 4 — Re-scope the startup warning

- **Files:**
  - `services/backend-api/src/api/main.py:140-187` —
    `warn_unconfigured_webhook_secrets`: re-word both messages from "being accepted
    unverified / before enforcement lands" to the fail-closed notice ("webhook
    deliveries will be rejected until you set X — see docs/SELF_HOSTING.md").
  - `services/backend-api/tests/test_startup_webhook_secret_warnings.py` (:33-85,
    :88-148) — update the substring assertions to the new wording (warn-when-unset
    semantics unchanged).
- **Validation:** the startup-warning tests green.
- **Commit:** `feat(api): re-scope webhook secret startup warnings to fail-closed notice (TDD)`.

## Phase 5 — Docs, env examples, changelog, tracking

- **Files:**
  - `docs/SELF_HOSTING.md:2468-2469` table rows → fail-closed ("Rejects deliveries
    until configured") + `:2473-2478` grace-period paragraph → fail-closed wording +
    configure instructions (set `SLACK_SIGNING_SECRET` / `RESEND_INBOUND_WEBHOOK_SECRET`).
  - `.env.example:36-44` + `.env.prod.example:128-136` — replace the shadow copy
    ("currently ACCEPTED UNVERIFIED…") with fail-closed copy.
  - `CHANGELOG.md` — behavior-change entry at the top of Unreleased (Slack/email
    webhooks now require a configured signing secret; deliveries rejected with 401
    until set) + an append-only correction note on the grace-period entry (:319-324).
  - `DEV-TRACKING.md:423-426` — follow-up → **FIXED** with a shipped summary
    (verifiers fail closed, sweep allowlist empty + pinned, shadow tests flipped,
    startup warnings re-scoped, docs/env/copy updated) + `(merged <merge-sha>, PR
    <# pending>)` placeholder (filled post-merge) + the S1 follow-up note
    (generic-webhook per-source `secret_token` fail-open, source_webhooks.py:270-274).
- **Validation:** grep sweep — `SECURITY-SHADOW` / "accepted unverified" absent from
  SELF_HOSTING, both env examples, and the flipped code paths (CHANGELOG may retain
  the phrase only inside the correction note).
- **Commits:** `docs(self-hosting): ...` / `docs(changelog): ...` / `docs(tracking): ...`
  (per house style, split by file group).

## File-by-file build order

1. `tests/test_webhook_verifiers_fail_closed.py` (allowlist + pin)
2. `src/api/routes/source_webhooks.py` (verify_slack_signature)
3. `src/api/routes/email_webhooks.py` (_verify_webhook_signature)
4. `tests/test_slack_webhook.py`, `tests/test_email_webhooks.py` (shadow flips)
5. `src/api/main.py`, `tests/test_startup_webhook_secret_warnings.py`
6. `docs/SELF_HOSTING.md`, `.env.example`, `.env.prod.example`, `CHANGELOG.md`, `DEV-TRACKING.md`

## Testing strategy

- RED: sweep test with the emptied allowlist (the guard).
- Per-phase: targeted test files; final: full backend suite
  (`venv/bin/pytest tests/ -q --timeout=300` from `services/backend-api`).
- No worker/frontend impact (badge survives; frontend untouched).

## Environment & secrets

- No new env vars; the flipped code reads the existing `SLACK_SIGNING_SECRET`
  (source_webhooks.py:33) / `RESEND_INBOUND_WEBHOOK_SECRET` (email_webhooks.py:38)
  at import time.
- venv: `services/backend-api/venv` (python3.12).

## Edge cases & error handling

- Both verifiers with the secret SET: behavior unchanged (valid → 200, invalid → 401).
- The email verifier's module-constant secret: tests patch
  `email_webhooks.RESEND_INBOUND_WEBHOOK_SECRET` (the existing `_call_email` pattern).
- Startup warning fires only when unset AND an active Slack integration / email source
  exists (existing semantics preserved).

## Agent execution notes

- The sweep test is the non-negotiable RED; if it doesn't fail in Phase 1, the
  verifiers were already fail-closed and the plan's premise is wrong — stop and report.
- Keep the two replacement warning strings byte-identical in wording (only the env var
  name differs).
- Verify no other test asserts the shadow marker string
  (`SECURITY-SHADOW`) after Phase 3 (grep).
