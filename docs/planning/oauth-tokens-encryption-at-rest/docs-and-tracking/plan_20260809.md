# Implementation Plan — docs-and-tracking

**Aspect:** `docs-and-tracking` · **Branch:** `bug/oauth-tokens-stored-plaintext`
**Date:** 2026-08-09 · **Sources:** `docs/planning/oauth-tokens-encryption-at-rest/prd.md` (R7), `docs/planning/oauth-tokens-encryption-at-rest/docs-and-tracking/spec.md`

## Project Setup Checklist

- Repo root only (docs + tracking files). No code, no tests.
- **Sequencing:** run this aspect LAST — after the three code aspects land — so it can quote the actual merge commit and the migration's exact error text.

## Implementation Phases

### Phase 1: SELF_HOSTING.md callout

**Files to modify:**
- `docs/SELF_HOSTING.md`

**Implementation steps:**
1. Find the existing section that documents `LLM_ENCRYPTION_KEY` (it exists — SSO/integration saves return 422 without it, per `.env.example:6-7`).
2. Add a short subsection (or paragraph) titled e.g. "OAuth token encryption & the upgrade requirement":
   - Slack/Intercom OAuth tokens are now encrypted at rest with Fernet (same as every other integration).
   - `alembic upgrade head` **fails** with `RuntimeError` when `LLM_ENCRYPTION_KEY` is unset, rather than leaving tokens in plaintext.
   - Quote the actual error text from the migration; give the remedy: generate a key (`python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`), set `LLM_ENCRYPTION_KEY`, re-run `alembic upgrade head`.
   - Note that existing plaintext rows are encrypted in place on the first upgrade, and that rotating the key afterwards invalidates stored tokens (already-true for every integration — document it once, honestly).

### Phase 2: DEV-TRACKING marker

**Files to modify:**
- `DEV-TRACKING.md`

**Implementation steps:**
1. Locate the P1 entry `oauth-tokens-stored-plaintext` (`DEV-TRACKING.md:500-518`).
2. Follow the house pattern used by sibling entries (see how P0/P0b/P1 `automation-slack-channel` mark **FIXED**): retitle with **FIXED**, add a `> Shipped:` block above the original triage listing the merge commit, what shipped (encrypt-on-write at both callbacks, decrypt at 8 read sites, worker local `_decrypt` mirrors, fail-closed backfill migration, intercom_sync dead-import fix), and the test counts.
3. Roadmap-hygiene rule (DEV-TRACKING.md:497): the marker ships in the **same branch/commit** as the fix. Do not leave the entry unchecked.
4. If the dig confirmed anything else deliberately not bundled (e.g. the `intercom-oauth-path-retirement` note, R9), add it as a short follow-up line below the FIXED block.

### Phase 3: CHANGELOG entry

**Files to modify:**
- `CHANGELOG.md` (repo root)

**Implementation steps:**
1. Follow the file's existing entry style (look at recent entries for the shape).
2. Entry text: OAuth tokens for Slack and Intercom integrations are now encrypted at rest; a migration encrypts existing tokens on upgrade (requires `LLM_ENCRYPTION_KEY`); worker alert paths decrypt internally. Note the upgrade requirement for operators who never set the key.

### Phase 4: Commit

**Commit message:** `docs: SELF_HOSTING upgrade callout, DEV-TRACKING FIXED marker, CHANGELOG for OAuth token encryption`

## File-by-File Build Order

| File | Purpose |
|---|---|
| `docs/SELF_HOSTING.md` | fail-closed upgrade callout + rotation honesty note |
| `DEV-TRACKING.md` | P1 entry → FIXED with shipped summary + follow-up notes |
| `CHANGELOG.md` | release note |

## Testing Strategy

- None (docs). Validation is a read-through: the SELF_HOSTING section must match the migration's actual error string; the DEV-TRACKING entry must quote the real merge commit.

## Environment & Secrets

- None.

## Edge Cases & Error Handling

- Do not restore or weaken the corrective comment at `models/integration.py:19-25` — it must remain exactly as-is (the code now matches its promise).
- Do not touch the billing/plan-tier/Railway/Resend sections of `CLAUDE.md` or `DEV-TRACKING.md` — pre-pivot framing, out of scope.

## Agent Execution Notes

- **Checkpoint:** before Phase 1 — confirm the three code aspects are merged/landed on the branch and grab the merge commit hash + the migration's exact `RuntimeError` text.
- Read the migration source before writing the SELF_HOSTING callout so the quoted error is verbatim.
