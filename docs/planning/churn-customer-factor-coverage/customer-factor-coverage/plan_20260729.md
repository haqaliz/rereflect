# Implementation Plan — `customer-factor-coverage` (2026-07-29)

**Spec:** `./spec.md` · **PRD:** `../prd.md` · **Branch:** `bug/worker-resolution-time-scoring`
**Service:** `services/worker-service` only. No migration, no API, no frontend.

## Setup

- venv: `cd services/worker-service && python3.12 -m venv venv && ./venv/bin/pip install -r requirements.txt`
- Test: `./venv/bin/pytest tests/test_churn_factor_computation.py tests/test_churn_heuristic.py -v`
- **A `db` fixture already exists** — `tests/conftest.py:54`, in-memory SQLite
  (`sqlite:///:memory:`, `TestingSessionLocal`). The DB-backed tests were always possible;
  nobody wrote them. Use it; do not build a new harness.

## Anchors

`services/worker-service/src/tasks/analysis.py`:
- defaults **L700-701** — `resolution_score_pts = 0`, `resolution_label = "Insufficient resolution data"`
- broken import **L821** — `from src.models.feedback_workflow_event import FeedbackWorkflowEvent`
- swallowing except **~L872-873** — `except Exception: pass`
- factors dict **L919-928**, `"resolution_time"` at **L926**
- total **L929-930** — `sum(...)`, capped at 100

---

## Phase 1 — RED: prove resolution_time is dead

**File:** `tests/test_churn_factor_computation.py` (append a `TestCustomerLevelFactorsWithDB` class)

Write **two** tests, both using the `db` fixture:
1. `test_resolution_time_scores_10_for_slow_resolution` — org + customer, a `FeedbackItem`
   created 10 days ago, a `FeedbackWorkflowEvent(event_type='status_changed',
   new_value='resolved')` dated 9 days after creation → expect
   `factors["resolution_time"]["score"] == 10`.
2. `test_resolution_time_scores_0_for_fast_resolution` — resolved 1 day after creation →
   expect score `0` **and** a label containing "Resolved within" (this is what proves the
   branch actually ran, rather than defaulting).

**Both must FAIL now** — test 1 on the score, test 2 on the label still reading
"Insufficient resolution data". Record the failure output.

**Commit:** `test(worker): failing tests proving the churn resolution_time factor never runs`

---

## Phase 2 — GREEN: fix the import

**File:** `services/worker-service/src/tasks/analysis.py`

1. Delete the function-local import at L821.
2. Add `FeedbackWorkflowEvent` to the existing module-level `src.models` import.
3. Leave the surrounding query logic untouched.

**Validation:** Phase 1 tests pass; the existing 38 tests still pass.

**Commit:** `fix(worker): correct the FeedbackWorkflowEvent import so resolution_time scores`

---

## Phase 3 — RED→GREEN: prove the other four factors

For **each** of `sentiment_trend`, `feedback_frequency`, `pain_severity`,
`feature_density`, add a DB-backed test producing its documented non-zero score (see spec S2
table for the exact condition per factor).

These are expected to **pass immediately** — they use `FeedbackItem`, which imports fine.
That is the point: they convert "assumed working" into "proven working".

**⚠️ STOP CONDITION.** If any of these four fails, that factor is *also* broken. **Report it
and stop** — do not fix it in this branch. Two such bugs have already been found this
session; a third is an operator decision, not an implementer's.

Also add:
- `test_feedback_workflow_event_symbol_resolves` — import guard (spec S4).
- Confirm the existing `db=None` / `customer_email=None` tests still assert 0 for all five.

Comment any test whose expected semantics were inferred from the implementation rather than a
spec — that is characterization, not verification.

**Commit:** `test(worker): DB-backed coverage for all five customer-level churn factors`

---

## Phase 4 — GREEN: stop the silence

**File:** `services/worker-service/src/tasks/analysis.py`

Replace each of the nine `except Exception: pass` blocks in the factor computation with:

```python
except Exception as exc:
    logger.warning("churn factor %s failed to compute: %s", "<factor_name>", exc)
```

**Keep the per-factor isolation** — one failing factor must not void the score. Only the
silence changes.

Add `test_factor_failure_logs_and_leaves_others_intact` — simulate one factor raising
(e.g. monkeypatch a query to raise), assert a warning is logged and the other eight still
score.

**Validation:** full worker suite `SENTRY_DSN="" ./venv/bin/pytest tests/ -q`.
Baseline to beat: **1372 passed, 0 failed** (measured 2026-07-29).

**Commit:** `fix(worker): log churn factor failures instead of swallowing them`

---

## Phase 5 — Docs

- **CHANGELOG**: state plainly that churn scores will rise by up to 10 points and some
  customers change risk band, because the resolution-time factor was never running. Do not
  soften this to "improved accuracy".
- Document `backend-api/scripts/backfill_churn_factors.py` as the **opt-in** command for
  recomputing historical rows. Not run as part of this fix.

**Commit:** `docs: record the churn score change and the opt-in factor backfill`

---

## Edge cases

| Case | Expected |
|---|---|
| Customer with resolved events but no matching `FeedbackItem` | No crash; score 0, "No resolved issues in 60 days" |
| Resolution event older than the 60-day window | Excluded from the average |
| `created_at` missing for a feedback id | Skipped in the average, others still counted |
| Exactly 7.0 / 3.0 days average | Boundary — `>7` and `>3` are strict; assert the boundary explicitly |

## Agent execution notes

- **Checkpoint after Phase 1** — confirm both tests fail for the right reason (dead factor),
  not a fixture error.
- **Checkpoint at the Phase 3 stop condition** — if a second factor is broken, stop and report.
- Do not "improve" factor thresholds while here. Weights and thresholds are product decisions.
