# Implementation Plan — docs-and-tracking

**Aspect spec:** `./spec.md` · **PRD:** `../prd.md` (M4) · **Date:** 2026-07-15 · **Branch:** `feat/public-api-crud-v3`
**No code, no tests** — docs + tracking only. **Runs LAST**, after the two feature aspects merge, so it
reflects final shipped shapes.

---

## 0. Prerequisites

- `bulk-feedback-write` and `public-taxonomy-crud` implemented and green. Pull the **final** request/response
  shapes + route paths from the merged code (do not copy from the plans — copy from what shipped).

## 1. Tasks (single phase)

### Files to update
| File | Change |
|---|---|
| `docs/SELF_HOSTING.md` | Add a "Public API — bulk & taxonomy" subsection: `POST /api/public/v1/feedback/bulk` (ids≤500, uniform patch, per-item results, `count_only`), and the `/api/public/v1/categories` CRUD (+ `X-Rereflect-Warning` on delete). Note both need the `write` scope (GET categories needs `read`). |
| `CHANGELOG.md` | New entry under the working version: "Public API: bulk feedback writes + custom-category CRUD (v3)." Match the existing entry style. |
| `AI-TRACKING.md` | Line ~292 (the public-API-write row): move "bulk writes" and "taxonomy CRUD" out of **Deferred (v2)** into shipped, dated 2026-07-15, with a one-line description mirroring the other shipped rows. Leave "customer CRUD" + "mutating the stored category/sentiment column" in Deferred. |
| `DEV-TRACKING.md` | Mirror: mark bulk writes + taxonomy CRUD shipped in the public-API line (line ~26). |
| Public OpenAPI description | Confirm the router-level description / tags text (public_api.py) reads correctly for the new routes; Swagger schemas auto-generate — **verify**, don't hand-author. |

### Steps
1. Read the merged code for both aspects; capture exact paths, fields, status codes, headers.
2. Update `SELF_HOSTING.md` (operator-facing, concrete `curl` examples consistent with what shipped).
3. Add the `CHANGELOG.md` entry.
4. Update `AI-TRACKING.md` + `DEV-TRACKING.md` — keep the "still deferred" items accurate (customer CRUD stays deferred; note it was resolved-as-incoherent, pointing at `docs/planning/public-api-crud-v3/`).
5. Start the backend, open `/api/public/v1/docs`, confirm the bulk + categories routes render with correct schemas.

## 2. Validation

1. `/api/public/v1/docs` (Swagger) shows `POST /feedback/bulk`, `GET/POST /categories`, `PATCH/DELETE /categories/{id}` with accurate schemas.
2. The four docs/tracking files are internally consistent and the deferral list is honest (no claiming customer CRUD shipped).
3. `git grep` for the old "Deferred (v2): … bulk writes, … taxonomy CRUD" wording — ensure it's updated everywhere it appears (AI-TRACKING + DEV-TRACKING + any PRD lineage note).

## 3. Commit

`docs(public-api-crud-v3): SELF_HOSTING + CHANGELOG + AI/DEV-TRACKING for bulk + taxonomy CRUD`

## 4. Agent execution notes

- This is a **documentation** aspect — accuracy over prose. Every claim must match shipped code.
- Do not re-open feature decisions here; if a doc reveals a spec/impl mismatch, flag it to the integrator
  rather than silently "fixing" via docs.
