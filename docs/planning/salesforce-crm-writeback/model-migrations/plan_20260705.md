# Implementation Plan — model-migrations

**Feature:** `salesforce-crm-writeback` · **Aspect:** `model-migrations` · **Date:** 2026-07-05
**Spec:** `./spec.md` · **PRD:** `../prd.md` · **Deps:** none (build first)

## Services touched
`services/backend-api` (2 models + 1 migration), `services/worker-service` (CrmEnrichment mirror).
No new deps. **DB change: yes — one Alembic migration.**

## Phase 1 — Model columns (RED → GREEN)
- **Files:**
  - `services/backend-api/src/models/salesforce_integration.py` (+6 writeback columns)
  - `services/backend-api/src/models/crm_enrichment.py` (+`salesforce_contact_id`)
  - `services/worker-service/src/models/__init__.py` (mirror `salesforce_contact_id` on `CrmEnrichment`)
  - `services/backend-api/tests/test_salesforce_model.py` (extend)
- **Steps:**
  1. RED: extend `test_salesforce_model.py` — assert a new `SalesforceIntegration` defaults
     `writeback_enabled is False`, `contacts_written == 0`, and the 4 nullable columns default `None`;
     assert `CrmEnrichment(salesforce_contact_id=...)` round-trips and defaults `None`.
  2. GREEN: add the 6 columns to `SalesforceIntegration` (copy types/defaults from
     `hubspot_integration.py:38-44`) and `salesforce_contact_id String(100) nullable` to
     `crm_enrichment.py` beside `hubspot_*_id`. Mirror the column on the worker `CrmEnrichment`.
  3. Run the backend↔worker parity test — must stay green.
- **Validation:** `cd services/backend-api && pytest tests/test_salesforce_model.py tests/test_crm_provider_generalization.py -v` (parity + characterization green).
- **Commit:** `feat(sf-writeback): writeback columns on SalesforceIntegration + salesforce_contact_id on crm_enrichment`

## Phase 2 — Alembic migration (RED → GREEN)
- **Files:** `services/backend-api/alembic/versions/<rev>_add_salesforce_writeback_columns.py`
- **Steps:**
  1. `cd services/backend-api && alembic revision -m "add salesforce writeback columns"` (or hand-author
     off current head; precedent `9f56f9a4f999_add_salesforce_integrations_table.py`).
  2. `upgrade()`: `op.add_column` for the 6 `salesforce_integrations` columns (with `server_default`
     for the NOT NULL ones) + `salesforce_contact_id` on `crm_enrichment`. `downgrade()`: drop all 7.
  3. Apply + round-trip on a scratch DB.
- **Validation:** `alembic upgrade head && alembic downgrade -1 && alembic upgrade head`; then the full
  `pytest tests/ -v` for the CRM/health/salesforce suites stays green. Confirm
  `test_crm_provider_generalization.py` byte-identical.
- **Commit:** `feat(sf-writeback): alembic migration for salesforce writeback columns`

## File build order
1. `models/salesforce_integration.py` — writeback config columns.
2. `models/crm_enrichment.py` + worker mirror — `salesforce_contact_id`.
3. `tests/test_salesforce_model.py` — column tests (RED first).
4. `alembic/versions/<rev>_...py` — migration.

## Edge cases / error handling
- `server_default` required so existing rows don't violate NOT NULL on `writeback_enabled`/`contacts_written`.
- Do not touch `provider`, read fields, or `_compute_crm_component`.
- Keep worker/backend `CrmEnrichment` column-identical (parity test).

## Agent execution notes
- Checkpoint after Phase 2: run the whole CRM+health+salesforce test set and confirm the two
  characterization snapshots are unchanged before handing off to config-api / push-task aspects.
