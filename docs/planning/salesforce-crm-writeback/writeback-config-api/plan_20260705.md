# Implementation Plan — writeback-config-api

**Feature:** `salesforce-crm-writeback` · **Aspect:** `writeback-config-api` · **Date:** 2026-07-05
**Spec:** `./spec.md` · **PRD:** `../prd.md`
**Deps:** `model-migrations` (columns), `salesforce-write-client` (validation service)

## Services touched
`services/backend-api` (routes + Pydantic schemas). No new deps. No DB change (columns from aspect 1).

## Phase 1 — Pydantic schemas + GET /status extension (RED → GREEN)
- **Files:**
  - `services/backend-api/src/api/routes/salesforce_integration.py` (schemas + status fields)
  - `services/backend-api/tests/test_salesforce_writeback_routes.py` (new)
- **Steps:**
  1. RED: assert `GET /status` returns the 6 writeback fields with defaults when never configured.
  2. GREEN: add `SalesforceWritebackRequest/Response`, `SalesforceWritebackTestRequest/Response`
     (mirror `hubspot_integration.py:89-107`); extend the status response model + handler to surface
     the writeback columns (mirror `:303-308`).
- **Validation:** `pytest tests/test_salesforce_writeback_routes.py -k status -v`.
- **Commit:** `feat(sf-writeback): salesforce writeback pydantic schemas + status fields`

## Phase 2 — PATCH /writeback + POST /writeback/test + backfill (RED → GREEN)
- **Files:** same route file + test file.
- **Steps:**
  1. RED (mock `validate_writeback_field` + `send_task`): enable-valid → 200,
     `writeback_enabled=True`, backfill enqueued; enable-empty-field → 422; enable-invalid → 400
     `{reason, message}` and stays disabled; disable → 200 disabled; not-connected → 404; member → 403.
     `POST /writeback/test` → `{ok, reason}`.
  2. GREEN: implement `salesforce_configure_writeback` (`PATCH /writeback`) mirroring
     `hubspot_integration.py:396-456`, `salesforce_writeback_test` (`POST /writeback/test`), and the
     inline `_enqueue_backfill_writeback(org_id, db)` (mirror `:459-512`, `WRITEBACK_BACKFILL_CAP=500`,
     filter `provider='salesforce'`, task name
     `src.tasks.salesforce_writeback.push_health_to_salesforce`). Decrypt refresh token via the
     existing read-route helper. All endpoints:
     `dependencies=[Depends(require_admin_or_owner), Depends(require_feature("salesforce_integration"))]`.
- **Validation:** `cd services/backend-api && pytest tests/test_salesforce_writeback_routes.py -v` +
  full route suite green.
- **Commit:** `feat(sf-writeback): PATCH/POST salesforce writeback config + backfill-on-enable`

## File build order
1. `routes/salesforce_integration.py` — schemas, status fields, two endpoints, backfill helper.
2. `tests/test_salesforce_writeback_routes.py`.

## Edge cases / error handling
- Enable-without-field → **422** (not 400). Validation failure → **400** with structured detail; leave
  disabled. Never 500 on a bad token/field.
- Multi-tenancy: scope every query by `organization_id` from JWT.
- Backfill never raises (log + swallow); task-name string must exactly match the push task + trigger.

## Agent execution notes
- Freeze the endpoint contracts here — `writeback-ui` consumes them.
- Confirm the refresh-token decrypt helper name from the shipped Salesforce routes before Phase 2.
