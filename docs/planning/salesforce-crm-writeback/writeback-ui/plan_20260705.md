# Implementation Plan — writeback-ui

**Feature:** `salesforce-crm-writeback` · **Aspect:** `writeback-ui` · **Date:** 2026-07-05
**Spec:** `./spec.md` · **PRD:** `../prd.md` · **Deps:** `writeback-config-api` (contracts)

## Services touched
`services/frontend-web` only (api client + component + settings page). No new deps.

## Phase 1 — API client (RED → GREEN)
- **Files:**
  - `services/frontend-web/lib/api/salesforce.ts`
  - `services/frontend-web/lib/api/__tests__/salesforce.test.ts` (extend)
- **Steps:**
  1. RED: `updateWriteback({enabled:true, field_name:"Rereflect_Health_Score__c"})` PATCHes
     `/api/v1/integrations/salesforce/writeback`; `testWriteback("...")` POSTs
     `/api/v1/integrations/salesforce/writeback/test` and returns `{ok, reason}`.
  2. GREEN: extend `SalesforceConnectionStatus` with the 6 writeback fields (mirror `hubspot.ts:17-24`);
     add `updateWriteback` / `testWriteback` + the 3 interfaces (mirror `hubspot.ts:43-60,95-112`).
- **Validation:** `cd services/frontend-web && npm run test -- salesforce`.
- **Commit:** `feat(sf-writeback): salesforce.ts updateWriteback/testWriteback + status writeback fields`

## Phase 2 — SalesforceWritebackCard (RED → GREEN)
- **Files:**
  - `services/frontend-web/components/settings/SalesforceWritebackCard.tsx` (new)
  - `services/frontend-web/components/settings/__tests__/SalesforceWritebackCard.test.tsx` (new)
- **Steps:**
  1. RED (mirror the HubSpot card test): renders `null` when `!status.connected`; enabling calls
     `updateWriteback` then `getStatus` then `onStatusChange` (not optimistic); validate shows
     ok/reason; status grid + `last_writeback_error` alert render; field Input locked when
     `writeback_enabled`.
  2. GREEN: build the card mirroring `HubSpotWritebackCard.tsx` (Switch, Input default
     `Rereflect_Health_Score__c`, Validate button, status grid, `REASON_COPY`/`STATUS_COPY` adapted to
     Salesforce incl. `ambiguous_contact` + `deferred: daily_limit`). shadcn primitives + CSS vars only.
- **Validation:** `npm run test -- SalesforceWritebackCard && npm run lint`.
- **Commit:** `feat(sf-writeback): SalesforceWritebackCard (enable/field/validate/status)`

## Phase 3 — Wire into the settings page
- **Files:** `services/frontend-web/app/(dashboard)/settings/integrations/salesforce/page.tsx`
- **Steps:** import + render `<SalesforceWritebackCard status={status} onStatusChange={setStatus} />`
  between the connection Card (~`:368`) and the Help Card (~`:371`), gated
  `status?.connected && isAdminOrOwner` (mirror HubSpot page `:400-403`).
- **Validation:** `npm run test && npm run lint && npm run build`.
- **Commit:** `feat(sf-writeback): render SalesforceWritebackCard on salesforce settings page`

## File build order
1. `lib/api/salesforce.ts` (+ test).
2. `components/settings/SalesforceWritebackCard.tsx` (+ test).
3. `app/(dashboard)/settings/integrations/salesforce/page.tsx`.

## Edge cases / error handling
- Refetch-on-change (never optimistic), matching the HubSpot card, so status/last-error stay truthful.
- No hardcoded colors — CSS variables / shadcn only.
- Card returns `null` when disconnected (no dangling UI).

## Agent execution notes
- Build against the frozen config-api contracts; validate end-to-end after that aspect lands.
- Manual check: `./start-all.sh`, connect Salesforce (or fixture status), enable writeback, confirm the
  status grid shows `contacts_written` + `last_writeback_at` and a bad field surfaces the reason.
