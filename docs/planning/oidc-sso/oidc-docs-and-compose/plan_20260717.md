# Implementation Plan — `oidc-docs-and-compose`

**Feature:** `oidc-sso` · **Aspect:** `oidc-docs-and-compose` · **Date:** 2026-07-17
**Spec:** `./spec.md` · **PRD:** `../prd.md` (M13, S-4) · **Branch:** `feat/oidc-sso`
**Low-risk final aspect (docs + config + one nav line). Faithful to shipped behavior — no aspirational claims.**

## Global constraints (verbatim)
- Document only what SHIPPED: RS256-only, `allowed_email_domains` empty=deny-all, one enabled config per
  deployment (D5), JIT→member + verified-email linking, callback `{BACKEND_URL}/api/v1/auth/oidc/callback`.
- pnpm workspace for the sidebar change (`npx eslint <file>`, not repo-wide lint).
- Compose changes must not break the default prod `up` — dev-file or `profiles:` gated; validate with
  `docker compose config`.

## Tasks

### Task 1 — env + SELF_HOSTING docs (M13)

- **Files:**
  - `services/backend-api/.env.example` — add (near JWT keys) `LLM_ENCRYPTION_KEY=` with a comment:
    `# Fernet key encrypting all stored integration secrets (incl. OIDC client_secret). Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`.
    Add a one-line comment on the existing `FRONTEND_URL`/`BACKEND_URL` noting they set the SSO redirect +
    callback base.
  - `docs/SELF_HOSTING.md` — new "## Single Sign-On (OIDC)" section per spec §2. Read a couple of the
    existing integration sections in that file first and MATCH their voice/formatting. Include: overview;
    IdP client registration with the exact callback URL to whitelist; the `/settings/sso` admin flow;
    the deny-all allowlist warning; D5 one-enabled note; JIT+linking behavior; the **RS256-only**
    limitation stated plainly; and a troubleshooting table of the `sso_error=` codes
    (`disabled/state/token/unverified/domain/exchange/config/denied`) with a one-line meaning each.
- **Validate:** proofread against the code (grep the `sso_error=` codes in `src/api/routes/auth.py` to be
  sure the list is complete/accurate); `docs/SELF_HOSTING.md` renders (no broken md).
- **Commit:** `docs(oidc-sso): SELF_HOSTING OIDC section + LLM_ENCRYPTION_KEY in .env.example (M13)`

### Task 2 — Keycloak in docker-compose (S-4)

- **Files:** `docker-compose.yml` — add a `keycloak` service: `image: quay.io/keycloak/keycloak:<pinned>`
  (pick a real recent tag), `command: start-dev`, env `KEYCLOAK_ADMIN`/`KEYCLOAK_ADMIN_PASSWORD` (dev
  creds), a mapped port (e.g. `8080:8080` — avoid colliding with backend `8000`/frontend `3000`), on the
  existing network. **Gate it so it does NOT start with a plain prod `up`** — put it under a
  `profiles: ["dev-idp"]` (so `docker compose --profile dev-idp up keycloak` starts it) OR add it only if
  there is a dev compose file; simplest here is a `profiles:` tag on the service in `docker-compose.yml`.
- **Validate:** `docker compose config >/dev/null` succeeds (valid YAML/schema). If Docker isn't runnable
  in the agent's env, at minimum `docker compose config` (parse-only) — if even that's unavailable, report
  and hand-validate the YAML structure against the other services; do NOT guess.
- Add a short "Testing against a local Keycloak" note to the SELF_HOSTING SSO section (or here) — realm/
  client setup pointers + the issuer URL to paste into `/settings/sso`.
- **Commit:** `chore(oidc-sso): dev Keycloak service for local OIDC testing (S-4)`

### Task 3 — sidebar nav link (closes the oidc-frontend URL-only gap)

- **Files:** `services/frontend-web/components/AppSidebar.tsx` — add ONE entry to the settings nav list
  (around `:139-150`): `{ title: 'SSO', href: '/settings/sso', icon: <an imported lock/shield icon, e.g. KeyRound or ShieldCheck already used in lucide-react — reuse one already imported if possible>, requiredRole: 'admin' as const }`. Mirror the `Integrations`/`AI` admin-gated entries exactly.
- **Validate:** `npx eslint components/AppSidebar.tsx` clean; if there's an AppSidebar test, run it; otherwise
  `npx vitest run` a quick smoke that the module imports. Confirm the icon is imported (no undefined ref).
- **Commit:** `feat(oidc-sso): admin sidebar link to /settings/sso (M11 discoverability)`

## File-by-file build order
| # | File | New/Mod | Task |
|---|---|---|---|
| 1 | `services/backend-api/.env.example` | mod | 1 |
| 2 | `docs/SELF_HOSTING.md` | mod (new section) | 1 |
| 3 | `docker-compose.yml` | mod (add service) | 2 |
| 4 | `services/frontend-web/components/AppSidebar.tsx` | mod (one entry) | 3 |

## Agent execution notes
- Faithfulness over polish: verify every claim against the code before writing it (esp. the sso_error
  code list and RS256-only).
- Keep the Keycloak service OUT of the default prod `up` — a profile tag is the safest lever.
- The sidebar entry is the only code change; keep it a one-liner mirroring existing entries so it can't
  regress the sidebar.
- After this: the final whole-branch review (controller runs it), then finishing-a-development-branch.
