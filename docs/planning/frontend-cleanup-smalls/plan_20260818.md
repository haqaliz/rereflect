# Implementation plan — frontend-cleanup-smalls (2026-08-18)

Source: `docs/planning/frontend-cleanup-smalls/prd.md`. House commit style
(`feat(ui): ... (TDD)` / `test(ui): RED — ...`). Commands from `services/frontend-web`:
`pnpm test` / `pnpm lint` (use `pnpm vitest run <file>` for the RED/GREEN loop).

## Phase 1 — Delete the promo banner

- **Files:** `app/signup/page.tsx` — remove the const (:18), state+effect (:50-66),
  banner JSX (:332-352), gsap `.promo-banner` refs (:82, :100), the
  localStorage/trackEvent reads (:143-146, :177-180); keep the `Sparkles` import.
  `lib/analytics.ts` — delete `promoSignup` (:110-111) + `promoCheckoutStarted`
  (:114-115) + any now-unused imports.
- **Tests:** none pin the banner (verified) — run the signup-adjacent suites
  (GoogleSignInButton + full suite) as the guard.
- **Commit:** `feat(ui): remove vestigial promo banner + dead analytics helpers`.

## Phase 2 — Integrations `[id]` + `new` member redirects

- **Files:** `app/(dashboard)/settings/integrations/[id]/page.tsx` +
  `new/page.tsx` — add the house guard (useAuth + `isAdminOrOwner` +
  `router.replace('/settings/preferences')`, mirroring index page :104-111).
- **Tests:** the existing discord tests (`__tests__/settings/IntegrationDetailPage.discord.test.tsx`,
  `NewIntegrationPage.discord.test.tsx`) render without an AuthContext mock → add
  the `mockUseAuth` treatment (admin role — the IntegrationsListPage tests'
  pattern); add member-redirect tests (the SalesforcePage.test.tsx:157-164
  pattern: `router.replace('/settings/preferences')` + no connect form).
- **Commit:** `feat(ui): redirect members from integration detail + new pages (TDD)`.

## Phase 3 — Create-issue wizard member state

- **Files:** `app/(dashboard)/feedbacks/[id]/create-issue/page.tsx` — page-level
  guard: members see the "Only admins and owners can create issues" copy card;
  owners/admins render the wizard unchanged. `useAuth` is already imported (:74) —
  the `user` var (:135) finally gets used.
- **Tests:** `feedbacks/[id]/__tests__/createIssueDraft.test.tsx` — add a member
  case (copy card shown, wizard hidden); existing owner cases unchanged.
- **Commit:** `feat(ui): gate create-issue wizard to admins/owners with member copy (TDD)`.

## Phase 4 — feedback-sources write guards

- **Files:** `app/(dashboard)/feedback-sources/page.tsx` (Add Source :194-199,
  per-row Pause/Play :319-337, Configure :338-342, Delete :343-359, empty-state
  CTA :220-225, per-type "+" :448-454), `[id]/page.tsx` (Delete :358-365, name
  input :544-549, Active :558-561, Auto-import :574-577, triggers :589-684, field
  mapping :696-741, Save :819-828), `new/page.tsx` (Create :1214-1217) — gate
  behind `isAdminOrOwner`; members keep read-only views (list + webhook/email
  address cards). `pending/page.tsx` untouched.
- **Tests:** no tests exist for these pages → new tests with `mockUseAuth`
  (member: writes hidden/disabled, reads visible; admin: writes visible).
- **Commit:** `feat(ui): gate feedback-sources write controls to admins/owners (TDD)`.

## Phase 5 — Docs + tracking

- DEV-TRACKING: signup-promo-banner-vestigial (:489-494) → FIXED,
  frontend-integration-role-guards (:612-615) → FIXED (shipped summaries +
  placeholders); CHANGELOG entries.
- **Commits:** `docs(...)` per house style.

## Verification

- `pnpm test` full suite green; `pnpm lint` 0 new errors; `tsc --noEmit` no new
  errors in touched files.
