# Implementation Plan — segment-api (2026-07-08)

**Feature:** customer-segments · **Aspect:** segment-api · **Branch:** `feat/customer-segments`
**Sources:** `../prd.md`, `./spec.md` · **Service:** backend-api · **Depends on:** segment-engine (done)
**Execution:** strict TDD, single cohesive agent (all edits in one route module + serializer + docs).

## Impact analysis (verified cites)

- `list_customers` — `src/api/routes/customers.py:247-347`. Validation allowlists at :24-27
  (`VALID_SORT_FIELDS`, `VALID_RISK_LEVELS`). Query build :276-306. Item assembly :322-337.
  `CustomerListItem` schema :39-50. `CustomerProfileResponse` :83-116; route filters returned keys to
  `model_fields` at :397.
- Serializer `serialize_customer_profile` — `src/services/customer_profile_serializer.py:18-88`, Core block
  :57-66. Shared by the internal profile route AND the public REST API (single source → no drift).
- Canonical slug set: import `SEGMENT_SLUGS` from `src/services/segment_service.py` (do NOT re-declare).

## Phase 1 — List filter + column (TDD)

- **Files:** MODIFY `src/api/routes/customers.py`; CREATE/extend `tests/test_customers_segment.py`.
- **Steps:**
  1. Add `segment: str = Query(None)` param to `list_customers` (mirror `risk_level`).
  2. Validate: if `segment` not in the imported `SEGMENT_SLUGS` set → `raise HTTPException(422, ...)`
     (mirror the risk_level validation style near :24-27 / param handling).
  3. Add filter clause in the query builder (:276-306): `if segment: query = query.filter(CustomerHealth.segment == segment)`.
  4. Add `segment: Optional[str] = None` to `CustomerListItem` (:39-50) and populate from `row.segment`
     in the item loop (:322-337) — no recompute at read time.
  5. (should-have) allow `sort_by="segment"`: add to `VALID_SORT_FIELDS` + `sort_column_map`.
- **Tests (RED first):** `?segment=silent_churner` returns only those rows; composes with `risk_level`+`search`;
  invalid slug → 422; `CustomerListItem` carries `segment`; org-scoping unchanged (cross-org empty).
- **Validate:** `pytest tests/test_customers_segment.py tests/test_customers.py -q`.
- **Commit:** `feat(segments): segment filter + column on customers list API`

## Phase 2 — Serializer field (internal + public API) (TDD)

- **Files:** MODIFY `src/services/customer_profile_serializer.py` (Core block :57-66) + `CustomerProfileResponse`
  (`customers.py:83-116`); tests in `tests/test_customers_segment.py` (+ a public-API test if one exists —
  find `tests/test_public*`).
- **Steps:**
  1. Add `"segment": record.segment` to the Core dict (:57-66).
  2. Add `segment: Optional[str] = None` to `CustomerProfileResponse` so it survives the `model_fields`
     filter at :397.
- **Tests (RED first):** internal `GET /api/v1/customers/{email}` payload includes `segment`; the PUBLIC
  REST API customer profile payload includes `segment` (assert via the existing public customer endpoint
  test module — proves the shared serializer carries it with no extra work).
- **Validate:** `pytest tests/test_customers_segment.py <public test module> -q`.
- **Commit:** `feat(segments): expose segment on customer profile serializer (internal + public API)`

## Phase 3 — Docs (no test)

- **Files:** `docs/API.md` (new `segment` field on public customer list/profile + the 6 slug meanings),
  `AI-TRACKING.md` (M3.4: mark Customer Segments shipped, rule-based), `docs/SELF_HOSTING.md` (segment
  meanings + note usage-dependent segments — silent_churner/power_user/dormant-by-usage — need usage events
  wired, else customers fall through to health/sentiment segments or `unsegmented`).
- **Validate:** prose only.
- **Commit:** `docs(segments): document segment field (API.md, SELF_HOSTING, AI-TRACKING)`

## Edge cases

- Null segment (unsegmented / not-yet-computed) → serialize as null; frontend decides rendering.
- Invalid `segment` query value → 422 (not silent empty).
- All reads org-scoped via existing `organization_id` filter — no new cross-org surface.

## Agent notes

- Keep the field/param name exactly `segment` — `segment-ui` consumes this contract.
- Import `SEGMENT_SLUGS` from segment_service; never hardcode the slug list in the route.
