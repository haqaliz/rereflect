# Implementation Plan — docs-tracking-changelog (aspect 6, close-out, runs last)

**Date**: 2026-08-15
**Feature**: `intercom-writeback` (prd.md R10 — docs & tracking are part of "done")
**Aspect**: `docs-tracking-changelog`
**Source**: `docs/planning/intercom-writeback/docs-tracking-changelog/spec.md` + `prd.md`
**Branch**: `feat/intercom-writeback` (worktree `.claude/worktrees/feat-intercom-writeback`)
**Sequencing**: **LAST** — execute only after all code aspects (`db-config-model`,
`config-api-routes`, `dispatch-seams`, `worker-write-client`, `worker-writeback-task`,
`frontend-writeback-card`) have merged into the branch. This plan's placeholders
(merge sha, test counts, migration revision, final task name) must be filled from the
merged state before any step runs.

## 0. Facts gathered (verified 2026-08-15, before writing)

| Fact | Value | Source |
|---|---|---|
| "No write-back." line | `docs/SELF_HOSTING.md:1752-1753`, last bullet of the "Shipped scope:" list in `## Connecting Intercom` (:1742-1753); `---` follows at :1755 | read |
| HubSpot writeback heading style | `### Enable writeback (optional)` (:1173), numbered steps + prose + honest failure sentence | read |
| Outbound-call table | `docs/SELF_HOSTING.md:138-143` — row 3 already lists Intercom ("Only for integrations you explicitly connect and authorize"). **No table change needed**; the write-back calls are covered. Verify only. | read |
| CHANGELOG insertion point | top of `## Unreleased` (:8); newest entries first — current top is "Fixed — Five Celery beat jobs…" (:10). New entry goes above it. | read |
| CHANGELOG tone models | Intercom ingestion `### Added —` entry :284-315 (`*Honest limits:*` italic line at :312-315); two-way-sync removal :348-350; correction-note precedent `**Correction (2026-08-14):**` :329-333 | read |
| DEV-TRACKING P2 entry | `### P2 — intercom-writeback-orphaned (bug/cleanup, NOT STARTED)` :589-602, three checkboxes | read |
| FIXED-block precedent | `oauth-tokens-stored-plaintext` :533-549 (`> Shipped:` blockquote: what shipped → merge sha/PR/date → suite counts → follow-up line → comment-correction line) | read |
| Deferred v2 block | `### Deferred v2 — Intercom (opened 2026-08-01, all NOT STARTED)` :504-519; `intercom-writeback` bullet :512-514 | read |
| Stale refs found by pre-plan grep | `DEV-TRACKING.md:1008,1230` ("two-way sync" in hosted-era backlog records); `docs/planning/**` (archived docs, incl. `_card/card.md`, `ingestion-source-visibility`, `integration-auth-tenancy-hardening`, `worker-write-client` spec/plan); `services/landing-web/lib/integrations.ts:136-171` (OAuth-era entry) | grep |
| `models/integration.py` comment | :19-25 claims OAuth tokens "stored in PLAINTEXT" — **false since P1** (encryption merged `737bbd5`); PRD D4 says treat as encrypted. The P1 FIXED entry (:548) itself promised this comment tracks reality. | read + grep |
| intercom-service mention in ingestion PRD | `docs/planning/intercom-selfhost-ingestion/prd.md:294-297` out-of-scope bullet (its own resolution-style precedent: `~~K2~~ **RESOLVED 2026-07-31**` at :278, `~~**Open question O1:**~~ **Closed**` at :284-287) | read |

## 1. Project setup checklist

Docs-only aspect. No scaffolding, no dependencies, no env. Files touched (exactly six):

1. `docs/SELF_HOSTING.md` — new Intercom write-back subsection
2. `CHANGELOG.md` — Unreleased entry + optional correction note on the two-way-sync entry
3. `DEV-TRACKING.md` — P2 → FIXED; deferred-v2 bullet → SHIPPED; header count fix
4. `services/backend-api/src/models/integration.py` — comment-only truth correction (decision in Phase 5)
5. `docs/planning/intercom-selfhost-ingestion/prd.md` — pointer-only (Phase 6)
6. `docs/planning/intercom-writeback/prd.md` — Decision Record: aspect statuses (optional, house close-out convention)

**Explicitly NOT edited:** `services/landing-web/lib/integrations.ts:136-171` (flag-only,
prd OQ3), `README.md` (zero Intercom mentions), any archived planning doc body.

## 2. Implementation phases

### Phase 1 — SELF_HOSTING.md: "No write-back." → "Write-back (opt-in)" subsection

- **Goal:** the operator-facing claim flips from "No write-back." to an honest
  description of what shipped — exact behavior, defaults, scope requirement, honest limits.
- **Prerequisites:** all code aspects merged (the feature exists to document).
- **File:** `docs/SELF_HOSTING.md`
- **Steps:**
  1. Delete the `- **No write-back.** Rereflect does not add notes to or close Intercom
     conversations.` bullet (:1752-1753) from the "Shipped scope:" list.
  2. In its place insert one pointer bullet: `- **Write-back on resolve — opt-in, off by
     default.** See [Write-back (opt-in)](#write-back-opt-in) below.` (matches the list's
     bold-led bullet style; the anchor is the GitHub anchor for the new heading).
  3. After the bullet list (i.e. between :1753 and the `---` at :1755), add the new
     subsection, heading style matching the file's `###` rhythm (cf. `### Enable writeback
     (optional)` :1173 and `### Connecting Intercom with an access token` :1757):
     `### Write-back (opt-in)` — and its body, in the section's plain operator voice:

     > When you mark an Intercom-sourced feedback item **resolved**, Rereflect can post a
     > note to the linked Intercom conversation and close it, so your support team sees
     > the outcome where they work.
     >
     > - **Off by default, per organization.** Enable it on **Settings → Integrations →
     >   Intercom** (the Write-back card). Choose **note + close** (default) or **note
     >   only** — `note_only` leaves closing to your support team.
     > - **What ships on resolve:** a note containing the resolution note you wrote on the
     >   feedback item (or the default *"Marked resolved in Rereflect."*), posted as your
     >   Intercom app's admin; then the conversation is closed (unless `note_only`).
     > - **Scope requirement.** Your Intercom app must grant **`conversation:write`**.
     >   If the token lacks it, every resolve records `missing_write_scope` on the
     >   settings page and nothing is sent — the integration stays connected. Rereflect
     >   reports the absence; it cannot grant the scope.
     > - **Transitions after enable only.** Conversations already resolved before you
     >   enable the write-back are never touched — there is no backfill.
     > - **`resolved` is the only trigger.** Re-resolving an item after reopening it is a
     >   no-op: Rereflect tracks each item it has already written back, so no second note
     >   is appended to a closed conversation.

     Then an honest-limits blockquote in the section's established `> ### Honest limits`
     form (:1892-1902):

     > ### Honest limits
     >
     > - The write-back fires only on transitions to `resolved` **after enable** — a
     >   backfill is deliberately not offered, because closing a conversation is one-shot
     >   and mass-closing would surprise a support team.
     > - `resolved` is the only trigger in v1; there is no configurable target status.
     > - The dispatch is fire-and-forget: the note lands when the background worker picks
     >   the task up — no response-time claim.
     > - A crash in the seconds between Intercom accepting the note and Rereflect
     >   recording it can duplicate a note on retry (the close itself stays a no-op).
     >   Cosmetic and bounded; no action needed.
     > - Scope (`conversation:write`) is a property of your Intercom app. Rereflect
     >   reports its absence; it cannot grant it.

  4. Verify the outbound-call table (:138-143) still covers the write-back calls — it
     does ("Integrations (… Intercom …) — Only for integrations you explicitly connect
     and authorize"); do **not** edit it.
  5. Do not use the phrase "Two-Way Sync" anywhere in the new copy — describe exactly
     what ships (note + close on resolve, opt-in, off by default).
- **Validation:** `git grep -n "No write-back" docs/SELF_HOSTING.md` → zero matches;
  `git grep -n "Write-back (opt-in)" docs/SELF_HOSTING.md` → ≥ 1; re-read the section —
  the `## Connecting Intercom` section still flows (list → subsection → `---`).
- **Commit:** `docs(self-hosting): Intercom write-back on resolve — SELF_HOSTING subsection + CHANGELOG entry` (combined with Phase 2, mirroring `50a6c688`'s combined changelog+selfhosting commit).

### Phase 2 — CHANGELOG entry (house format, honesty framing)

- **Goal:** the release notes record the feature, the deletion of the orphan, and what
  the claim discipline does and does not claim — in the voice of the Intercom ingestion
  entry (:284-315).
- **Prerequisites:** Phase 1 (commit can be one combined commit).
- **File:** `CHANGELOG.md`
- **Steps:**
  1. Insert at the **top of `## Unreleased`** (above :10), newest-first per house
     convention:
     `### Added — Intercom write-back: note + close on resolve (opt-in)` with this body
     outline (house bullets; bold-led; *honest-limits* italic closer):
     - **Opt-in, off by default.** Per-org toggle on **Settings → Integrations →
       Intercom**; `note_and_close` (default) or `note_only`. Enabling does **not**
       backfill — only transitions to `resolved` after enable fire.
     - **What happens on resolve:** an Intercom-sourced feedback item transitioning to
       `resolved` posts a note to the linked conversation (the resolution note, or the
       default "Marked resolved in Rereflect.") and, with `note_and_close`, closes it.
       Re-resolve after reopen is a no-op (per-item durable marker).
     - **Config API:** `PATCH /api/v1/integrations/intercom/writeback` (admin/owner) and
       the five writeback fields on `GET /api/v1/integrations/intercom/status`.
     - **The orphan is gone.** `intercom_service.py` — the "Two-Way Sync" code the 1.0.0
       notes said "nothing in the app has ever called" — is **deleted**, and its behavior
       is ported into the worker's `IntercomClient` (`add_note`, `close_conversation`,
       `fetch_admin_id`) with the worker error taxonomy (missing scope recorded, never a
       silent `False`). The new task is **registered under exactly the name its
       dispatchers use**, pinned by a name-consistency test, so the "green tests over
       code that never executes" class cannot recur.
     - `*Honest limits:* resolved-only trigger; no backfill-on-enable; the worker task
       is fire-and-forget (no response-time claim); a crash between the Intercom call and
       the marker write can duplicate a note on retry (close remains a no-op).` — mirror
       the :312-315 closer.
     Leave `<test counts>` for the merge fill-in if the executor is not running after
     merge (should be filled — this aspect runs last).
  2. **Correction note on the two-way-sync removal entry** (:348-350), mirroring the
     `**Correction (2026-08-14):**` precedent (:329-333): append one paragraph —
     `**Correction (2026-08-16):** that was true when written. The write-back now ships —
     opt-in, off by default, note + close on resolve — and `intercom_service.py` is
     deleted; see the Added entry above. "Two-Way Sync" copy may return only in this
     shipped form.` — so the historical claim is not left standing as current truth.
- **Validation:** the new entry sits above :10; `git grep -n "intercom_service" CHANGELOG.md`
  → only the new entry + the correction note, both past-tense; the entry's *Honest
  limits* line mentions no backfill and resolved-only.
- **Commit:** same as Phase 1.

### Phase 3 — DEV-TRACKING: P2 `intercom-writeback-orphaned` → **FIXED** with `> Shipped:` block

- **Goal:** the P2 is closed in the house FIXED form, carrying the shipped summary, the
  merge facts, and the two follow-ups.
- **Prerequisites:** merge sha + PR number + suite counts known.
- **File:** `DEV-TRACKING.md`
- **Steps:**
  1. Retitle :589 per the :533 precedent:
     `### P2 — intercom-writeback-orphaned — **FIXED**, merged <merge-sha> (<PR #>, 2026-08-16)`
  2. Replace the three checkboxes (:590-602) with one `> Shipped:` blockquote
     (exact template, fill placeholders from the merged state):

     ```
     > Shipped: the wire-or-delete decision landed as **wire it**. `intercom_service.py`
     > deleted, its behavior ported into the worker's `IntercomClient`
     > (`add_note` / `close_conversation` / `fetch_admin_id`, 404 → `IntercomNotFoundError`,
     > 401/403 → `missing_write_scope` / `auth_error`, 429/5xx → retry ≤3 × 30s).
     > New worker task `src.tasks.intercom_writeback` registered under exactly the name
     > its dispatchers use (backend `send_task` dotted name + worker `delay()`), pinned
     > by a name-consistency test so the "green tests over code that never executes"
     > class cannot recur. Dispatch at all five status-change writers (workflow
     > `change_status`, public-api bulk + single, playbook engine, feedback automation)
     > with per-site seam tests. Durable `feedback_items.intercom_writeback_at` marker +
     > five `IntercomIntegration` writeback columns (migration <migration-revision>,
     > chained to `3cb9a0d1456b`; one alembic head). `PATCH
     > /api/v1/integrations/intercom/writeback` + extended `GET /status` +
     > `IntercomWritebackCard` on Settings → Integrations → Intercom. Merged as
     > <merge-sha> (PR <#>) on 2026-08-16. Backend <BACKEND_TOTAL> passed (<BACKEND_NEW>
     > new vs <BACKEND_BASELINE> baseline); worker <WORKER_TOTAL> passed
     > (<WORKER_NEW> net new).
     > **Follow-up (chore, not started): `landing-intercom-entry-refresh`** — the
     > OAuth-era Intercom entry in `services/landing-web/lib/integrations.ts:136-171`
     > still claims OAuth-only setup and webhook-only ingestion; refresh it to match
     > token-paste + pull + the write-back card, respecting the "claim only what shipped"
     > rule (prd OQ3).
     > The false "stored in PLAINTEXT" comment at `models/integration.py:19-25` is
     > corrected to encrypted-at-rest (P1, merged `737bbd5`) — see the comment fix in
     > this change.
     ```
  3. Keep the detection-lesson point from the old :597-602 checkbox alive in one
     sentence of the block (already present: "pinned by a name-consistency test…").
  4. Do **not** edit `DEV-TRACKING.md:1008` or `:1230` (hosted-era backlog records
     describing the state as it was then — history, not live claims; the FIXED block
     supersedes them). Classify, don't touch.
- **Validation:** heading carries FIXED + merge sha + date; the block names the five
  dispatch sites, the marker, the migration, the config API and the card; test counts
  filled with real numbers.
- **Commit:** `docs(tracking): intercom-writeback-orphaned FIXED + shipped summary` (part of the tracking commit with Phase 4).

### Phase 4 — DEV-TRACKING: deferred-v2 `intercom-writeback` → **SHIPPED**

- **Goal:** the deferred list reflects reality — one shipped, three still open.
- **File:** `DEV-TRACKING.md`
- **Steps:**
  1. Header :504: `### Deferred v2 — Intercom (opened 2026-08-01, all NOT STARTED)` →
     `### Deferred v2 — Intercom (opened 2026-08-01; 1 SHIPPED, 3 NOT STARTED)`.
  2. Rewrite the :512-514 bullet in the file's own resolution style (strikethrough +
     SHIPPED, cf. `~~K2~~ **RESOLVED 2026-07-31**` in the ingestion PRD):

     ```
     - ~~**`intercom-writeback`** — still no write-back; `intercom_service.py` remains
       orphaned with zero production callers.~~ — **SHIPPED 2026-08-16** via
       `feat/intercom-writeback`: the wire-or-delete P2 landed as **wire it** — opt-in
       note + close on resolve, off by default, resolved-only. The "Two-Way Sync" rule
       now resolves: copy may return only in the form shipped. See the FIXED P2 above.
     ```
  3. The other three bullets (:509-511, :515-519) stay NOT STARTED, untouched.
- **Validation:** `git grep -n "SHIPPED 2026-08-16" DEV-TRACKING.md` → ≥ 1; header count
  matches reality; no other deferred bullet changed.
- **Commit:** same as Phase 3.

### Phase 5 — Grep sweep for stale references + `models/integration.py` comment decision

- **Goal:** prove the acceptance criterion — no production code or docs treat
  `intercom_service.py` as live — and land the comment truth fix.
- **Decision (recorded):** **fix the comment.** The spec open question leans fix-only-if
  one-line; this is not a one-line edit (7-line block :19-25) but it *is* a truth-only
  correction with zero code change, it is exactly the claim-discipline this aspect
  exists to enforce, and the P1 FIXED entry (:548) already committed this comment to
  tracking reality ("treat as encrypted", prd D4). Fix it here; if the sweep below
  surfaces a live plaintext write path (it should not post-P1), abort the edit and
  downgrade to flag-only.
- **Steps:**
  1. Replace `services/backend-api/src/models/integration.py:19-25` with a corrected
     comment (mirror the P1 FIXED wording):

     ```
     # OAuth tokens — encrypted at rest with Fernet (P1 `oauth-tokens-stored-plaintext`,
     # merged 737bbd5). Encrypt-on-write at both OAuth callbacks (missing
     # LLM_ENCRYPTION_KEY → 422); decrypt at all 8 read sites (3 backend, 5 worker) with
     # the same never-500 contract; worker-local `_decrypt` mirrors (worker-service
     # cannot import backend-api). Do not restore the old "plaintext" text without the
     # encryption.
     ```
     Comment-only; no code, no migration, no behavior change.
  2. Run the sweep (below) and record the per-target classification in the commit
     message / PR description.
- **Validation — the proof sweep (final gate, run from the repo root):**
  ```bash
  git grep -rn "intercom_service" services/                    # → 0 matches (module + tests deleted)
  git grep -rn "add_note_to_conversation\|get_admin_id" -- services/   # → 0 matches (old names gone)
  git grep -rn "intercom_service" docs/SELF_HOSTING.md CHANGELOG.md    # → only past-tense mentions in the new entry/correction
  git grep -rn "intercom_service" DEV-TRACKING.md              # → only the FIXED block + SHIPPED bullet (past tense)
  git grep -n "No write-back" docs/SELF_HOSTING.md             # → 0 matches
  git grep -rn "Two-Way Sync" -- services/ docs/SELF_HOSTING.md CHANGELOG.md   # → only CHANGELOG:348 (historical, correction note appended)
  git grep -rn "intercom_writeback\|Write-back (opt-in)" docs/SELF_HOSTING.md # → new subsection present
  cd services/backend-api && pytest tests/test_intercom.py -v  # → green (surviving tests); no src.services.intercom_service import
  cd services/worker-service && pytest -q                      # → <WORKER_TOTAL> passed (task + client suite still green)
  ```
  Remaining `intercom_service` hits **outside** those targets are expected and
  intentional: archived planning docs (`docs/planning/**`, `_card/card.md`,
  `worker-write-client/spec.md`+`plan_20260815.md`, this spec) — historical record, do
  not edit. `DEV-TRACKING.md:1008/1230` — hosted-era backlog, do not edit.
- **Commit:** `docs(planning): intercom write-back shipped pointer + models/integration.py comment truth fix` (with Phase 6, or separate if Phase 6 is skipped).

### Phase 6 — Pointer in `intercom-selfhost-ingestion/prd.md` + Decision Record

- **Goal:** the old out-of-scope note points to the shipped card instead of leaving a
  dangling "wire-or-delete stands separately"; the feature PRD records the close-out.
- **Steps:**
  1. `docs/planning/intercom-selfhost-ingestion/prd.md:294-297`: do **not** edit the
     bullet text. Append a pointer line in that file's own resolution style
     (cf. `~~K2~~ **RESOLVED 2026-07-31, favourably.**`):
     `- ~~**Write-back to Intercom**~~ — **SHIPPED 2026-08-16**: the wire-or-delete P2
     landed as **wire it** — opt-in note + close on resolve, off by default,
     resolved-only; `intercom_service.py` is deleted. See
     `docs/planning/intercom-writeback/`.` — history intact, claim resolved.
  2. `docs/planning/intercom-writeback/prd.md`: append a **Decision Record** (house
     close-out convention): the P2 decision executed (wire it), the honest-limits list
     as shipped, each aspect's spec/plan marked shipped, and the OQ1/OQ2/OQ3 verdicts
     (resolved-only shipped; no backfill shipped; landing entry flagged as follow-up).
- **Validation:** `git grep -n "SHIPPED 2026-08-16" docs/planning/intercom-selfhost-ingestion/prd.md` → 1.
- **Commit:** with Phase 5.

## 3. File-by-file build order

| # | File | Edit type | Purpose |
|---|------|-----------|---------|
| 1 | `docs/SELF_HOSTING.md` | Modify | "No write-back." bullet → pointer bullet + `### Write-back (opt-in)` subsection + honest-limits blockquote (Phase 1) |
| 2 | `CHANGELOG.md` | Modify | `### Added — Intercom write-back…` entry at top of Unreleased + correction note on :348-350 (Phase 2) |
| 3 | `DEV-TRACKING.md` | Modify | P2 → FIXED + `> Shipped:` block; deferred-v2 bullet → SHIPPED; header count (Phases 3-4) |
| 4 | `services/backend-api/src/models/integration.py` | Modify (comment only) | :19-25 plaintext claim → encrypted-at-rest truth (Phase 5) |
| 5 | `docs/planning/intercom-selfhost-ingestion/prd.md` | Modify (pointer only) | :294-297 SHIPPED marker line (Phase 6) |
| 6 | `docs/planning/intercom-writeback/prd.md` | Modify | Decision Record + aspect statuses (Phase 6) |

## 4. Testing strategy

- No new tests (docs + one comment). Verification is the Phase 5 grep suite + suite
  reruns to confirm the code aspects' test counts are unchanged by this aspect:
  `cd services/backend-api && pytest tests/ -q` → <BACKEND_TOTAL> passed;
  `cd services/worker-service && pytest -q` → <WORKER_TOTAL> passed.
- Human review pass: re-read the SELF_HOSTING Intercom section end-to-end (:1722-1921)
  to confirm flow, tone, and that no sentence contradicts the new subsection.

## 5. Environment & secrets

- None. The `models/integration.py` edit is a comment; it does not require the Fernet
  key or a running stack. Do not add env vars.

## 6. Edge cases & error handling

- **Merge facts unavailable** (aspect runs before the last PR merges): stop and wait —
  do not invent the merge sha or test counts; placeholders are a red flag in a FIXED
  block (the house rule: "correct the marker in the same commit").
- **Sweep finds a live `intercom_service` import**: a code aspect did not fully merge —
  halt, surface to the lead, do not mark FIXED.
- **Sweep finds a plaintext write path** (contradicting P1): abort the comment edit,
  downgrade to flag-only in the FIXED block, file the contradiction.
- **CHANGELOG has newer entries above :10 by execution time**: insert at the actual top
  of Unreleased, not at a line number.
- **Heading/anchor drift** in SELF_HOSTING (someone reflowed the Intercom section):
  re-locate by the "No write-back." text at execution time, not by line number.
- **Correction-note date**: use the actual merge date, not a hardcoded 2026-08-16.

## 7. Agent execution notes

- Runs last; the two human checkpoints: (1) before Phase 1 — confirm every code aspect
  is merged on the branch and fill the placeholder table; (2) before the Phase 5 commit
  — a reviewer signs off the FIXED block's shipped summary against the actual diff.
- The landing flag is recorded inside the FIXED block's follow-up line — do not create
  a separate entry in this aspect.
- Commit sequence (house style): `docs(self-hosting): …` (Phases 1+2),
  `docs(tracking): …` (Phases 3+4), `docs(planning): …` (Phases 5+6). Do not amend;
  push and open the PR per the house workflow.

## 8. Spec ambiguities flagged

1. **"FIXED markers for the beat/registration-adjacent naming (task name consistency)"**
   (spec.md:23-24) — ambiguous. There is no separate DEV-TRACKING P-entry for a
   write-back task-name bug; interpreted here as a FIXED-style note *inside* the
   CHANGELOG Added entry (the task is registered under exactly the name its dispatchers
   use, pinned by a name-consistency test) plus one sentence in the P2 `> Shipped:`
   block. If a task-name P-entry exists at execution time, add its own FIXED marker.
2. **Precedent name** (spec.md:30) says "`oauth-tokens-encryption-at-rest` precedent" —
   the actual FIXED entry in DEV-TRACKING is titled `oauth-tokens-stored-plaintext`
   (:533). Same precedent, different slug; the plan follows the real heading.
3. **`models/integration.py` fix is not one-line** (spec.md:67-70) — the spec leans
   "fix only if a one-line truth correction"; the edit is a ~7-line comment rewrite.
   Decision recorded in Phase 5: fix it — truth-only, zero code, and this aspect is the
   claim-discipline pass; abort-and-flag fallback defined.
4. **Placement of the new subsection** — spec says :1752 "becomes a new subsection" but
   the line is a bullet inside the "Shipped scope:" list of `## Connecting Intercom`.
   Decided: bullet → one-line pointer, full subsection inserted between the list and
   the `---` at :1755, as a `###` under `## Connecting Intercom` (file's own heading
   rhythm). Confirm at execution if the section moved.
5. **Correction note on CHANGELOG :348-350** — not explicitly requested by the spec;
   included because the house precedent (`**Correction (2026-08-14):**` :329-333) is the
   established way to keep a historical "never worked" claim from standing as current
   truth, and R10 is the honesty obligation. Droppable if reviewers object.
6. **DEV-TRACKING :1008/:1230** — "intercom-service references elsewhere… updated"
   (spec.md:34) is ambiguous about these. Classified as hosted-era backlog history and
   left untouched (documented in Phase 5); the FIXED block supersedes them.
