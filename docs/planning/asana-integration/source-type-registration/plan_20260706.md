# Implementation Plan — source-type-registration (2026-07-06)

**Aspect:** `asana-integration/source-type-registration` · **Service:** `services/backend-api`
**Depends on:** none (independent, parallel with backend-connection). **Precedent:** the `jira` entry in `feedback_sources.py`.

## 1. Setup checklist
- Service: `services/backend-api` only. No packages, no migration. Trivial.

## 2. Implementation phase (TDD)

### Phase 1 — Register `asana` source type
- **Files:** edit `src/api/routes/feedback_sources.py`; test `tests/test_feedback_sources_asana.py`.
- **Steps:**
  1. RED (`tests/test_feedback_sources_asana.py`, mirror `test_feedback_sources_jira.py`): `GET /types` includes an `asana` entry with `requires_integration=False` and `available=True`; `create_feedback_source` accepts `source_type="asana"`.
  2. GREEN: add an `asana` `SourceTypeInfo` in `list_source_types()` (`type="asana"`, `name="Asana"`, `requires_integration=False`, `available=True`, description mirroring jira); add `"asana"` to the `valid_types` list in `create_feedback_source`.
- **Validation:** `pytest tests/test_feedback_sources_asana.py -v` + full `pytest tests/ -v`.
- **Commit:** `feat(asana): register asana as a selectable own-auth source type`

## 3. File build order
1. `src/api/routes/feedback_sources.py` — `SourceTypeInfo` entry + `valid_types`.

## 4. Testing strategy
Single test file mirroring the Jira source-type test.

## 5. Environment & secrets
None.

## 6. Edge cases & error handling
- OSS: no plan-gating branch (all unlocked) — confirm the jira entry has none either and match it.
- **Explicitly NO worker adapter** — do not add `asana` to `services/worker-service/src/adapters/__init__.py` (Jira has none; outbound-only slice).

## 7. Agent execution notes
- Smallest aspect; safe to do first or in parallel. No downstream blockers.
