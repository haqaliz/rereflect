# Implementation Plan — landing (2026-07-06)

**Aspect:** `asana-integration/landing` · **Service:** `services/landing-web` + `docs/`
**Depends on:** none (fully independent, parallel with everything). **Precedent:** the `jira` entry in `landing-web/lib/integrations.ts` + `app/integrations/jira/page.tsx` + the Jira SELF_HOSTING section.

## 1. Setup checklist
- Services: `services/landing-web` + `docs/SELF_HOSTING.md`. No new packages.
- Landing icons use a `{className, size}` signature (distinct from frontend-web's icons).

## 2. Implementation phases

### Phase 1 — Registry entry + icon
- **Files:** edit `services/landing-web/lib/integrations.ts`; create `services/landing-web/components/icons/AsanaIcon.tsx`.
- **Steps:** add an `asana` entry (`slug:"asana"`, `name:"Asana"`, `status:"available"`, coral-pink `color`/`gradient` `#F06A6A`, hero/how-it-works/features/use-cases/FAQs/setupSteps) mirroring the `jira` entry; **copy honest** — "create Asana tasks from feedback" (outbound), not two-way sync. Create the landing `AsanaIcon`.
- **Validation:** `cd services/landing-web && npm run build` (entry compiles) + `npm run lint`.
- **Commit:** `feat(asana-landing): registry entry (available) + AsanaIcon`

### Phase 2 — Integration page + icon maps
- **Files:** create `services/landing-web/app/integrations/asana/page.tsx`; edit `services/landing-web/app/integrations/page.tsx` (slug→icon map) + `services/landing-web/components/landing/IntegrationBar.tsx` (import + marquee).
- **Steps:** mirror `app/integrations/jira/page.tsx` (`getIntegration('asana')`); add `asana→AsanaIcon` to both icon maps.
- **Validation:** `npm run build` + `npm run lint`; `/integrations/asana` renders; Asana appears in the grid + marquee.
- **Commit:** `feat(asana-landing): /integrations/asana page + icon maps`

### Phase 3 — Self-hosting docs
- **Files:** edit `docs/SELF_HOSTING.md`.
- **Steps:** add a "Connecting Asana" section after the Zendesk section (mint a Personal Access Token in Asana → paste PAT into Settings → Integrations → Asana; token is pasted into the app, NOT an env var; create tasks from feedback). **Document the team-scoped-projects limitation** (flat picker may omit team-only projects; v2 will add a team selector). Add a TOC link.
- **Validation:** markdown renders; links resolve.
- **Commit:** `docs(asana): SELF_HOSTING Connecting Asana section`

## 3. File build order
1. `services/landing-web/lib/integrations.ts` (edit) · 2. `services/landing-web/components/icons/AsanaIcon.tsx` · 3. `services/landing-web/app/integrations/asana/page.tsx` · 4. `services/landing-web/app/integrations/page.tsx` (edit) · 5. `services/landing-web/components/landing/IntegrationBar.tsx` (edit) · 6. `docs/SELF_HOSTING.md` (edit).

## 4. Testing strategy
Build + lint the landing site (`npm run build && npm run lint`). Visual check: `/integrations` grid shows Asana as available; `/integrations/asana` renders; marquee includes it. Docs: proofread the PAT flow.

## 5. Environment & secrets
None.

## 6. Edge cases & error handling
- Keep marketing claims honest (outbound task creation, not sync; no accuracy numbers).
- Ensure `status:"available"` (not `coming_soon`) so `getAvailableIntegrations()` includes it.

## 7. Agent execution notes
- Fully parallelizable — assign to its own agent from the start; no dependency on backend/frontend.
- Optional nice-to-have: a `changelog_entries` row announcing the integration (discovery hook) — skip if it complicates the slice.
